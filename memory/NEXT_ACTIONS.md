# Next Actions

- Keep using `./scripts/agent-start.sh` or the equivalent safe startup flow at the beginning of each session.
- Keep using `./scripts/agent-save.sh` or the equivalent safe save flow at the end of each session.
- Approve automatic folder tasks if VS Code prompts for trust/permission.
- Keep using `./scripts/agent-save.sh` or the `Agent Save` VS Code task before closing a session, because editor shutdown hooks are not reliable.
