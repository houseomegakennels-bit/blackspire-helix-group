# Blackspire Canonical Source of Truth

## Authority

The durable authority is GitHub repository `houseomegakennels-bit/blackspire-helix-group`. Code, commits, tests, deployment evidence, and explicit operator-confirmed results outrank summaries. Unsupported current-state claims are `UNVERIFIED`.

- Last reconciled: 2026-07-28 UTC
- Verified implementation anchor (the `Last verified implementation commit` value below, `7a34aecb9f34cb69af41728f0281045fabf4ff64`, PR #51's reviewed head) is the most recently independently reviewed implementation-bearing commit, **not** a claim about the exact current `origin/main` head. Current trusted `origin/main` is resolved live by `scripts/check-living-memory.sh` at validation time and is expected to move ahead of this anchor through canonical-memory-only documentation commits (such as this one) without invalidating it; the checker proves that containment by comparing the aggregate tree diff from the anchor to trusted `origin/main` against an explicit canonical-memory Markdown allowlist, failing closed on any other change. A prior design instead required the doc to record an exact SHA equal to trusted `origin/main`, which made any documentation refresh that advanced main immediately invalidate its own recorded value (PR #37 hit this loop); that equality requirement was removed by PR #38. The prior verified implementation anchor was PR #36's reviewed head `bb727976c47eb430af1587c3f361864aaa7d15f4` (merge `6f40a4443f498e77a8a2ee1e17be6da4a4837e67`). The PR #35 merge remains `693fb03e4596d26e990f87a40508307810cc5e5d`, the PR #34 merge remains `14509f22f07a761bbd7314416287faf6710cfc39`, the PR #32 merge remains `2428f06769c34990f333c95da9937e6b9c936339`, the PR #29 merge remains `f0a7b66853b4c983d6930c1b70d1d94cb5f26f36`, the PR #30 merge remains `588ea6e1156d7d39e7f8a5a7bde44f8c4e519807`, and the PR #28 merge remains `691973870e0048f273fa7e9251d7f78776e3612b`.
- PR #39 (`docs/refresh-implementation-anchor-after-pr38`) is MERGED into `origin/main` as `1a28343f7d3ea3c2968799bc57fb13555fc73787` (reviewed head `a4feeeb2d693f66f85b6fb5814032124bfe47fe5`), a canonical-memory-only next-actions numbering fix on top of PR #38's anchor refresh; `scripts/check-living-memory.sh` reruns `CANONICAL_MEMORY_ONLY_DESCENDANT`/`PASS` against the unchanged verified implementation anchor `bc5a817231c1a325f31a112e256425140bbbf073`. Trusted `origin/main` `1a28343f7d3ea3c2968799bc57fb13555fc73787` was then used, unmodified, to build the immutable release at `/opt/blackspire-command/releases/1a28343f7d3ea3c2968799bc57fb13555fc73787`; the release directory's own `COMMIT_SHA` file records the identical value, and repository validation, the release manifest, ownership/permissions, and the release-tree preflight all passed. A bounded, disposable private blue-green boot then ran the built release on loopback `127.0.0.1:8789` with disposable SQLite state, providers disabled, Hermes mock/bounded mode only, and Telegram dry-run/disconnected: repeated `/health` and root-endpoint checks returned HTTP 200, readiness reported providers unconfigured, disposable task creation/retrieval worked, a harmless task failed closed before any unauthorized provider dispatch, and prohibited funds/credential actions were denied before Hermes/worker dispatch, with no provider call, no real Hermes connection, no Telegram delivery, and no secrets in logs. Teardown terminated the private API/worker descendants, released port 8789, and removed the disposable database/environment/logs/temporary directory; the immutable release and all prior rollback releases were preserved. Read-only before/after host comparison confirmed port 8787, staging port 8788 (PID/uptime), staging and public-preview health (200), and nginx (PID/start time) were unchanged and neither nginx nor staging was restarted; production stayed disabled/inactive with its `current` symlink, environment, and database still absent, providers stayed disabled/manual, and real Hermes/Telegram stayed disconnected. `PRODUCTION_CHANGED: no`. One contained tooling-hardening observation: the private startup wrapper did not forward termination signals to its child processes, so teardown terminated the orphaned API/worker descendants directly; teardown still succeeded with no process left behind, but signal forwarding is tracked as a separate future hardening item before the private runner is called production-grade. This validation makes Gate 3 data-protection, WAL, and backup/restore rehearsal requestable; it does not run Gate 3, does not run Gate 4, does not install or replace the production systemd unit (the installed unit still differs from the reviewed template and remains a later host-activation blocker), and does not create a production environment, database, or `current` symlink. `READY_TO_REQUEST_GATE_3_DATA_PROTECTION: yes`; `READY_FOR_PRODUCTION_ACTIVATION: no`.
- **INSTALLED PRODUCTION SYSTEMD UNIT: REPAIRED** on 2026-07-27 UTC (host-only correction, no code change). The installed `/etc/systemd/system/blackspire-command.service` was a verbatim copy of the 2026-07-21 review template, predating PR #32's bind/port contract and PR #35's interpreter pinning. It was not merely "different from the reviewed template": it declared `ExecStart=/usr/bin/env node`, and systemd's manager PATH excludes `/opt/nodejs`, so it resolved `/usr/bin/node` — verified on this host as **v18.19.1, without `node:sqlite`**. Every production start would therefore have failed immediately, exhausted `StartLimitBurst` (5 in 600s), and left the unit failed. It also omitted the pinned `PATH` and `BLACKSPIRE_NODE_BIN` and still justified its empty capability set from the superseded port 8787. The reviewed template `ops/runtime-ownership/blackspire-command.service` was installed **verbatim** — required, because `scripts/production-preflight-check.js` compares installed against template with exact string equality — as `root:root` `0644`, SHA-256 `4c3f3387e99086f94f2aca4e1755d12b34b6422c6618add1c9ff5b09aa95866b` matching the template byte for byte; the previous file (SHA-256 `ddfff3511557e53cafd0e0e8c18b01d5edc624a08b83c6af4ba5357221b6f7ab`) was backed up outside `/etc` first. `systemctl daemon-reload` was run; the service was **not** started, restarted, enabled, or activated and remains `UnitFileState=disabled`, `ActiveState=inactive`, `SubState=dead`, `MainPID=0`, with no drop-in overrides and `systemd-analyze verify` clean. Verified from systemd's parsed view, not just the file text: `ExecStart` resolves `/opt/nodejs/node-v22.23.1-linux-x64/bin/node` (v22.23.1, `node:sqlite` available), no `/usr/bin/env node` remains, `Environment=` carries the pinned `PATH` and `BLACKSPIRE_NODE_BIN` after `EnvironmentFile=` so the reviewed values outrank the operator-managed file, and `User`/`Group=blackspire`, `WorkingDirectory=/opt/blackspire-command/current`, `ProtectSystem=strict`, `ReadWritePaths=/opt/blackspire-command/shared`, `NoNewPrivileges=yes`, and empty capability sets are intact. The bind/port contract enforced by `ExecStartPre` was exercised directly and fails closed on wildcard `0.0.0.0`/`::`/`*`, non-loopback, absent `BIND_HOST`, reserved 8787 and 8788, absent `PORT`, privileged and malformed ports, and a non-`vps-production` state owner, while `127.0.0.1:8789` clears the bind/port gate. `scripts/lib/node-bin.sh` was confirmed to refuse an interpreter substitution under `BLACKSPIRE_STATE_OWNER=vps-production` when its function is actually invoked, so `ExecStartPre` and `ExecStart` cannot validate one binary and run another. `npm run production:preflight` and `npm run production:preflight:host` both now report `ok=true` with `source=21/21` and **`deployment=2/2`** — the first fully green deployment class. No code or template change was required or made. nginx, TLS, DNS, firewall, the production database, the production `current` symlink, Telegram, live providers, credentials, staging, and releases were untouched; staging (PID `2909886`) and nginx (PID `2909804`) retained identical PIDs and `ActiveEnterTimestamp` values across the change, staging health stayed HTTP 200, and all seven releases were preserved. Gate 4 remains unauthorized and production remains inactive. One residual observation, tracked and not repaired in that scope: `scripts/verify-environment.sh` did not validate `BLACKSPIRE_WORKSPACE_ROOT`, and `scripts/production-supervisor.js` does not import `packages/shared/config.js`, so an invalid workspace root would pass `ExecStartPre` and surface only when the API/worker children load config — failing closed, but as a restart loop rather than a clean preflight refusal. The preflight half is addressed by **PR #50** (`fix/verify-workspace-root-preflight`, reviewed head `3452b33278ed250f00fc4a173d32a66e530fac9c`), **OPEN and not merged**: `vps-production` fails closed on a missing, empty, whitespace-only, relative, nonexistent, non-directory, symlinked, unreadable, untraversable, unwritable, non-git, or application-file-missing workspace root, deliberately stricter than the runtime resolver and never looser. Do not treat this as closed, and do not build an immutable release assuming it is, until that PR merges and its anchor refresh lands; the supervisor/`config.js` coupling stays open regardless.
- **GATE 3 DATA-PROTECTION REHEARSAL: PASSED** on 2026-07-27 UTC, disposable-only, at trusted `origin/main` `73856891c5f6f35f14c09d1ce5acef39f20c4b25`. The immutable release was built at exactly that SHA (`/opt/blackspire-command/releases/73856891c5f6f35f14c09d1ce5acef39f20c4b25`); its own `COMMIT_SHA` records the identical value, it is an ordinary directory (not a symlink), `root:blackspire` `0755` with a `0644` `.release-complete` marker, review/development metadata (`tests`, `.github`, `.claude`, `.agents`, `AGENTS.md`) excluded, `scripts/release-preflight.sh` exit `0`, and the four changed files verified byte-identical (SHA-256) to their Git blobs at that commit. The rehearsal ran the **released artifact**, not the working tree, against a private disposable workspace, and passed 36 checks with 0 failures: explicit-migration-only enforcement (`scripts/migrate.js` refused without `BLACKSPIRE_RUN_MIGRATIONS=true`); WAL confirmed as the journal mode; a WAL-safe backup taken with an open writer connection and 8272 uncheckpointed WAL bytes; backup artifact at mode `0600` with a matching SHA-256 sidecar, `integrity_check=ok`, and 0 missing schema objects; restore to a disposable target preserving the row committed in the WAL, with no temp artifact left by the atomic publish; eight restore failure paths all failing closed with no target created (0-byte backup with a matching sidecar — the exact Gate 3 Finding #1 scenario, schema-less SQLite, truncated backup, checksum mismatch, missing sidecar, production mode without the disposable flag, restore onto the configured live database, and a pre-existing destination left byte-identical); two backup failure paths failing closed with no artifact or sidecar (0-byte source, missing database); a rollback rehearsal on a **disposable** release root covering a switch to a known-good release, fail-closed refusal of an unknown release with `current` unchanged, preservation of the prior release, a round trip back, and a negative proof that the `root:blackspire` ownership contract is genuinely enforced (a `root:root` release is refused and `current` is left unchanged); and confirmation that no Blackspire backup systemd timer or cron entry is installed. The disposable workspace was removed and verified absent. Read-only before/after comparison confirmed port 8787 (docker-proxy PID `2342805`), staging port 8788 (PID `2909886`, `ActiveEnterTimestamp` `2026-07-26 01:02:07 UTC`), nginx (PID `2909804`, `2026-07-26 01:01:54 UTC`), and staging health (HTTP 200) all unchanged, with neither nginx nor staging restarted; production stayed disabled/inactive with its `current` symlink, environment file, and database still absent; all six prior releases were preserved. `PRODUCTION_CHANGED: no`. `GATE_3_DATA_PROTECTION: PASSED`; `READY_TO_REQUEST_GATE_4_PRODUCTION_ACTIVATION: no` — Gate 4 remains unauthorized and the six host-side blockers plus the installed-unit mismatch remain open.
- PR #41 (`fix/restore-reject-empty-invalid-backup`) is MERGED into `origin/main` as `d800d283b11b03d88c519e18abbed0870d9fa750` (reviewed head `40e6d6a873e1579b91004736ba55e2ab87f2e462`), and its reviewed head is the new verified implementation anchor. It closes Gate 3 Finding #1 on both sides of the backup/restore pair. The pre-fix `scripts/restore.js` was independently reproduced printing `{"ok":true}` and exiting `0` for a 0-byte backup with a matching checksum sidecar, because `PRAGMA integrity_check` cannot distinguish a real database from an empty file that SQLite treats as a valid new database with zero tables. `scripts/restore.js` now rejects a zero-byte backup before opening it, validates the backup against the required Blackspire schema before copying, re-validates the copy, and publishes atomically through a uniquely named temp file plus `fs.linkSync` that refuses `EEXIST` rather than overwriting. The 19-table required-schema list moved from `packages/task-engine/db.js` into the shared `packages/shared/schema-validation.js` (`findMissingSchemaObjects`). The fresh independent exact-head review of `3ff2362827e6e8337b57888050ee3f120dca0bc0` found the restore-side change correct and free of defects under adversarial probing (symlinked destination, directory destination, source-equals-destination, production-mode guard, and a full WAL source → `VACUUM INTO` snapshot → restore round trip), and found one real unfixed finding on the sibling script: `scripts/backup.js` reported `{"ok":true}` and wrote a 4096-byte snapshot plus a matching SHA-256 sidecar for a 0-byte source database, and likewise for a valid-but-schema-less one, so the operator's backup routine blessed a snapshot that could only be discovered as worthless during an actual recovery. That finding was reproduced, fixed at `40e6d6a873e1579b91004736ba55e2ab87f2e462`, and regression-tested: `scripts/backup.js` now refuses a source with no tables and independently re-proves the snapshot reproduced the source's table set (new shared `listTableNames`) instead of trusting `VACUUM INTO`. The backup side is deliberately **not** pinned to the current application schema, because a backup taken immediately before a migration legitimately has an older schema and must still be taken. Three of the new regression tests were proven to fail against the unfixed `scripts/backup.js` and pass with it. At the exact merged head: 432 full tests with 423 passed, 0 failed, and 9 host-conditional skips under Node 22.23.1, trusted inventory `36/36/36/36/36` with 0 mutated and 0 remaining descendants; 68/68 focused restore/backup/migration tests (1 root-conditional skip); build, lint, typecheck, secret scan, `npm audit --audit-level=high`, and `git diff --check` all clean; `npm run production:preflight` `ok=true` with `source=21/21` and the pre-existing `installed-unit` host mismatch unchanged. GitHub CI and all four Vercel checks passed at the exact reviewed head.
- PR #44 (`docs/runbook-backup-restore-contract`) is MERGED into `origin/main` as `a78b6395c6176b1dc02d97b7d70807938a3d1beb` (reviewed head `5fa024ec02a7c8677172dd54d9d60579b561da1d`), and its reviewed head is the new verified implementation anchor. It closes tracked next-action item 22 by documenting the enforced backup/restore contract in `docs/VPS_RUNTIME_RUNBOOK.md`, which previously described `PRAGMA integrity_check` as the content check — precisely the check Gate 3 Finding #1 proved insufficient. The runbook now records that backup refuses a source containing no tables, re-derives the snapshot's table set and compares it against the source's instead of trusting `VACUUM INTO`, leaves no artifact or sidecar on refusal, and is deliberately **not** pinned to the current application schema (with an explicit warning not to resolve a pre-migration refusal by migrating first — take the backup, then migrate); and that restore rejects a zero-byte backup before opening it, validates the full required Blackspire schema before writing anything and re-validates the copy, publishes atomically through a temp file plus a link that refuses an existing destination, and leaves no target, no temp artifact, and an untouched source and pre-existing destination on every refusal. It also records that the snapshot is WAL-safe with an open, uncheckpointed writer while retaining the writer-stop procedure. Every documented behaviour was verified empirically during the Gate 3 rehearsal against the built release artifact, not inferred from source reading. Because `docs/VPS_RUNTIME_RUNBOOK.md` is outside the six-file canonical-memory Markdown allowlist, the immediate post-merge checker correctly returned `UNREVIEWED_NON_DOCUMENTATION_CHANGE`; this docs-only refresh records the new anchor and satisfies that requirement. GitHub CI and all four Vercel checks passed at the exact reviewed head. No source, script, test, package, workflow, or release-tooling file changed, no release was built, no Gate was run, and no production state was touched.
- PR #47 (`feat/configurable-workspace-root`) is MERGED into `origin/main` as `e55de773fe3e10e7b1352fd8b2b2b10b40063e87` (reviewed head `d11ac82f62e1bf44e716af7d58b6cd3d77115c30`), and its reviewed head is the new verified implementation anchor. It adds the server-side `BLACKSPIRE_WORKSPACE_ROOT` contract (`packages/shared/workspace-root.js`, exported as `WORKSPACE_ROOT` from `packages/shared/config.js` and seeded as the default workspace's `rootPath`). Hermes uses `workspace.root_path` as the cwd for `createTaskBranch`, `applyEdits`, `inspectChangedFiles`, `commitAll`, and `runAllowed`; that root was hardcoded to `.`, and under the immutable-release architecture the reviewed systemd unit runs with `WorkingDirectory=/opt/blackspire-command/current`, `ProtectSystem=strict`, and only `/opt/blackspire-command/shared` writable, so `.` pointed every one of those operations at a release tree the `blackspire` account cannot modify. The value is read only from the server's own environment — never a request, frontend value, database row, or task payload — and `/api/workspaces` remains a read-only route with no request body reaching `upsertWorkspace`, both pinned by tests. An absent variable preserves the historical `.` default unchanged; when set it is validated at startup and fails closed on empty, whitespace-only, relative, missing, non-directory, symlinked, non-git, symlinked-`.git`, and NUL-byte values, and never degrades back to `.`. Binding, ports, release layout, workspace allowlists, and policy boundaries are untouched. This is the narrowly scoped replacement for the one piece of unique work in PR #22; PR #22's docker-compose service layout, port publishing, container changes, and host bind-mount were deliberately excluded as conflicting with the systemd/immutable-release architecture and the loopback-bind contract that reserves ports 8787 and 8788, and its branch was not merged, rebased, or reused. Validation at the exact reviewed head under Node 22.23.1: 18/18 focused tests, 52/52 adjacent suites, full suite 450 tests with 441 passed, 0 failed, 9 host-conditional skips and trusted inventory `37/37/37/37/37` with 0 mutated; build, lint, typecheck, secret scan, high audit, `git diff --check`, and living-memory all clean; `npm run production:preflight` `ok=true` with `source=21/21`. GitHub CI and all four Vercel checks passed at the exact reviewed head. One full run failed on the `tests/trust-boundary-regression.test.js` flake tracked in next actions; it was investigated rather than retried blind and proven unrelated — it passes in isolation, the branch leaves that test and `scripts/test-inventory.js` byte-identical to `origin/main`, a control run on pristine `origin/main` passed, and a second branch run passed with zero failures.
- PR #50 (`fix/verify-workspace-root-preflight`) is MERGED into `origin/main` as `827c50015f8d3af3f6bb6c2c9f581097edf6b839` (reviewed head `3452b33278ed250f00fc4a173d32a66e530fac9c`), merged 2026-07-28T02:30:21Z with parents base `96d804da2237e3978a9cc96ff06ec8e2d78811dd` and that reviewed head. It closes the preflight half of next-action item 25. `scripts/verify-environment.sh vps-production` now fails closed when `BLACKSPIRE_WORKSPACE_ROOT` is missing, empty or whitespace-only, relative, nonexistent, not a directory, a symlink, unreadable or untraversable, unwritable, not a git checkout (or has a symlinked `.git`), or is missing `package.json`, `apps`, or `packages`. The symlink check deliberately precedes the existence and type checks so a symlinked root is refused rather than followed, and the writability check catches the `ProtectSystem=strict` sandbox rather than the permission bits, since `access(2)` reports `EROFS` for a read-only mount and only `ReadWritePaths=/opt/blackspire-command/shared` is writable. The rules are deliberately stricter than `packages/shared/workspace-root.js` and never looser — the resolver keeps the historical `.` development default, while production refuses an absent variable because `.` is the read-only immutable release — and that asymmetry is pinned by a test. `scripts/production-profile.env.example` documents `BLACKSPIRE_WORKSPACE_ROOT=/opt/blackspire-command/shared/workspace` and `profile-required-keys` in `scripts/production-preflight-check.js` enforces it, so a profile omitting the key can no longer pass review and then fail every start. `packages/shared/workspace-root.js` is unchanged. Independent verification at the exact reviewed head: focused `tests/vps-readiness-hardening.test.js` 39 tests with 38 passed and 1 root-only skip as the non-root `blackspire` account (all 13 workspace-root cases passing, including unreadable and unwritable), 36 passed with 3 permission skips as root, so both runs together exercise all 39; full trusted suite 463 tests with 454 passed, 0 failed, 9 host-conditional skips, trusted inventory `37/37/37/37/37`, 0 mutated, clean containment; `tests/production-bind-boundary.test.js` 46/46 under the pinned interpreter; both preflights `ok=true source=21/21 deployment=2/2`; lint, typecheck, build, secret scan, `git diff --check`, and `bash -n` clean. GitHub CI and all Vercel checks passed at that exact head. Merged as a merge commit with the source branch preserved, since PR #51 was stacked on it.
- PR #51 (`ops/gate4-preparation`) is MERGED into `origin/main` as `437e27e0f2c5ac1e79cd0b93ea44c23790d06486` (reviewed head `7a34aecb9f34cb69af41728f0281045fabf4ff64`, the new verified implementation anchor), merged 2026-07-28T02:41:08Z with parents base `827c50015f8d3af3f6bb6c2c9f581097edf6b839` (the PR #50 merge) and that reviewed head. It was opened stacked on PR #50 and retargeted to `main` only after PR #50 merged; its diff was verified after retargeting to contain exactly its own ten files, with none of PR #50's exclusive files present. It adds `scripts/gate4-prepare.sh`, a **read-only** Gate 4 preparation checker with no mutating mode: it never writes `/etc/blackspire/command.env`, seeds a workspace, creates or switches `current`, touches nginx, TLS, DNS, firewall, Telegram, providers, databases, or releases, and never starts, enables, restarts, or reloads a unit. It fails closed on any unmet prerequisite, keeps reverse-proxy/TLS, monitoring-alert testing, and the backup/migration rehearsal as `MANUAL` operator attestations rather than fabricating evidence, mirrors `ExecStartPre` workspace checks plus the runbook prohibition on a writable workspace inside an immutable release, and pins `gate4-authorization` to `PENDING` unconditionally, so no environment variable can grant it. `--check`/`--validate-only` inspect only, `--plan`/`--dry-run` print the operator command plan and run nothing, and `--json` emits a machine-readable record. It also adds `docs/GATE4_ACTIVATION_CHECKLIST.md`, which names the exact pinned-interpreter suite invocation, and `tests/gate4-preparation.test.js` (23 cases pinning non-mutation, idempotence, secret non-disclosure, malformed-input refusal, non-overwrite guards, exact-SHA planning, the complete profile contract, and the hard authorization boundary). `scripts/production-profile.env.example` and `scripts/production-preflight-check.js` gain the reviewed profile keys the unit requires. Verification at the exact reviewed head: focused suite 23/23; full trusted suite 486 tests with 477 passed, 0 failed, 9 host-conditional skips, trusted inventory `38/38/38/38/38`, 0 mutated; both preflights `ok=true source=21/21 deployment=2/2`. GitHub CI and all Vercel checks passed. Post-merge revalidation at `437e27e0f2c5ac1e79cd0b93ea44c23790d06486` reproduced 23/23 focused and both preflights `ok=true source=21/21 deployment=2/2`, with lint, typecheck, build, secret scan, `git diff --check`, and `bash -n` over all 39 tracked shell scripts clean. Live read-only checker result on this host remains `ready=false`: `source-preflight`, `production-inactive`, `installed-unit`, `rollback-target`, and `runtime-ownership` are READY, while the approved SHA, environment file, workspace, approved release, `current` symlink, production backup, log rotation, three operator attestations, and Gate 4 authorization itself remain outstanding. **This PR prepares Gate 4; it does not authorize it and activates nothing.**
- Last verified implementation commit: `7a34aecb9f34cb69af41728f0281045fabf4ff64` (the recorded verified implementation anchor and PR #51 reviewed head; independently reviewed at that exact head, GitHub CI and all Vercel checks passed, and contained by trusted `origin/main` through merge `437e27e0f2c5ac1e79cd0b93ea44c23790d06486`). Because PR #51 was stacked on PR #50 and merged after it, this single anchor covers both: PR #50's reviewed head `3452b33278ed250f00fc4a173d32a66e530fac9c` (merge `827c50015f8d3af3f6bb6c2c9f581097edf6b839`) is an ancestor of `7a34aecb9f34cb69af41728f0281045fabf4ff64`, and the merged `origin/main` tree `db5aee6acf893cb300848f3c97153378c79e1e4f` is byte-identical to the tree at that anchor, so no PR #50 content sits outside the anchor. The prior anchor was PR #47's reviewed head `d11ac82f62e1bf44e716af7d58b6cd3d77115c30` (merge `e55de773fe3e10e7b1352fd8b2b2b10b40063e87`), independently reviewed at that exact head with 0 unresolved findings. The anchor before that was PR #44's reviewed head `5fa024ec02a7c8677172dd54d9d60579b561da1d` (GitHub CI and Vercel checks passed at that exact head; merge `a78b6395c6176b1dc02d97b7d70807938a3d1beb`). The prior anchor was PR #41's reviewed head `40e6d6a873e1579b91004736ba55e2ab87f2e462` (merge `d800d283b11b03d88c519e18abbed0870d9fa750`), independently reviewed at that exact head with 0 unresolved Critical/High/Medium/Low findings; the anchor before that was PR #38's reviewed head `bc5a817231c1a325f31a112e256425140bbbf073` (merge `ce6dcecf5335233656392248a67326a2524521ea`). The Gate 3 PASS recorded above was rehearsed at trusted `origin/main` `73856891c5f6f35f14c09d1ce5acef39f20c4b25`; PR #44 changed documentation only and does not invalidate that result.
- PR #38 (`fix/living-memory-docs-descendant-policy`) is MERGED into `origin/main` as `ce6dcecf5335233656392248a67326a2524521ea` (reviewed head `bc5a817231c1a325f31a112e256425140bbbf073`). Independent exact-head review returned 0 Critical, 0 High, 0 Medium, and 0 Low findings; GitHub CI and Vercel checks passed at the exact reviewed head. PR #38 replaced the prior self-invalidating living-memory main-equality contract with the fail-closed canonical-memory-only descendant policy (changing `scripts/check-living-memory.sh` and `tests/living-memory-ancestry.test.js`) and independently re-validated the docs-only descendant fix. Because PR #38 changed non-documentation files, the post-merge checker correctly returned `UNREVIEWED_NON_DOCUMENTATION_CHANGE` until this docs-only refresh recorded the new reviewed anchor; this refresh satisfies that requirement. No immutable release was built, no release/Gate 3/Gate 4 was attempted, and no production action occurred as part of the PR #38 merge or this refresh. `READY_FOR_VPS_PRODUCTION_DEPLOYMENT` remains `no`.
- PR #36 (`fix/durable-living-memory-ancestry`) is MERGED into `origin/main` as `6f40a4443f498e77a8a2ee1e17be6da4a4837e67` (parents: base `693fb03e4596d26e990f87a40508307810cc5e5d`, reviewed head `bb727976c47eb430af1587c3f361864aaa7d15f4`), merged 2026-07-26T00:36:23Z. Independent exact-head review approved the fail-closed living-memory ancestry containment checker and its adversarial test coverage with zero findings; GitHub CI and Vercel checks passed at the exact reviewed head. The corrected checker binds to its physical repository, exact trusted origin URL/refspec, exact `refs/remotes/origin/main`, canonical repository-format object IDs, `/usr/bin/git`, sanitized Git configuration/environment, complete non-shallow/non-partial history, and replacement/graft/alternate-free object storage. Exact-direction ancestry is `verified implementation anchor -> trusted origin/main`; reverse ancestry is rejected. The living-memory implementation is now on trusted `origin/main`. Immutable-release creation for this merge was intentionally stopped because canonical memory had not yet been refreshed to reflect the new trusted `origin/main`; **no release was created** by the PR #36 merge. PR #37 attempted that refresh by recording an exact-equality SHA, which made the refresh commit itself immediately invalidate the check once merged (a self-invalidating documentation loop, not a real regression); PR #38 replaced that exact-equality contract with the fail-closed canonical-memory-only descendant policy described above, and created no release, Gate 3, Gate 4, or production activation.
- Separately, a staging/nginx outage (root-caused to a 2026-07-24 10:03 UTC mass service-kill) was recovered on 2026-07-26: `blackspire-command-staging.service` and `nginx` are both active again, local staging health returns 200, and public preview health is restored. Ports 8787 and production remained untouched throughout. The nginx restart-policy and staging clean-exit resilience gap this outage exposed is tracked as a separate future reliability task, not part of PR #36.
- Canonical memory merged by PR #24; this feature branch preserves the canonical structure
- Canonical current state: this file plus the other `docs/BLACKSPIRE_*.md` memory files

This record supersedes `PROJECT_CONTEXT.md`, `WORKFLOW.md`, `AI_WORKSPACE_SYNC.md`, root `memory/*.md`, and historical delivery/planning documents for current-state recovery. Those remain evidence and history, not living authority.

## Verified architecture

- `frontend/` is the Next.js public Blackspire surface deployed separately through Vercel. Its sanitized `/health` route is on `origin/main`.
- Root Blackspire Command is a Node.js control plane: API, Jarvis PWA, worker, Telegram bridge, Hermes orchestration, deterministic policy, workspace registry, approvals, evidence, audit, emergency controls, provider adapters, and SQLite persistence.
- Unified Input adds shared Jarvis/Telegram conversations, ordered events, idempotency, cancellation, channel bindings, a delivery outbox, and mock-safe acceptance tooling.
- Canonical runtime state is persisted before channel delivery. SQLite is validated for a single durable host, not multi-instance/serverless production.
- The planned production-state owner is the durable VPS. Codespaces and Quick Tunnels are disposable development/recovery/test surfaces and must not own production state or uptime.

## Implementation and validation status

| Area | Status | Evidence and limit |
|---|---|---|
| Command control plane | VERIFIED on `origin/main` | Merged through PR #21; historical delivery evidence records 114 tests passing. |
| Public health endpoint | VERIFIED implemented and merged | `afc330c`, merged by `e4ddbc9`; deployment trigger commits `642c0e0`, `cccfbba`. Point-in-time live checks are historical evidence. |
| Wave 1 monitoring | OPERATOR-CONFIRMED complete | `WAVE1_MONITORING_COMPLETION.md` records UptimeRobot HTTP monitoring and email alerts. Current dashboard/alert delivery is `UNVERIFIED`. |
| Unified Jarvis + Telegram | VERIFIED locally and OPERATOR-CONFIRMED on iPhone Safari after main integration and repository-policy fix, not published | Harmless input, shared-conversation follow-up, idempotent replay, Telegram policy denial, cancellation, and bounded mock delivery failure passed on the isolated disposable surface; 139 full tests passed with mock-only providers and disposable state. |
| Real Telegram transport | UNVERIFIED / disconnected | Mock transport, allowlisting, attachments, replay protection, and delivery behavior are tested. No real bot connection is authorized or claimed. |
| Hermes/providers | VERIFIED restricted foundation and subscription Codex worker acceptance | Version 1 restricted contracts and the single-use subscription Codex adapter are locally tested. One final authorized invocation used ChatGPT subscription authentication, passed the exact response contract, completed canonical state, and cleaned disposable state with no API key, retry, fallback, or observed tool call. |
| Restricted worker publication | PUBLISHED (branch only); no PR | Corrected 2026-07-19 against observed refs: `origin/feature/unified-input-foundation` is at `7dbe07f`, not `2ab3394`, and the remote-ref reflog records two pushes (`2ab3394`, then `7dbe07f`). The published range is `029e38a..7dbe07f` (ahead=25, behind=0). No PR exists and nothing is merged to `main`. GitHub deployment evidence shows both `blackspire-helix-group` and `frontend` create Vercel previews for non-main commits. No authenticated Vercel CLI session or approved Vercel token is available to read back or set a branch-specific Ignored Build Step, so no push was attempted. |
| VPS production Command | READINESS TOOLING MERGED; live UNVERIFIED | Immutable release, WAL-safe backup/restore, no-provider profile, production supervisor, hardened path/runtime verification, monitoring templates, shallow-checkout-safe release tests, CI-hardened smoke startup, and collision-safe worker claim readback merged into `main` via PR #26 (`a9602496`). Live supervision, reverse proxy/TLS, least-privileged runtime ownership, backups, stable HTTPS, and activation remain unverified and untouched; six host-side blockers stay open. |
| Codespace recovery/test | BLOCKED | Preparation exists; creation returned a usage-budget HTTP 402 and no resource was created or changed. |

## Important branches and commits

- Trusted `origin/main` is resolved live by `scripts/check-living-memory.sh`, not recorded as a fixed SHA here. PR #38's merge commit `ce6dcecf5335233656392248a67326a2524521ea` and reviewed head/verified implementation anchor `bc5a817231c1a325f31a112e256425140bbbf073` are the current last independently reviewed implementation state; this docs-only refresh's merge is a canonical-memory-only commit on top of it. The prior verified implementation anchor was PR #36's merge commit `6f40a4443f498e77a8a2ee1e17be6da4a4837e67` / reviewed head `bb727976c47eb430af1587c3f361864aaa7d15f4`, which PR #37's merge `5424cde6c4045424f3df8de952fb23e22cd5d500` sat on top of as a canonical-memory-only commit. The prior PR #35 merge commit remains `693fb03e4596d26e990f87a40508307810cc5e5d` with reviewed head `aa2a9cb132d34d45f9453d914f121afb61475b41`.
- `feature/unified-input-foundation`: preserved commits `70e7f36`, `273a25b`, `43735f6`, and `9bdfa5f`; integration uses a merge commit, never rebase or history rewriting.
- Restricted subscription Codex milestones on that branch: implementation `c21db48`, diagnostic classification `aedb9db`, exact-contract correction `ebead48`, and sanitized successful acceptance evidence `85beabc`.
- PR #26 repository-side durable-VPS readiness work is merged by `a9602496`; its host-side activation blockers remain open. PR #29 (`fix: restore immutable release runtime access`, `fix/immutable-release-runtime-access`) is **MERGED** into `origin/main` as `f0a7b66853b4c983d6930c1b70d1d94cb5f26f36` (parents: base `588ea6e1156d7d39e7f8a5a7bde44f8c4e519807`, reviewed head `819d0fc57c6157ecd2bc70dde51216d146d9fc91`), merged 2026-07-22T22:09:46Z. Both the merge commit and the reviewed head are contained in current trusted `origin/main`. Canonical memory previously described this PR as OPEN/draft and carried a live instruction not to mark it ready or merge; that record was stale from 2026-07-22 until this correction, because the session that performed the merge recorded it only in PR #31, which was never merged. See the 2026-07-27 correction entry in `BLACKSPIRE_DECISIONS.md`.
- PR #30 (`fix/prestaging-test-inventory`) is MERGED into `main` as `588ea6e` (head `c62587e`, last implementation commit `7863a64`), including the trusted-inventory contract and root-Vercel boundary repair.
- Backup before integration: `backup/unified-input-foundation-9bdfa5f` at `9bdfa5f`.
- Existing `feature/public-health-route` remains intentionally untouched.
- Readiness commits include `ac7a0ba` (release/backup/no-provider foundation), `73bf031` (explicit disposable restore rehearsal guard), `302e2f0` (rehearsal docs), `bd90fcf` (checksum-required restore), `204a98e` (verified restore guard docs), `42a0d1f` (shallow-checkout-safe release test), `459db4e` (CI-hardened smoke startup wait), and `8c1010d` (worker claim readback collision safety). They are merged through PR #26; live host activation remains separately unverified and unauthorized.

## Tests and evidence

- Merged base delivery: 114 tests, zero failures; build, lint, typecheck, security scan, audit, and whitespace checks passed as recorded in `BLACKSPIRE_DELIVERY.md`.
- Unified Input iPhone milestone at `dccb391`: 5 targeted iPhone tests, 25 targeted regression tests, and 130 full tests passed.
- Dual-environment head `9bdfa5f`: 14 targeted and 132 full tests passed with zero failures/skips under Node 22.23.1; build, lint, typecheck, full-tree secret scan, dependency audit, disposable lifecycle smoke, and whitespace checks passed.
- Post-integration at `b270ad3`: 40 targeted and 132 full tests passed with zero failures/skips under Node 22.23.1. Build, lint, typecheck, full-tree secret scan, dependency audit, and whitespace checks passed. Real Telegram, paid/live providers, and production Unified Jarvis remain `UNVERIFIED`.
- Repository-policy fix at the current branch head: 7 focused policy tests, 35 focused Unified/iPhone/Telegram tests, and 139 full tests passed with zero failures/skips under Node 22.23.1. Repository creation variants return HTTP 403, remain terminal without queued/running events, and record zero Hermes, provider, worker, or approval dispatch. Build, lint, typecheck, secret scan, living-memory, and whitespace checks passed.
- Operator iPhone Safari acceptance at `eceb921`: harmless task, follow-up conversation reuse, idempotent replay, Telegram denial before Hermes, eligible cancellation, and bounded mock delivery failure all passed. The temporary application, authentication material, SQLite workspace, loopback listener, and Quick Tunnel were then removed and verified absent.
- Restricted Hermes readiness at `ac3d887`: 35 focused Hermes/policy/Unified tests and 148 full tests passed with zero failures/skips under Node 22.23.1; build, lint, typecheck, secret scan, living-memory, and whitespace checks passed.
- Restricted subscription Codex acceptance: official Codex CLI 0.144.5 reported ChatGPT login; the final authorized noninteractive invocation passed the exact version 1 operational contract and completed canonical state with no API key, retry, fallback, or observed tool call. Disposable runtime/state were removed. Detailed sanitized evidence is in `RESTRICTED_CODEX_ACCEPTANCE.md`.
- Subscription Codex worker implementation at `c21db48`: 38 targeted and 154 full tests passed with zero failures/skips under Node 22.23.1; build, lint, typecheck, secret scan, living-memory, and whitespace checks passed.
- Subscription Codex diagnostic at `aedb9db`: one authorized invocation produced category `nonzero_exit_with_structured_stdout_error`; a confirmed stdout/stderr classification defect was fixed. Forty targeted and 156 full tests passed with zero failures/skips; build, lint, typecheck, secret scan, living-memory, and whitespace checks passed.
- Final subscription Codex acceptance at `85beabc`: exactly one ChatGPT-subscription-authenticated `codex exec` invocation exited zero, emitted four sanitized structured records with no error event, passed the exact version 1 operational contract, and completed canonical task state. It used no API key, standalone Blackspire Responses API call, retry, fallback, or observed tool call. Forty targeted and 156 full tests passed; build, lint, typecheck, secret scan, living-memory, whitespace, teardown, and scope checks passed.
- Publication preflight reran 40 targeted and 156 full tests with zero failures/skips; build, lint, typecheck, secret scan, living-memory, and whitespace checks passed. The working tree remained clean and no runtime, deployment, push, PR, merge, production change, or Telegram connection occurred.
- Durable-VPS readiness publication: local head `204a98e` passed 59 focused readiness/Jarvis/CSP tests and 228 full tests under Node 22.23.1; build, lint, typecheck, secret scan, living-memory, and whitespace checks passed. Draft PR #26 exposed CI-only test fragility: `42a0d1f` fixes the release readiness test for depth-1 PR merge checkouts, `459db4e` makes the smoke test wait on `127.0.0.1` health before asserting, and `8c1010d` makes task-claim readback filter by both timestamp and assigned worker to avoid same-second CI collisions. Focused acceptance/orchestration/core validation passed 26/26 under Node 22.23.1 after the worker-claim fix. No live service or database was touched.
- The untouched reviewed PR #30 head `5b4d3247aaf5dad5bcd54934ca0c7d8f6a6a85d8` was reproduced under Node 22.23.1 at 262/262 with 29 expected/executed files and exit 0. The PR description's historical 261 count is `UNVERIFIED` and contradicted by this retained exact-head reproduction.
- The historical `54aae9f6253995d6c0987494f8b85441c57aee51` run passed 274/274, but its same-UID isolation claims are `UNVERIFIED` and superseded: random/0600 report paths, child IPC, and prefilled starts did not establish the required trust boundary.
- Final repair `1d9a2ad3ec9b2230aaf8edbca7b2efaf4300cbc6` plus CI portability fix `f3b597fb28ee71bfe6042e0cbc428cc1c9577dd4` eliminates authoritative report files, run nonces, child IPC authority, path-based verification, and wholesale parent-environment inheritance. A trusted Node parent consumes in-process `node:test` enqueue/dequeue/complete events; workers have no `process.send`; test stdout/stderr remain diagnostics only. Linux PID namespaces with a PID-1 signal-forwarding reaper, a dedicated process group, output-endpoint tracking, bounded TERM/KILL cleanup, sticky parent interruption, EOF drain, and real child status gate success. Root enters the namespace directly; non-root CI uses noninteractive sudo only for namespace creation and immediately drops to the invoking UID/GID before tests, with fixed test-only configuration and an OS-variable allowlist. Test identities are bound before/immediately-before/after execution by canonical containment, no-follow opened descriptors, device/inode/type/link count, size/metadata, SHA-256, full-tree mutation watches, and deterministic UTF-8 byte ordering. Clean Node 22.23.1 validation passed 22 focused trust-boundary tests, 38 focused migration/release tests, and complete TAP `1..281`: 281 passed, zero failed/skipped/cancelled. Trusted file totals were intended/discovered/scheduled/started/completed `31/31/31/31/31`, passed `31`, all other file states and mutations `0`; child exit `0`, interruption `null`, streams drained, process group terminated, and remaining descendants `0`. Build, lint, typecheck, secret scan, high audit, and whitespace checks passed. Exact-head CI at `d451106…` failed before tests because hosted-runner user-ID mapping was denied; after `f3b597f…`, exact-head GitHub Actions run `29949288699` passed at published head `8e1693bad23f3c63e402bdca383b34ce93b8a831` with install, migration, test, build, lint, typecheck, scan, and audit steps successful.
- Root-Vercel regression repair `554cb5b44bbe5f402d7fef30cbdb078d36f44639` adds a repository-root `vercel.json` whose sole behavior is `ignoreCommand: "exit 0"`, plus a contract test proving the VPS-owned Command project is skipped while `frontend/vercel.json` remains independently deployable. Node 22.23.1 validation passed 2/2 focused tests and full 283/283 on PR #30; build, lint, typecheck, secret scan, audit, living-memory, and whitespace checks passed. The equivalent PR #29 commit `857124a21e67a6ef7a49a4fc4da89807466346f6` passed 2/2 focused and 269/269 full tests plus the same build-quality gates.
- Advisory review of PR #30 then identified two exact-head defects: test workers could inherit the invoking account's privilege-escalation capability, and sorting lifecycle arrays before validation erased causal ordering. Repair `7863a64c6c34160031a93f650b8f9c7a1f29e68f` executes a tracked/non-ignored repository snapshot as an unprivileged UID with `no_new_privs`, cleared groups, an empty capability bounding/inheritable/ambient set, and a required Yama ptrace boundary; tests cannot use sudo or inspect the trusted parent's memory. Lifecycle transitions are rejected as they arrive, before canonical result ordering. Node 22.23.1 validation passed 21/21 focused tests and complete TAP `1..285`: 285 passed, zero failed/skipped/cancelled. Trusted files were intended/discovered/scheduled/started/completed `32/32/32/32/32`, with zero failed/skipped/canceled/interrupted/mutated identities; child exit `0`, interruption `null`, output drained, process group terminated, and remaining descendants `0`. Build, lint, typecheck, secret scan, zero-vulnerability high audit, living-memory, and whitespace checks passed. This advisory repair is not an eligible submitted GitHub review, so the independent-review gate remains open.

## Environments and integrations

- Public frontend production is owned by Vercel; production configuration remains external.
- Connected Vercel readback confirmed the former PR #29 and PR #30 root failures were identical `STATIC_BUILD_NO_OUT_DIR` failures: the root project (`framework: null`, repository root, default `npm run build`) successfully ran `scripts/build-check.js`, then required an externally configured `public` output directory that the VPS-owned Command build intentionally does not produce. The passing `frontend` project uses Root Directory `frontend`, framework `nextjs`, `frontend/package.json`, and `frontend/vercel.json`; it is a separate public deployment. No missing environment variable caused the root failure, and no secret value was read.
- Repository-root `vercel.json` now makes the root project exit the Ignored Build Step with zero on every ref. Exact-head root deployments for PR #29 at `857124a21e67a6ef7a49a4fc4da89807466346f6` and PR #30 at `554cb5b44bbe5f402d7fef30cbdb078d36f44639` were canceled before build as intended while GitHub recorded `Vercel – blackspire-helix-group` success. The frontend project remained independent and successful. No Vercel project setting, environment value, domain, alias, DNS record, staging service, or production surface was changed.
- VPS is the sole planned Command state owner. Actual live ownership and health are `UNVERIFIED`.
- Codespaces are disposable development/recovery/test environments and are currently limited by exhausted usage credit.
- Quick Tunnel is temporary, expiring, isolated, and mock-only; it is never production.
- Mock/local validated integrations: Hermes, Telegram input/delivery behavior, disposable SQLite, loopback API/PWA, policy denial, cancellation, idempotency, outbox failure, and backup/restore.
- Implemented and restricted-live-validated here: the subscription-authenticated Codex CLI worker path only. Not live-validated here: Claude Code CLI, standalone OpenAI/Anthropic providers, real Telegram, and production Command startup.
- GitHub headless authentication method: root-owned Blackspire GitHub CLI wrapper using an operator-provided fine-grained personal access token. Method name only; no value belongs in Git or memory.

## Security and authority restrictions

- Deterministic policy must run before any model/provider.
- Required approvals cannot be created or widened by an agent or Telegram.
- Enforce workspace/path/tool isolation and budget limits before execution.
- Evidence, audit records, provider attribution, and delivery failures must remain durable and sanitized.
- Emergency stop and Safe Mode deny work before provider dispatch; Telegram cannot alter them.
- No secret disclosure, credential persistence in docs, unapproved push/merge/deploy, host-security changes, production calls, budget increases, real Telegram connection, live trading, or funds movement.
- The location/text of a canonical Constitution is `UNVERIFIED`; do not invent one.

## Blockers and next safe actions

Gate 3 data-protection rehearsal **PASSED on 2026-07-27 UTC** at trusted `origin/main` `73856891c5f6f35f14c09d1ce5acef39f20c4b25`, disposable-only, against the immutable release built at that exact SHA; see the Gate 3 entry above for the full 36-check result and the unchanged before/after host evidence. The remainder of this paragraph is the superseded 2026-07-22 blocked state, retained as history; in particular its statement that "Draft PR #29 is OPEN" is stale — PR #29 merged as `f0a7b66853b4c983d6930c1b70d1d94cb5f26f36` later that same day. Gate 4 production activation remains separately unauthorized. Historical state follows: Gate 3 data-protection rehearsal remained BLOCKED as of 2026-07-22 UTC. PR #28 explicit-migration repair was independently reviewed and merged by `691973870e0048f273fa7e9251d7f78776e3612b`; migration safety is green. PR #30 (deterministic test discovery, trusted-inventory contract, PID-namespace trusted runner, root-Vercel boundary) was independently reviewed and merged as `588ea6e`. Draft PR #29 is OPEN on `fix/immutable-release-runtime-access`, now reconciled with merged `origin/main` (PR #30) by a merge commit that only resolved the three canonical-memory docs and preserved PR #30's newer test-infra content; its former root Vercel failure is repaired, but it remains unmerged and requires its normal review/merge authority and a fresh independent exact-head review. A restricted-staging rebuild previously stopped before activation when its new immutable release `releases/691973870e0048f273fa7e9251d7f78776e3612b` was `0700 root:root`, preventing the `blackspire` runtime from traversing it. PR #29’s source-only follow-up establishes `root:blackspire` / `0755` for both the release root and `releases/` parent; normalizes release directories/executables to `0755` and ordinary files to `0644`; excludes safe review/development metadata from the artifact; and creates `.release-complete` only after full type, containment, ownership, and mode validation. Its one shared validator is used by release creation, explicit preflight, switch, and rollback; it fails closed for symlinked ancestors and every dangling, looping, or out-of-tree canonical symlink while allowing only canonical in-tree targets. Disposable archived-source fixtures cover relative, absolute, nested, chained, dangling, and looping links before any candidate can receive the marker or replace active state. It preserves completed releases/shared state and the active link on failure. PR #29 is source-only and has not rebuilt or switched staging. Read-only host evidence shows `/opt/blackspire-command` as staged host-readiness infrastructure, with staging loading `releases/0a9affacaf13dd1b040c5d96eb112d979ab59444`; the inactive candidate `releases/691973870e0048f273fa7e9251d7f78776e3612b` is `0700 root:root`. There is no `/opt/blackspire-command/current` symlink, `/etc/blackspire/command.env`, or production `shared/command.sqlite`; `blackspire-command.service` is disabled/inactive. The separate staging service remains active on loopback `127.0.0.1:8788`; production port `8787`, API/worker PIDs, providers, and Telegram were not changed. Providers remain disabled, Telegram remains disconnected/dry-run, no Gate 3 backup, restore, WAL rehearsal, timer installation, production-path write, service restart, or production cutover occurred. `READY_TO_REQUEST_GATE_4_PRODUCTION_ACTIVATION: no` and `READY_FOR_VPS_PRODUCTION_DEPLOYMENT: no`.

Current blockers: PR #26 readiness tooling is merged into `main`, but production deployment is not authorized. Six host-side blockers remain, each requiring separate explicit approval: (1) approved reverse proxy and TLS installation and verification; (2) least-privileged non-root runtime ownership provisioning; (3) installed and alert-tested monitoring and log rotation; (4) separately approved real production backup and migration rehearsal or execution; (5) an exact known-good live release and database rollback target recorded; and (6) readiness tooling incorporated into the deployment artifact. Reverse-proxy/TLS and runtime-ownership plans plus credential-free verification are prepared as reviewed `ops/` templates only; nothing is installed or applied. Codespaces usage credit is exhausted; every new subscription Codex task still requires separate scoped approval; real Telegram, production Command state, and other live providers are unverified; SQLite requires a single-host production design; Constitution authority is unresolved.

Immediate safe actions:

1. Preserve the unpublished Unified Input stack and its backup branch without rewriting history.
2. Preserve the operator-confirmed iPhone acceptance and verified teardown evidence; do not restage unless a new acceptance need is explicitly approved.
3. Continue credential-free local checks only when they validate new work or reconcile changed state.
4. Complete production readiness, backup, monitoring, stable HTTPS, and rollback verification before any VPS promotion.
5. PR #26 readiness tooling is merged; do not deploy or activate any release until the six host-side blockers are closed and a separate bounded production approval is given. Treat the prepared `ops/` proxy/TLS and ownership templates as review artifacts, not installed configuration.

Operator-only actions include spending/budget changes, credential provisioning, GitHub authorization, device acceptance, real Telegram connection, production/DNS/provider/host-security changes, approval-policy changes, emergency-control changes, trading, and funds actions.

## Rollback and recovery

- Preserve commits; use reviewed `git revert`, never reset or history rewriting, for shared-history rollback.
- Recover pre-integration Unified work from `backup/unified-input-foundation-9bdfa5f`.
- A protected, owner-only Git bundle of `feature/unified-input-foundation` at `85beabc` was created outside the repository and verified as complete. Sanitized SHA-256: `bf22dd01d8433e516e084a323740f5b39403dabe846e08acd21f4db9c7a5a9b6`.
- Use tested backup/restore commands only against an explicitly identified stopped local/disposable database.
- Disposable iPhone state and tunnel must be stopped and deleted without touching durable VPS state.
- Recovery begins with `git fetch`, worktree/branch/status inspection, this file, then affected code/evidence. Never copy production state into Codespaces or tests.
