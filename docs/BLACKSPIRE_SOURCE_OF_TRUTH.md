# Blackspire Canonical Source of Truth

- Last verified implementation commit: `96a8db5616d0753319bf16e6ee6826a5c32e928e`

## 2026-09-21 — existing cancellation fixture rejection race corrected locally

PR #149 CI run `35565499093` failed because the unchanged Buyer cancellation fixture created an abort-rejectable promise and then yielded through `setImmediate` before attaching its rejection handler. CI recorded both assertion groups passing followed by an unhandled `Error: aborted`; this is an asynchronous fixture defect, not a passing suite. Three isolated reruns of the old fixture passed, confirming the failure is scheduling-dependent. The local test-only correction returns the adapter promise synchronously, waits for explicit adapter entry before cancellation, and explicitly releases the response after cancellation/emergency-stop state is set. Existing cancellation and non-disclosure assertions remain unchanged. Three repeated combined cancellation/role-boundary runs pass 4/4 each. No runtime source was ported from the Zola release branch. Independent review and a fresh exact-head CI run remain required; no production action occurred. The known historical memory-anchor failure remains unchanged.

## 2026-09-21 — generic operator role boundary repair prepared locally

Review of main `c97118090f3f5c806a8565213ee5c43a2dfc0242` found that generic signed-in guards still admitted the new demo/client roles to Studio import and video-render actions and operator pages. The isolated fix makes those guards require `admin` or `beta_tester`, preserving anonymous denial and ordinary operator access. Behavioral tests execute the existing import/render handlers with stubbed provider callbacks and prove active/expired demo, client, unassigned and anonymous callers stop before route work; both focused tests pass under Node 22.23.1. Root build, lint and syntax/typecheck gates pass. Frontend production build, independent exact-commit review, CI and deployment remain UNVERIFIED for this isolated branch. The living-memory checker still fails on the pre-existing historical anchor; the anchor and checker are unchanged. Main retains the known defect until an approved fix is merged and deployed. No push, merge, deployment, production mutation or provider action occurred in this work.

## Protected client demonstration access (2026-09-21 UTC)

Local branch `feat/temporary-demo-access` adds an explicit server-owned role model (`admin`, `beta_tester`, `demo_viewer`, and `client_only`) backed by Supabase `app_metadata`. The original first-user admin fallback remains for migration safety, while untagged later accounts now fail closed to `client_only`. Admins create private one-time invitation links instead of choosing a client's credentials. The recipient opens the branded invitation page, chooses their own email and password, and receives an expiring `demo_viewer` role. Only a SHA-256 hash of the 256-bit invite token is stored. The link has its own expiry and becomes unusable after the first successful claim. The `/demo` surface is read-only and exposes live aggregate counts plus a bounded buyer-entity sample without seller contacts, mailing addresses, document links, exports, or workflow controls. Expired viewers are redirected to a dedicated expiry page. Full operator routes under `/workspace`, along with legacy buyer, seller, harvester, search, workflow, beta, and workspace-directory surfaces, now require an admin or beta role. Primary browser-facing buyer-report, search-job, seller-lead/source/summary, Sentinel, and Harvester-entity APIs enforce the same workspace role boundary. Public auth also accepts the deployed `NEXT_PUBLIC_SUPABASE_ANON_KEY` name, and new beta signups receive their role in server-owned metadata. Frontend lint and the Node 22.23.1 production build pass locally. The production `demo_access_invites` migration is applied and verified with RLS enabled, the expiry index present, and no direct table privileges for anonymous or authenticated browser roles. Production website deployment, account migration, Peggy's invite creation, and browser acceptance of the new flow remain UNVERIFIED.

## Public marketing refresh (2026-09-20 UTC)

A frontend-only refresh on `feat/blackspire-marketing-polish` replaces the parent homepage with customer benefits, the approved tagline, an existing labeled demonstration, and direct inquiry actions. Services, demos, navigation, footer, and contact now use plain language. Public contact is `Blackspirehelix@gmail.com` and `743-444-0586`. The contact form prepares an email in the visitor's mail app; it does not claim server delivery. Product/workspace routes and backend authorization are unchanged. Node 22.23.1 frontend build (including TypeScript) and lint passed locally. Production publication and browser acceptance are UNVERIFIED at this checkpoint. The living-memory checker reports a pre-existing stale implementation anchor on main; no anchor was advanced or check weakened by this marketing change.

## Nexus read-only capability validation (2026-09-04 UTC)

Local branch `feature/nexus-read-capabilities` is at `a26f93ddcf4a44f2e1a6e9490f2dc93adb34266e`, two commits ahead of `origin/main` at `96a8db5616d0753319bf16e6ee6826a5c32e928e`. It adds the server-owned `nexus.enrichment.status` capability with `nexus.enrichment.read`, backed by persisted `nexus_contacts` status reads only; raw phone, raw email, provider payload, paid provider calls, enrichment triggers, and read-path database mutations are not exposed or performed. Focused Nexus, Buyer, Deal, and capability-orchestration tests pass and exit naturally. The prior Deal mixed-routing discrepancy is fixed by restoring the `analysis` keyword to the existing Deal analysis matcher. Root lint, typecheck, build, secret scan, and high-severity npm audit pass locally. The repository trusted runner is `UNVERIFIED` in this sandbox because its required user-namespace snapshot `chown` fails before test execution; a direct uncontained full run is not valid evidence because it lacks the runner's isolation contract. Nexus has not been pushed, reviewed, merged, or deployed.

## PR #120 final P1/P2 correction (2026-09-02 UTC)

Branch `feature/jarvis-password-auth` continues the authenticated Blackspire Command browser/PWA path on `origin/main`. It replaces token-shaped browser login with asynchronous password verification against `COMMAND_ADMIN_PASSWORD_HASH`, preserves `COMMAND_ADMIN_TOKEN` solely for explicitly enabled bearer clients, and enforces one process-wide four-derivation admission bound that refuses excess scrypt work immediately with no waiting queue. Session issuance still requires verified credentials and a server-resolved canonical principal. Production requires a valid supported password hash; development retains only an explicit non-browser legacy fixture path.

## PR #119 capability/security history

Branch `feature/jarvis-capability-registry` extends the existing Blackspire Command/Hermes path with one server-owned capability contract and deterministic fail-closed registry. The first registered capability, `seller.opportunities.search`, is read-only, requires the persisted `seller.opportunities.read` workspace grant, and reaches the existing Seller Engine Supabase `seller_leads`/`properties` owner only through a token-authenticated, exact-workspace internal adapter. It returns bounded, non-contact opportunity fields and canonical evidence/result text; Jarvis receives no database, filesystem, credential, provider, or registration authority.

The first exact-head review reported three P1 and five P2 defects. The local correction makes successful attempt/task/evidence/event finalization one authority-fenced transaction; blocks Seller-bearing task, evidence, export, conversation, event, and action surfaces without the current Seller permission; distinguishes empty results from Seller configuration/query failure; and enforces byte-safe token comparison, object-only JSON, request-scoped result counts, streaming response bounds, and deterministic query ties. Focused acceptance includes disclosure ablation plus cancellation, grant, emergency-stop, claim-loss, oversized stream, and version-skewed adapter cases. This correction remains local: new exact-head CI/review, merge, deployment, production Seller configuration, and production verification are not yet claimed.

PR #117 is merged in `main` and supplies the canonical server-authoritative Jarvis task result consumed by this slice. PR #118 remains open at documentation head `d269015b4ac7a71e3c6e4eb09e5b1fe700757428`; its architecture remains consistent, but an exact-head external review was unavailable, so no approval or merge is claimed.

Local follow-up to `759970c07c8b6c86f60179a687252edeff75200e` rejects API credential-group numeric aliasing with either worker-allowed GID. The offline session-fence primitive now independently requires a successful systemd proof that the API unit is `inactive` before revocation; the runbook is fail-fast and checks the same state before invoking it. Targeted regressions and mutations passed. This correction is local and unpushed; production changed: NO.

## Canonical Jarvis result implemented and validated locally (2026-08-30 UTC)

- Added a canonical frozen capability contract and deterministic server registry, then extended existing Hermes rather than creating a second orchestrator. The first registered capability is `seller.opportunities.search`, fixed to read-only intent, low risk, no approval, zero external cost, exact task workspace, and the persisted `seller.opportunities.read` grant.
- Added durable capability dispatch/finalization on the existing provider-attempt owner. Grants are resolved from the canonical persisted principal and rechecked before dispatch and disclosure; cancellation, emergency stop, worker ownership loss, and uncertain post-dispatch failure remain absorbing/no-replay outcomes.
- Added a token-authenticated, exact-workspace internal Seller adapter that queries the existing Supabase Seller Engine owner and returns bounded canonical property/lead fields without contact data. No parallel real-estate database, unrestricted division access, or caller-controlled capability authority was introduced.
- Deterministic focused tests pass 11/11, the broader compatibility lane passes 79/79, root lint/typecheck/build and frontend lint/build pass. A deliberate removal of the permission guard failed the targeted authorization regression before restoration. The full trusted lane recorded 1,140 tests: 1,130 passed, 1 failed, and 9 host-conditional skips; the sole failure was `launcher-signal-containment`, which immediately passed 6/6 in isolation and does not touch changed files. CI, review, merge, deployment, production Seller configuration, and live data acceptance are not yet claimed.

Independent read-only review approved exact repair `069d6ffca7f285b36d651f2b8c4c8908ad18001a`; the runtime/test diff SHA256 is `92a3990aae445a130ea447efd0a2e2e5fb926c02c47130ff36ee519d009f0474`. Draft PR #149 contains this repair. Production remains unchanged; remote CI and separate production approval are still required.

## 2026-09-21 — isolated interactive demonstration upgrade

User chose a separate workspace. The candidate now keeps demo_operator denied from production workspace pages/APIs and stores editable practice records exclusively in demo_workspaces, keyed by the authenticated user. Browser roles have no direct table privileges; RLS is enabled. Saves use revision checks, same-origin verification, bounded fields and explicit actions. The isolated pipeline computes a labeled 70% offer ceiling, advances a practice stage, and creates a follow-up; it does not call external providers. Live buyer activity and aggregate counts remain a distinct read-only reference. Missing/invalid/expired access timestamps fail closed. Existing real-estate route handlers now check production workspace authority before work. Eight focused tests cover calculations, roles, expiry, unsupported actions, owner scoping and concurrent-update rejection. Frontend lint, production build and all eight focused tests passed locally under required Node 22.23.1. Deployment acceptance remains pending at this checkpoint. The demo_workspaces migration has been applied and browser privileges verified denied. No full production workflow equivalence is claimed for this practice workspace.

First-user admin compatibility also honors explicit demo roles, and Social OS rejects demo accounts even when they are the oldest registered user. The focused legacy-authority regression passes locally.

Database acceptance identified existing authenticated-all policies. Applied restrictive demo-role denial across the 62 current public RLS tables and storage.objects, preserving pre-existing policy behavior for other roles. A transaction under authenticated/demo_operator returned zero Harvester/Sentinel records and zero affected live updates. Advisors report only existing function/search-path/password-protection warnings; no new demo-table finding. Future public tables must retain this demo denial when granting browser access.
