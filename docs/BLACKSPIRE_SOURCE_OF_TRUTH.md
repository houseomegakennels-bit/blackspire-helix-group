# Blackspire Canonical Source of Truth

## 2026-09-08 — timeout recovery, strict CI and connected migration preparation

The timeout left release `a92ef6a` on local, remote and PR125, plus one legitimate journal regression edit. That edit now holds the competing recovery winner until both processes report, removing its scheduler-dependent timeout. It was preserved and independently verified. The sole landed commit after `6ccfd46` contains the candidate collector, n8n transition command, configuration installer and corrected routing canary; those were not rebuilt from scratch.

Retained maintenance run 34224612648 attempt two records four acknowledged canary mutations and complete cleanup. Two final observations agree on empty live version `0a0ae2b9-78f3-4c2e-8034-0faad8118406`, no staging and two history entries; all 280 hosts returned to their original 404/410 responses without markers. The active canary reached 242 hosts, including canonical, release, candidate and fixed recovery immutable URLs. One alias returned a normal application 404 without the marker; 37 returned 410. These are coverage limits, not authorization denials. A bounded GET-only alias-detail diagnostic now investigates the gap without printing arbitrary routing/header values. Full authority containment and functional rollback remain UNVERIFIED.

Independent review found that successful CI 34224618637 for a92 recorded base `53adf74` while actual main was `eb3f5d1`. The host verifier now reads the actual main ref, requires exact head/base/origin/attempt, positively verifies the complete mandatory job and steps, orders runs explicitly and repeats observations to catch movement or reruns. A successful aggregate cannot hide skipped work. The new gate correctly refuses the historical successful run; new exact-head/current-main CI is required after push.

The connected migration adapter regenerates the reviewed package and prepares a fixed-project `apply_migration` request without a native database password. It establishes timeout settings before its guarded body, rejects missing transaction-local timeout settings, checks nonsuperuser identity and the shared advisory lock, and refuses native/connected duplicate histories. Its CLI shares the durable native intent namespace and validates exact sanitized history digests and the actual API-generated version. Nine actual PostgreSQL 17.6 rehearsal groups pass, including modeled API atomic body/history, row preservation, cross-transport rejection, competing locks, failed history append and missing-timeout-setting refusal. The actual Cloud endpoint transaction/history contract remains an execution-time gate; no production SQL was applied and absence never authorizes retry. This preparation bridge is not the global release commander. Actual protected CLI verification caught a top-level-array mismatch with the metadata reader; reconciliation now requires the exact `{rows: [...]}` object envelope, with a reader/reconciler regression.

The six-read collector adds an optional native read-only database observer that persists its baseline before admission and compares complete content and tuple-version digests for fifteen fixed division tables afterward. Strict schema, row ceilings, clocks, generation and journal order fail closed; same-value updates are detected. An actual authenticated PostgreSQL role witness checks an existing real owner and a distinct real user against one SearchJob. Parent independently repeated six PostgreSQL 17.6 groups; a separate connected read-only production snapshot covered roughly 153,000 rows and the real owner/foreign witness passed. Those observations were not paired with live six-read dispatch. Other application-owner boundaries, complete mutation-attempt auditing and paid-provider egress remain unverified; the CLI still returns nonacceptance. Connected SQL preparation does not pretend manually imported snapshots bracket the collector.

Fresh read-only n8n observations still match active and published revision `cdd141ba-8d20-4981-b598-6af8e35aff86`; both existing credentials resolve in the complete thirteen-entry inventory. The current transition inspector also positively identifies the unchanged baseline using GET only. The provider ZIP and eleven checksums remain exact, with zero known-secret matches; no ticket submission or provider response is evidenced. Existing disk envelope passes with 5,493,784,576 bytes free against 36,241,408 required; remeasure the final artifact. API/worker remain held, migrations unapplied and PR125 unmerged. Internal production acceptance and global orchestration work remain; provider action is not yet the sole blocker.


## 2026-09-08 — actual candidate collection and guarded workflow transitions

Recovered release `6ccfd46` with unchanged reviewed main `eb3f5d1`, clean source and canonical CI 34192627078 passing. The prior unpublished canary was independently confirmed discarded in run 34192623240: one exact-version discard received HTTP 200, then current routing and complete history were empty and explicit-version lookup returned 404. No routing was published. The original canary now follows observed optional flags and current/history/explicit-version semantics, uses durable full-write intent, refuses intervening routing history and reconciles uncertain acknowledgements without repeating mutations. Thirty-four focused cases pass independently. A fresh exact-path coverage experiment is prepared; coverage is not authorization denial and no new live result is claimed before retained evidence.

The collector adds `--candidate`: a real loopback API receives authenticated HTTP admissions in a fresh network namespace with no external route, then actual SQLite claims, dispatcher execution and six durable receipts use synthetic frontend data. A distinct generated session receives task-disclosure denials. Reopening the durable collector journal adds zero tasks and zero dispatches. Parent and independent runs pass; twelve collector/candidate groups and eighteen offline/migration-boundary regressions pass. Output explicitly limits generation and frontend identity to the disposable process and synthetic routes, lists the five files covered by its source digest, and retains `livePass:false`. Production generation integration, authoritative division row deltas, real owner-policy witnesses and production egress evidence remain required.

A concrete n8n transition command now prepares, inspects, deactivates, updates, publishes, reconciles and safely deactivates the candidate for rollback. It binds the protected package and baseline, fixed Cloud workflow and key path, current source/remote/PR/CI, actual canonical writer readiness and exclusive administration window. Forward updates require inactive intake and quiescent execution inventory. Mutation intents are durable; uncertain operations are observed rather than retried. Rollback does not depend on surviving credentials, writer health or an unchanged remote branch. Thirteen HTTP lifecycle and filesystem crash/replay groups pass independently. GET-only reconciliation recovers only a proven dead owner using boot/PID/start-time identity, preserves the old lock inode, and holds a kernel lock through recovery; a competing-process regression covers the unlink race. CI binds both PR head and current main. Fresh read-only Cloud observations match the original active and published revision; no workflow mutation occurred. This transition lane is not the complete global release commander.

The API-only configuration installer verifies root/NSS identity separation, stopped canonical units, sealed production artifact, explicit protected credentials, fixed database/CA/workspace/binding and scoped PostgreSQL allow/deny checks before publishing immutable API-private configuration and an API-only drop-in. It uses atomic no-replace files and preserves conflicting state. Independent review found and fixed an inherited-ACL exposure window: the empty temporary inode is checked before chmod or secret writes. Six actual filesystem tests cover that case, source and identity drift, unknown files, symlinks/hard links, failed authority checks, failed journal intent and partial-run reconciliation. It does not reload systemd, start services or activate writer authority.

One fresh provider catalog capture exactly matches the reviewed baseline (17 objects, 32 roles, 22 memberships, 65 columns). The provider archive and eleven member checksums remain valid; the ticket is unsent. Live n8n remains revision `cdd141ba-8d20-4981-b598-6af8e35aff86`, with both existing credential references resolved in a complete thirteen-entry inventory. Measured disk headroom passes without cleanup. Production remains gated: API/worker stopped, migrations unapplied, workflow unchanged and PR125 unmerged. Full immutable containment, rollback acceptance, production collector acceptance and global release orchestration remain unfinished. The full local suite passed 1,628 tests with twenty skips and zero failures; all 151 files completed and child containment passed. Build/lint/typecheck/audit, memory, whitespace and known-secret comparisons pass. Exact new-head CI and retained routing evidence must be verified after this tranche is committed.

## 2026-09-08 — production collection and release primitives

The protected six-read collector now has a concrete dry-run/production CLI, durable intent-before-admission journal, exact SQLite idempotency reconciliation, actual systemd/process/socket/database binding, observed frontend SHA checks and authenticated foreign-principal task-disclosure checks. It distinguishes worker identity from systemd generation and binds the receipt claim digest. Uncertain admission is never blindly retried. Native SQLite reconciliation and actual synthetic frontend route observations pass focused tests. Production collection is not live acceptance: division row deltas, Supabase owner-policy witnesses and complete egress containment remain separate, unverified gates. A real second authenticated Command principal/session is required before admission.

The application migration executor regenerates the exact reviewed package, requires one dedicated nonsuperuser PostgreSQL session, serializes through a fixed transaction advisory lock, and records exact Supabase migration history atomically with the guarded body. Lost COMMIT acknowledgement is outcome unknown and must be reconciled. A PostgreSQL 17.6 network-isolated rehearsal passes six groups including atomic history, duplicate prevention, conflicting locks/history, provider drift and lost acknowledgement. Its explicit protected CLI remains subject to every release prerequisite; no production application occurred.

A measured disk command includes two deployments, two backups, a recovery artifact/database, explicit build/package peaks and a log/temp reserve. It rejects links and sparse-file undercounting. One sealed-artifact-only observation measured 31,793,152 required bytes against 6,959,661,056 free bytes with zero additional bytes required; the 16 MiB log/temp allowance is an explicit reserve, not observed build usage. No VPS frontend build or uncapped package-manager run is authorized by that result. Recheck before execution. A new activation-profile builder derives actual API child/NSS identities, systemd generations and sealed artifact evidence, repeats observations and exclusively writes a protected profile without credentials. It neither installs configuration nor activates authority; the existing activation entrypoint independently revalidates the profile.

Release 3cb830b passed canonical CI 34190903951 and the local complete suite (1,603 pass, zero fail, twelve skip). Parent and independent review approved main anchor eb3f5d1: seven additive Pearson Triad docs/page/config files, no Zola runtime changes. The routing experiment created one unpublished exact-path canary in maintenance run 34190899875 attempt 2, then stopped on an API response-schema mismatch; no promotion occurred. Version 2a1d0b0b-e755-4438-899e-0327dd69621e requires exact-version reconciliation. A separately reviewed discard-only rescue now requires repeated current/history/version identity and byte checks, durable intent, and read-only absence confirmation after a successful or uncertain discard. Run 34192089393 issued no discard because its default routing response was incorrectly assumed to be live; fresh diagnostics show the exact staged version instead. The corrected check uses the sole history entry’s affirmative isStaging flag (documented as unpublished), rejects contradictory live flags, and verifies current and explicit-version bytes twice. Its completion is not claimed before retained remote evidence. Neither empty route metadata nor the baseline anonymous 404/410 responses establish application authorization denial. Exact new-head CI remains required after this correction.

Fresh read-only provider/n8n checks retain the same provider ACL boundary and unchanged published n8n revision cdd141ba-8d20-4981-b598-6af8e35aff86. Both prepared credential references resolve across a complete thirteen-entry inventory. Parent independently verified provider ZIP digest 520d5d45310e86a8475616f5b4c9a75ca24bcb9014a0d0a4841dc063dbf095e3 and all eleven member checksums. No matching provider response is observable. Full rollback containment, live acceptance, configuration installation, n8n publication reconciliation and end-to-end release orchestration remain unfinished. No canonical activation, production migration, live workflow replacement or merge occurred.

## 2026-09-08 — bounded six-read observer and measured disk gate

All six capability paths now share a request-local closed read client. It exposes reviewed projections and query methods only, with fixed Supabase-origin GET/HEAD transport, redirect rejection, request/deadline/byte bounds, parent and embedded-row limits, and sticky forbidden-operation rejection. Capability helpers receive the client explicitly; nested Buyer registry reads reuse it. Registry results above 200 rows fail closed instead of silently truncating matching. Missing Buyer-match opportunity IDs return 400; thrown read failures return sanitized 503 responses.

Successful routes seal a sanitized observation header. HTTP adapters bind validated metadata to the decoded result through a WeakMap without changing public capability schemas. The dispatcher persists observations through existing authority-fenced finalization. Older receivers with no observation remain compatible but cannot satisfy observed production acceptance; null frontend SHA is likewise not exact-release evidence. Observations prove only the supplied client's operations and the reviewed helper call graph, not global egress containment or database owner isolation.

The offline fixture now exercises the actual closed client and actual routes over synthetic REST, including all six observation headers and durable dispatcher evidence. Sixteen targeted groups and 119 capability regressions pass; frontend TypeScript passes. Independent reviews corrected missing Buyer-match finalization, exception handling, query URL bounds and array-SHA coercion. Exact-head CI and frontend build remain required after commit. Production collector orchestration and live runtime pairing are still unfinished. A reviewed GET-only inventory extension now captures retained project-routing version IDs and typed live/staging/count metadata, rejects partial or duplicate schemas, and makes no denial claim. Ten inventory tests pass; protected CI must collect actual history.

Eight safe live POST probes against the exact recovery deployment returned expected 401 responses: no-cookie anonymous and synthetic wrong-Bearer requests to three Buyer intake routes and Nexus trace. Source confirms these return before Supabase client creation. Protected evidence is `/var/lib/blackspire-operator/preparation/recovery-live-anonymous-denial-20260908.json`. This is not the full valid-session workspace/principal/generation matrix. Authenticated SearchJob probes would append beta activity, and anonymous legacy n8n webhook probes could dispatch; those were not performed. Full immutable authority containment remains UNVERIFIED.

One read-only disk check measured 7,252,045,824 free bytes. Two new runtime artifacts plus two backups and a recovery copy fit a conservative rounded 32 MiB envelope, leaving 7,218,491,392 bytes. Release creation streams one artifact then renames it; API and worker share that runtime, and no VPS frontend build is required. Minimum measured deployment/recovery space is sufficient; the preferred 8 GiB reserve is short by 1,337,888,768 bytes. Recheck before deployment because npm temporary growth and concurrent usage are not hard-capped. No deletion occurred. Evidence is `/var/lib/blackspire-operator/preparation/zola-measured-disk-headroom-20260908.json`.

Provider archive/request and current n8n definition digests remain unchanged; six protected values compared against 31 package/archive files had zero matches. Ticket remains UNSENT. Executable post-provider orchestration, complete rollback containment and production collector acceptance remain unfinished. No canonical activation, production migration, n8n replacement or merge occurred.


## 2026-09-08 — isolated recovery admission and replay binding

A rehearsal-only secure intake component pins recovery `2c0b600c268faa0571f08322e16d7f81f37789be` and its sealed artifact digest. It accepts one exact raw path, generated bearer authentication, bounded canonical JSON, fixed six-read capabilities, principal/workspace and API/worker-generation bindings; it derives objectives and replay keys internally and rechecks persisted authority before synchronous admission. It is not mounted by normal launchers. Adversarial tests cover malformed and coerced schema values, anonymous/wrong credentials, legacy and alternate paths, revoked authority and stale admission generations.

The operator-host command `bash scripts/with-node.sh scripts/zola-rollback-rehearsal.js` verifies the sealed artifact before importing its actual authority, unified input, claim and dispatcher implementations into fresh disposable SQLite. All six reads complete with durable receipts using current synthetic division route fixtures. A held old-worker result is rejected after actual claim reclamation, and the replacement refuses redispatch with outcome unknown. Intended authorization audit appends are distinguished from task/admission and division mutation checks. Protected production credentials are excluded by the supervised environment and child allowlist. Evidence is `/tmp/zola-recovery-intake-proof.json`. This does not prove kernel egress containment, real supervisor-generation admission, recovery frontend behavior, live owner policies or historical Vercel URL denial; full rollback and pre-ACL acceptance remain UNVERIFIED.

Current-source task and unified-input replay now bind workspace, principal, channel and authority before disclosing an existing request. Null-default retries preserve SQLite storage semantics. Same-workspace foreign-principal and reserved-key collisions are hidden before writes. Thirty-three focused existing/new tests pass; four intake adversarial groups and nine sanitized routing inventory groups pass. These current-source fixes do not modify the immutable recovery artifact. The routing inventory now reports fixed schema types/counts for unsupported responses without exposing rule values or treating unknown controls as denial. Exact-head CI and fresh routing evidence are required after push.

Read-only n8n verification confirms the active workflow still matches its protected backup and both prepared credentials resolve; nine structural/package/VM groups pass. Provider ZIP and member digests remain unchanged and its ticket is UNSENT. Disk observation was 7,281,000,448 bytes free, 1,308,934,144 bytes short of 8 GiB; active npx installations and protected data were retained. Production collector/observer, immutable URL containment and executable post-provider orchestration remain unfinished. No production migration, activation, n8n replacement or merge occurred.


## 2026-09-07 — exact-head CI and protected metadata sizing

Release `5ff763f509b71222ba2ea16beb91cd54f0624744` passed canonical CI `34166379571`, including the full suite, writer PostgreSQL/TLS/lock checks, eighteen guarded ACL/application groups, build/lint/typecheck/security/audit and immutable release cross-checks. PR125 remains open and unmerged. Its separate read-only protection inventory completed deployment/domain/alias pagination (358/3/102), but firewall configuration returned 404 and system bypass returned 402; it correctly remains INCOMPLETE. Protected artifact SHA256 is `d96b18972c84c53e402b12afd0858ab9c87e38870d4f3228436bc2a81f9261ad`. A reviewed GET-only project-routing extension now discovers the CDN control that runs before immutable deployment routes; it still makes no denial claim and performs no routing mutation.

Actual migration-package CLI validation found the 435 KiB reviewed catalog exceeded the credential reader's size limit. A separate fixed 2 MiB metadata reader preserves all ownership, ACL, link, inode and UTF-8 guards; credential readers retain their existing 16 KiB default and 64 KiB ceiling. Independent reader/configuration regressions pass. Actual CLI preparation passes in a protected synthetic clean checkout, including hostile Git-environment isolation, wrong-head, output-collision and symlink rejection. Evidence is `/var/lib/blackspire-operator/preparation/migration-cli-proof-dd90fca117fe465aa0d3a7d73cb2e2ce/parent-verification.json`; it is an offline proof, not production execution. Rebuild the real release package at the next clean committed head. Source and metadata changes require their own exact-head CI; live gates remain held.


## 2026-09-07 — deterministic n8n preparation and strict read acceptance

The n8n gateway is resolved to the existing HTTPS `jarvis.blackspirehelix.com` front door. Two distinct scoped header credentials were created and reconciled by returned ID and fresh credential inventory; they remain unattached. The live published workflow and protected rollback snapshot are unchanged. Protected preparation is `/var/lib/blackspire-operator/preparation/n8n-current-74ae0e00-192c-4f82-ada6-ed2099bcb151`. Its secret-free package contains the deterministic eleven-node workflow, exact draft/publication payloads, fail-closed deactivation rollback, structural diff, validation sequence and checksums. Candidate SHA256 is `10e27cc413cf2c9a4049282d6b3bd93f3cb74e1c9efa8483a72f862434ad613f`. Ten source/configuration tests and three actual-candidate VM groups pass. Definition readiness and offline validation are complete; actual Cloud execution and publication remain gated. Re-pin the package to the final release SHA after commit. Publication must follow canonical writer readiness because the gateway currently returns 502 while canonical services are inactive.

Direct Supabase PostgreSQL TLS now verifies over IPv6 with the authentic production CA and hostname checking. CA SHA256 is `700723581420dd1ac98fd7e9ac529f0ef210eadcaf87fc868a3ad7d114c2f3b7`. The protected `writer-configuration-tls-verified.json` supersedes the unverified draft; no database authentication, role provisioning or SQL occurred during this connection proof. No certificate verification was disabled.

Deal Records now rejects unavailable configuration and SQL errors in its internal read mode. Deal Analysis uses a dedicated bounded persisted-data helper and existing underwriting calculations, avoiding the UI detail graph's scaffold and tolerated-error paths. The one-command offline six-read rehearsal uses actual capability adapters, authenticated routes, observed synthetic database dependencies and fresh SQLite authority/dispatcher state. It checks persisted-before-dispatch receipts, duplicate prevention, missing-permission/foreign-workspace denial and uncertain-response non-replay. Its child has a scrubbed environment, independent deadline/output limits and parent-owned disposable-state cleanup. Independent review approves this offline scope; the report explicitly says `productionReady:false`. It is not the complete production API/worker collector or live mutation observer.

Frontend production compilation and TypeScript pass in a scrubbed disposable build with a bounded Node heap and clean namespace teardown. The first full local suite passed 1,552 tests with nine skips and found one offline-fixture migration-boundary violation; the fixture now uses the existing approved disposable-database helper, and all 41 migration-boundary/read regressions pass. Exact-head CI must verify the integrated result after push.

A new guarded application-migration package embeds a read-only provider-poststate assertion, exact pinned Buyer/Nexus SQL, five-second locks, thirty-second statements and a 120-second transaction deadline. Exact temporary row multisets cover six application tables and three private ledgers; browser denial, own-read policy and writer authority checks precede commit. The CLI binds the declared release to clean source HEAD. Eighteen disposable PostgreSQL groups pass, including forced atomic abort, reapplication with nonempty ledgers, own/cross-owner checks, all-five-table scoped writes and duplicate denial with receipt reconciliation. Existing provider apply/rollback SQL bytes remain unchanged. Production execution and Supabase migration-history recording are explicitly separate; no migration was applied.

Read-only Vercel protection inventory is now prepared in the existing protected CI workflow, using its stored token without exporting it. It enumerates retained deployments, domains, aliases, firewall and bypass metadata with bounded pagination, strict project checks and sanitized output. Inventory never claims direct-URL denial. Secure immutable rollback intake and complete live read/release orchestration remain unfinished beyond ACL.

Independent disk audit and parent verification removed only inactive ignored dependency/build output from `/tmp/zola-fix-nexus-endpoint/frontend`; package locks matched, no process references or recent writes existed, and repository/Git status were preserved. Cleanup recovered 1,109,729,280 bytes. Post-cleanup free space was 7,300,689,920 bytes, requiring another 1,289,244,672 usable bytes to reach 8 GiB. Evidence is `/tmp/zola-current-safe-build-cache-cleanup.json`. Credentials, repositories, active worktrees, rollback/composition artifacts, sessions, authority and queues were preserved.

The provider support ZIP retains its original digest and all eleven member checksums. It remains unsent. Read-only witness discovery found four persisted deals with Seller/Analysis data, zero Nexus contacts, and one real authenticated job/report owner; a second auth user exists without owned jobs. Canonical authority has one active operator with all six permissions. The IDs-only protected witness manifest records these limits; no production fixture data was invented. Production activation, workflow replacement, database migrations, merge and cutover remain held.


## 2026-09-07 — actual scoped activation and runtime ownership verified

Actual scoped activation now passes at `8645ab219759cb637575150e48c2a3d0edbd3ce5`, with sealed artifact digest `c5f007d8a52b532ebe76166e7273f524cff5bc4212254b8344601ea2bc5adefb`. The reviewed root fixture uses generated authentication, fresh SQLite with a dormant task and session, pinned PostgreSQL 17.6, verified TLS/SCRAM identities and a network namespace with no external route. It validates the artifact and both role profiles before startup. Real API/worker supervisors and the actual root activation entrypoint pass unbound denial, three committed activations, five-table writes, completed-receipt reconciliation after a post-response fault injection, stopped-worker denial, and rejection/rebinding after both worker and API generation changes. All non-heartbeat SQLite rows remain unchanged and scoped PostgreSQL sessions drain on shutdown. Independent review verifies the protected result, three commitment receipts, artifact, retained witnesses and cleanup. This is not physical network-loss, stale task-result, six-read, full rollback or live-production acceptance.

The rehearsal exposed a host prerequisite: the pinned Node executable and its runtime/bin directories were owned by UID 1000, which the existing executable guard correctly rejects. The installed binary matches the official Node 22.23.1 archive member. An independently reviewed correction changed only those three paths to root ownership, preserving modes, inodes and bytes; the protected executable was rehashed afterward. No service restart or validator relaxation occurred. Three earlier fixture failures are retained: noncanonical supervisor argv, the ownership prerequisite, and a synthetic deployment-record timestamp lacking canonical milliseconds. Their owned resources were stopped; the final run passes. The first outer absent-container cleanup check was corrected to verify successful exact-name inventory instead of Docker error wording.

Parent-verified cleanup removed only unused regenerable npx/download caches after process-reference, recent-write and symlink checks. Active tool installations and all repositories, worktrees, credentials, backups, artifacts, evidence, authority, queues and sessions were preserved. Two cleanups recovered 3,715,964,928 usable bytes. The second observation showed 6,173,106,176 bytes free (about 5.75 GiB), requiring another 2,416,828,416 usable bytes to reach 8 GiB; recheck concurrent usage before deployment. Installed uv tools are not disposable cache.

Evidence is `/var/lib/blackspire-zola-rehearsal/activation/5ef1c2e2-04b7-411f-a5a5-f4be0624260a/operator-result.json` (SHA256 `4a77f10918dfe507d63fe8e7b1128095b1243ee2ffc8c3e4aab1340e28aacc94`) and its `parent-verification.json`. Executed fixture source is retained under `/var/lib/blackspire-operator/preparation/scoped-activation-de4d667f346d47c080cf658086b66f74`; it is an operator proof, not a portable production release command. Official-byte/ownership evidence is under `/tmp/zola-node-verify-ze4ir2mj`; cleanup evidence is `/tmp/zola-safe-cache-cleanup-858f7ca0f0ac4eb7865694475ca3d66d.json` and `/tmp/zola-safe-cache-cleanup-f5780d4448384219a7a3f4dc1a0460b7.json`.

Fresh read-only audits confirm PR125 open/unmerged/mergeable and all five checks successful at exact `8645ab2`, including every mandatory CI step. Live n8n still matches its protected backup and published version; its actual candidate remains requirements-pending, without gateway/credential references or a workflow payload. Provider package hashes match and the existing request remains unsent; ownership privileges were not retried. Canonical services remain inactive. Secure immutable rollback intake/direct-URL denial, the contained six-read harness and real witnesses, n8n Cloud validation, exact production migration execution and guarded release orchestration remain unfinished engineering beyond ACL. Production migrations, workflow replacement, activation, merge and cutover remain held.

## 2026-09-07 — combined migration and actual runtime rehearsal

The canonical release branch no longer skips frontend preview builds for backend-only commits, so acceptance can pair exact frontend/VPS SHAs. Four deployment-boundary tests pass; other branches keep frontend-change filtering and the root VPS project remains ignored. Actual new-preview readiness must be verified after push.

The combined PostgreSQL 17.6 suite passes 30 groups using both exact reviewed migration hashes in production order. It verifies six-table row preservation, atomic rollback after a forced error, browser table/column/MAINTAIN denial, both-direction SearchJob ownership isolation, unchanged private ledger and writer grants after migration reapplication, and subsequent dedicated-writer success across all five Buyer tables. Independent review approves this isolated proof; production migrations remain unapplied.

Actual writer-disabled production supervisors now pass bounded isolated lifecycle rehearsal at release `6bc968d3f6f041640f6f6d3f703f77560a30f3d3` and recovery `2c0b600c268faa0571f08322e16d7f81f37789be`: API boot, missing-worker denial, worker readiness, stopped-worker denial, restart with a new generation, and preservation of fresh authority/queue/session state. Private loopback namespaces have no external route; generated authentication is synthetic. All owned units stopped and canonical services stayed inactive. Recovery artifact digest remains `0028052a7d08b1e7e73b8ce8cd441f90d10f16b288e10d10416891b5598f58bd`. This is not scoped-writer activation, stale task-result acceptance, six-read acceptance or complete functional rollback.

A deterministic offline n8n package command now binds candidate bytes, source digests, expected revision and backup digest. Six package/VM tests pass. Actual protected CLI checks reject output collisions and symlinks; missing gateway/credential references produce only a requirements manifest, never a placeholder workflow. Live n8n is unchanged. Provider execution, scoped activation, secure rollback intake, actual credential resolution and the contained six-read harness remain unfinished. No production workflow change, migration, activation, merge or cutover occurred.

## 2026-09-07 — fresh provider authority and safe disk recovery

Release `db9b82594e0aa67e6cfec42258078a7dec327594` is committed/pushed with all five checks green in CI `34123517394`. A fresh read-only PostgreSQL capture exactly matches the reviewed ACL baseline. Session/current role is `postgres`, not superuser, with neither SET access nor inherited authority to `supabase_admin`; all 31 affected net privilege grant-option checks are false. The protected package hashes remain exact. Provider execution is therefore required for this transaction; no escalation, extension call or production DDL occurred. The independently reviewed unsent request now specifies exact submission files, hashes, identity/window approval and post-execution evidence.

Twenty focused writer lifecycle/runtime/issuer/n8n tests pass. Fresh n8n authentication passes; the published workflow/version and nodes/connections match its protected pre-change backup, retaining seven anon nodes and nine unsafe writes. The exact rollback artifact digest still matches saved boot evidence, whose six-read result is explicitly NOT RUN. Secure intake and complete functional rollback remain unfinished.

Independent read-only disk inventory and parent rechecks authorized removal of only apt binary caches/downloads, Node V8 compile cache and downloaded apt indexes. The first cleanup recovered 346,402,816 bytes; apt indexes recovered another 198,045,696 bytes. Free space increased from 2,036,023,296 bytes before cleanup to 2,581,192,704 bytes (about 2.404 GiB), above the 2 GiB reserve plus 256 MiB minimum budget. Another 6,008,741,888 usable bytes would reach 8 GiB at that observation. Recent pip-cache activity caused its exclusion. Installed tools, repositories/worktrees, credentials, backups, authority, audit, rollback/releases, queues and browser sessions were preserved. Recheck ongoing usage immediately before deployment.

Direct Vercel API inspection found the db9b825 frontend deployment CANCELED despite its green GitHub status. It is not a ready exact-SHA acceptance pairing. The local Vercel CLI lacks saved authentication; its automatically attempted login was immediately canceled, with no login repeated. No deployment occurred. The concrete post-ACL sequence records actual credential/witness/containment/pairing prerequisites and does not mark the harness ready. Canonical API/worker remain inactive, production Buyer/Nexus migrations unapplied, six real reads unrun, PR125 unmerged and production unchanged.


## 2026-09-07 — provider ACL transaction isolated rehearsal

Current release and PR125 head is `115f07e000986ba63f8afc731365f2bad5d0301f`; its five checks passed in CI run `34102179630`. PR125 remains open and unmerged. The new offline provider ACL generator freezes catalog/role/version preconditions, preserves consumer grants and grant options, removes only captured PUBLIC edges and provides guarded exact rollback. Three focused generator tests and nine actual PostgreSQL 17.6 ACL checks pass. Independent review prompted collision-safe SQL literals and strict negative-test assertions; actual SQL execution caught and corrected syntax/precedence errors before the successful run.

The isolated fixture used inert extension stand-ins and no production credentials or network access. It proves ACL preservation, reapplication, drift rejection, writer denial and atomic rollback after a forced failure. Parent verification confirmed owned container/cgroup absence. The protected production review package and unsent provider request are recorded in `docs/ZOLA_PROVIDER_ACL_REQUEST.md`; no production ACL transaction was applied. Provider-authorized execution remains required because current database authority cannot revoke provider-owned grants.

The separate both-role rehearsal environment validator has two passing focused groups, but complete actual supervised activation remains unverified. Canonical API/worker remain inactive; live n8n still uses seven anon nodes and nine unsafe writes. Production migrations remain unapplied, rollback `2c0b600` remains functionally unverified, and all six real reads remain unrun. Disk is approximately 2 GiB free and remains below the release budget. No live integration, activation, merge or cutover occurred.


## 2026-09-07 — activation verification and isolated rehearsal preparation

API integration is committed and pushed at `4f188c6bc9bf5944b2ae6c34810e517482d36a50`; all five checks passed in CI run `34091871599`. PR125 remains open, unmerged and mergeable. The following activation work is local. Full regression passes 1,530 tests with zero failures and nine skips; all 132 files complete with bounded output and an empty owned cgroup. Build, lint, typecheck, secret scan, living-memory and whitespace checks pass. Independent reviews approve the implementation; final reviewed commit/push and exact-head CI remain required.

The root activation command now validates its bounded systemd container before loading protected configuration. It combines immutable artifact verification, supervised process/executable identity, worker attestation and loopback prerequisites, then rechecks process identities and service invocations. The issuer credential stays in a private readiness closure. Protected configuration snapshots are checked again synchronously immediately before commitment.

Review found that abrupt termination could bypass ordinary publication cleanup. The replacement protocol keeps a binding provisional: normal writes and readiness require a separate protected commit marker matching its inode, digest, workspace, release and generations. Issuer-authenticated preparation is read-only and cannot authorize writes. A two-link marker is rejected; removing its pending link is the commit point. Uncertain publication or errors after commitment preserve the actual state and report an unknown outcome for reconciliation. Pending links are protected deployment state and must never be removed as disposable temporary files. Independent reviews approve the protocol and wiring; 44 focused groups pass, including actual loopback HTTP and kernel listener ownership through the root entrypoint. An independently reviewed root filesystem fixture also passes actual SIGKILL before linking, after linking and after the commit-point unlink: the first two states remain rejected and the last remains committed for reconciliation. Its synthetic marker is explicitly revoked before pending-link cleanup; the owned cgroup and successful fixture are absent. An initial synthetic proof included an extra field and failed closed; that failed fixture and evidence are retained.

A separate bounded, credential-free root fixture passed actual artifact ownership/digest verification, tamper denial, writable-file denial, symlink denial and restoration of the same digest. Its owned unit/cgroup and fixture were removed. The initial fixture failed from generated JavaScript escaping and was corrected with a syntax check before the successful run; its evidence is retained. These proofs do not constitute complete supervised API/worker activation or atomic cross-database fencing.

The unchanged production supervisor requires a production identity. A protected rehearsal descriptor permits an exact UUID-bound noncanonical unit pair only with matching release, isolated state/configuration paths, loopback ports and disposable PostgreSQL target. The API checks its resolved startup context before pools open. Actual filesystem isolation and both roles' pre-start environment verification remain required in the unfinished root launcher. Do not start the full rehearsal merely because descriptor validation and command tests pass.

An independently audited regenerable npm cache was removed after parent verification and atomic quarantine. Free disk increased from 1,687,658,496 to 2,138,517,504 bytes, still below the release reserve and artifact budget; a subsequent read remains approximately 2.0 GiB free. Repositories, installed packages, credentials, backups, authority, release artifacts and audit evidence were preserved. Canonical API and worker remain inactive/disabled. The provider-authorized ACL transaction, actual n8n replacement, secure rollback intake and six real reads remain unfinished. Production migrations remain unapplied; no workflow mutation, production activation, merge or cutover occurred.

## 2026-09-07 — scoped writer API lifecycle and service identity

The canonical API now has an explicit scoped-writer startup path with protected configuration, separate verified PostgreSQL pools, authenticated fixed routes and fresh availability checks. Configuration rejects role overrides, connection strings, mixed database targets and reused credentials. Identity validation precedes secret loading; development/test startup cannot discover production writer configuration. Missing activation approval allows API boot but denies writer operations. Public readiness rechecks emergency stop after awaiting availability. Startup/shutdown handling prevents a late listener after termination and closes initialized pools before authority storage; disconnected admitted queries receive a bounded drain, with forced closure treated as an uncertain transaction outcome.

Protected configuration checks include safe root-owned ancestors, private API-group permissions, inode stability and rejection of extended POSIX ACLs on the same open descriptor. A real synthetic test under the API and worker accounts proves private-file separation and rejects a named-worker ACL even when ordinary OS reading succeeds. Canonical services use supervisors, so the observer verifies the API child and supervisor relationship, cgroup, IDs, groups and zero capabilities while preserving ProtectProc isolation. A separate real systemd fixture passes two stable observations under the actual service accounts; its two owned units and cgroups were removed. Worker restrictions remain root-attested and matched to live service metadata, not a claim of live worker process inspection by the API.

Independent reviews approve the implementation. Focused regressions include the actual SQLite stop race, disconnected HTTP query draining, configuration denial, protected files, service identity and asynchronous startup. Full root regression passes 1,478 tests with zero failures and nine skips; all 119 files complete with clean process containment, bounded output and an empty owned cgroup. Build, lint, typecheck, secret scan and living-memory checks pass; dependency audit reports zero vulnerabilities. Independent final diff review and exact-head CI remain required for this local increment.

Runtime commit `000516825b02a3ab846fb3f98c0312f076083277` has all five checks successful in CI run `34086110942`; PR125 remains the sole open release PR. This API increment is local. The root activation publisher, provider-authorized extension ACL transaction, actual n8n integration, immutable rollback intake continuity and six real reads remain unfinished. No production credentials were provisioned, workflow changed, SQL applied, service activated, migration run, PR merged or cutover performed. Both canonical services remain inactive/disabled, and disk remains approximately 1.6 GiB free.

## 2026-09-07 — verified PostgreSQL pools and dependency packaging

Dedicated issuer/runtime pools now use pinned `pg` 8.23.0, separate explicit credentials, verified TLS, fixed SQL allowlists and bounded admission/query/shutdown deadlines. Every checkout validates actual role identity, function ownership/security settings and effective privileges, including inherited PUBLIC extension access. Unexpected privilege drift fails closed; uncertain connections are destroyed without retry. No API mount or production credential provisioning is included yet.

Fourteen focused groups pass. PostgreSQL 17.6 passes 28 catalog/ledger groups. A separate owned internal-network fixture proves actual driver TLS, both scoped identities, HTTP writes across all five tables, reconciliation after an injected completion-response loss, privilege-drift denial and a real five-second lock timeout followed by connection destruction and recovery. This is isolated synthetic acceptance, not production continuity or a physical network-loss test.

Deterministic credential-free dependency installation now precedes hashing in fresh releases, CI archives and test snapshots. Completed releases remain untouched. A bounded synthetic-commit release-create proof passes tree/evidence verification, unprivileged driver import, idempotent creation and sealed-install rejection. Installed dependencies are 139 files / 469,296 bytes and remain covered by artifact hashing; audit reports zero vulnerabilities. Full root regression for this increment passes 1,436 tests with zero failures and nine skips; all 109 files complete with clean process containment and an empty owned cgroup. Build/lint/typecheck/secret checks pass.

Frontend commit `cc2702e61203e11e62f325586dea2983f10f63eb` has all five checks successful in CI run `34083220088`; PR125 remains open and mergeable. This subsequent runtime increment is local and requires independent final review, commit/push and its own exact-head CI. Read-only production metadata confirms provider-owned `pg_net` PUBLIC grants conflict with least privilege; the current database role lacks authority to revoke those grants. A provider-authorized ACL change and consumer-preservation rehearsal remain necessary. No production SQL, n8n change, activation, migration, real read, merge or cutover occurred. Disk is approximately 1.6 GiB free; deployment headroom remains insufficient.

## 2026-09-07 — authenticated frontend writer dispatch

The three Buyer entry routes now capture the successful guard principal and private authentication closure before asynchronous work. Opt-in scoped mode binds creation and lookup to that owner, revalidates entitlement, acquires only the complete approved source inventory, and sends issuance and authenticated n8n delivery within one bounded deadline. A fresh attempt UUID and original database revision survive timeout reconciliation. Only a canonical completed receipt reports success; uncertain results retain sanitized recovery coordinates and bypass legacy unfenced Deal failure writes. The unauthenticated Harvester caller receives an explicit scoped Buyer launch denial before Buyer/Deal reads; no authenticated Harvester continuity is claimed.

Independent review approves the implementation and 23 focused groups pass. Actual Next.js compilation, TypeScript, all 69 static pages and three Buyer route traces passed in a credential-free isolated build. Each route contains all 75 approved source entries. The build-specific PID namespace records the compiler exit separately and terminates remaining build descendants; final cgroup cleanup passed. Expanded tracing now explicitly excludes hidden environment, Git and Vercel configuration; the final build excludes six harmless protected-path witnesses across all 191 trace manifests. Targeted frontend lint and root build/lint/typecheck/memory/secret checks pass. The full root regression suite passes 1,422 tests with zero failures and nine skips; all 107 files complete with clean process containment, bounded output and an empty owned cgroup.

Committed issuer head `e22f1a0be8ed4008aba625999412146a0dcfdc17` has all five exact-head checks successful in run `34080115031`. These frontend changes are local and need their own reviewed commit/push/CI. Dedicated PostgreSQL pools and dependency packaging, live n8n integration, immutable rollback intake continuity and six real reads remain incomplete. Production migrations remain unapplied, canonical services inactive, and merge/cutover held. Disk remains approximately 1.6 GiB free; no additional deletion or production mutation occurred.

## 2026-09-07 00:22 UTC — transactional writer isolated proof

Issuer and reconciliation continuation: the trusted acquisition captures immutable criteria and the complete PostgreSQL revision before fetching. Issuance compares both under the job lock before mutation, with monotonic microsecond timestamps including cancellation before the first dispatch. The caller supplies a fresh attempt UUID before sending issuance; it becomes the dispatch ID. Separate authenticated issuer/reconciliation HTTP routes use fixed parameterized SQL. Reconciliation cancels only that attempt, preserves terminal receipts and newer generations, and fences a missing attempt against its original revision. It never returns a permit or retries issuance. An absent attempt has no database workspace association; the configured gateway and authenticated route guard supply that authority. Its UUID is not permanently tombstoned, so new acquisition requires a fresh UUID and must never refresh a failed attempt's revision.

PostgreSQL 17.6 passes 27 isolated groups, including both issuance/reconciliation lock orders, duplicate-attempt rollback, terminal preservation, stale/NULL/future revisions and real loopback HTTP through separate issuer/runtime logins writing 201 rows across all five tables. Fourteen focused issuer/criteria/HTTP groups pass independently. Build, lint, syntax/typecheck and secret scan pass. The full root suite passes 1,402 tests with zero failures and nine skips; all 104 files complete with clean process containment. A private 512 MiB temporary filesystem and 1 GiB memory limit bound this run; peak memory is 473.4 MiB, the 319 KB log remains bounded, and the owned cgroup is empty afterward. Final whitespace, secret and living-memory checks pass. The previous committed head `4dd1dd8682d04b2c10f85cf5974009d6cc7c5709` has all five exact-head CI checks successful in run `34077665341`; this subsequent delta is local.

Disk free fell to roughly 1.3–1.6 GiB during an unrelated worktree's frontend build. Independent read-only metadata accounts for approximately 1.41 GiB of recent dependency/build/npm/npx growth; that work is preserved and no additional deletion occurred. Heavy builds and production activation remain held. Authenticated frontend capture/mounting, actual dedicated pools, secure live n8n integration, functional rollback and six real reads remain unfinished. No production SQL/workflow change, county/provider request, activation, merge or cutover occurred.

Remaining-source continuation: the trusted acquisition path now includes the legacy Guilford/Wake/Stanly/Stokes/NC OneMap/Lincoln/generic branches, preserving ordered concatenation, response mappings and configured query semantics while rejecting failed or malformed sources. The static 75-source registry manifest matches the sanitized read-only metadata exactly. Explicit Stanly and virtual Mecklenburg policies bind their existing endpoints; no active database row is invented, registry lookup errors cannot become fallback authorization, and missing Wake configuration stays closed. Twenty-four focused groups pass independently, including query tampering, partial failure, cumulative rows, full manifest timestamp formats and fallback denial. The full root suite passes 1,394 tests with zero failures, nine skips and all 102 files completed with clean process containment. Build/lint/typecheck/security/memory/whitespace checks pass; this reviewed local delta requires new-head CI after commit/push.

Source-acquisition commit `31c14e3f70c41e3045ecdec2842448e2d72b91df` is pushed and all five exact-head checks pass in run `34076595715`. The new source delta remains local. Authenticated issuance must additionally compare the acquisition's captured criteria under the job lock to prevent a fetch/issue race; lifecycle fencing and actual frontend/runtime mounting remain unfinished. No source/provider request, production SQL installation, workflow change, activation, merge or cutover occurred.

Source-policy/acquisition continuation: the resolver binds exact approved active source inventory, URL hashes, type/cash policy and PostgreSQL microsecond ordering. The acquisition composition connects the 19 extracted frontend adapters to fixed bounded transports, rejects malformed responses and non-finite numbers, and caps exact serialized payload bytes before permit issuance. Fourteen isolated groups pass independently, including all 19 empty-result paths, Forsyth shared primary/secondary budgets, HTTP failure propagation and late-payload denial. No production manifest, generic source adapter, issuer/runtime mount or live workflow change is claimed. The full root suite passes 1,384 tests with zero failures, nine skips and all 101 files completed with clean process containment. Build/lint/typecheck/security/memory/whitespace checks pass. Independent code and final scope reviews approve this increment; it requires its own CI after commit/push.

County-adapter commit `69a173035e996fde11b835187320eb2cf563b67e` is pushed; all five exact-head checks pass in run `34075483996`. PR125 remains open/unmerged/mergeable. Current free disk is approximately 3.24 GiB; no further deletion occurred. Production migrations, canonical activation, functional rollback and six real reads remain held.

County-adapter extraction continuation: the 19 existing frontend adapters now live in a pure injected factory, with no implicit authentication, environment or network access. The current trigger composes the existing legacy transports until the coordinated switch; scoped callers can supply a fixed source snapshot and the bounded client. Independent review confirms 60 functions remain text-identical, preserving query, pagination and mapping behavior. Forsyth delegates only its transport; Mecklenburg's legacy lookup catch remains in legacy composition, while scoped errors propagate. Five isolated adapter groups pass, actual frontend TypeScript and targeted ESLint pass, and the full repository suite passes 1,370 tests with zero failures, nine skips and all 99 files contained. No frontend deployment, county request or production mutation occurred.

Bounded source-client commit `3c52dc55c3f9d7c5c2090f59cfc5844030e41f4d` is pushed and exact-head CI run `34074643681` passes all five checks. The local extraction requires its own CI after commit/push. Approved source-policy resolution and actual authenticated issuer/runtime/n8n integration remain incomplete; all production release gates stay held.

Bounded source-client continuation: `source-http.js` implements trusted endpoint-ID/configuration-digest dispatch, pinned public IPv4 DNS with TLS hostname verification, explicit proxy isolation, monotonic whole-request/aggregate deadlines, request/response-byte limits and strict HTTP/encoding/JSON/provider-error handling. Form request user-agent behavior is preserved. Six mocked-transport test groups pass independently; the monotonic deadline and response-budget clarification requested in review were applied and retested. The component remains unmounted; real TLS/SSRF exchange, endpoint policy and 19-adapter integration remain unfinished. No county or paid-provider requests occurred.

Offline n8n candidate commit `37c09858ce21f10ce0f7dd7fb98efc7081d4b87e` is pushed and exact-head CI run `34073800121` passes all five checks. Vercel builds remain canceled by Ignored Build Step; no new frontend deployment is inferred. A read-only sanitized registry query found 75 active NC sources, all with HTTPS hostnames; no active Wake or Mecklenburg source. Mecklenburg's existing approved fallback must remain explicit, and missing Wake configuration must not be silently activated. Registry source types can differ from adapter-stamped normalization types; bind the latter deliberately. Raw source URLs, query values and notes were not output. No production configuration or data changed.

Offline n8n candidate continuation: `packages/buyer-writer/n8n-workflow.js` builds a fresh secret-free definition with separate secure credential references, authenticated webhook, exact-byte source verification, bundled pure Cloud Code helpers, fixed gateway HTTP operations and a sequential matching-receipt loop. Known pre-write validation failures send one scoped failure operation and cannot report completion. Three VM test groups pass independently; all 31 focused writer tests pass. Node configuration and failure-path reviews approve the offline candidate. This is not actual n8n execution or live integration. Source acquisition/issuer must enforce the same 6 MiB raw limit; 90-second workload timing and unknown transport-outcome reconciliation remain unfinished. No workflow was changed or invoked.

Source-context commit `b8666a0c748afebdd000975994aab9367045cec1` is pushed and exact-head CI run `34073384273` is green. Parent reverified PR125 OPEN/MERGEABLE/CLEAN at that head. The offline builder is a subsequent local delta requiring its own CI. All production release gates remain held.

Source-context continuation: PostgreSQL now binds immutable source policy references and budgets, derives cash-scoring policy, removes the old boolean issuer signature and rejects context-free historical permits while preserving ledger history. Scoped context retrieval requires processing state and rechecks owner, criteria, generation and expiry after locking the job. Exact-byte raw payload digest/count verification and authenticated HTTP context access are implemented. PostgreSQL 17.6 passes 20 isolated groups, including cumulative lower row limits, empty payload completion, legacy signature removal, and context authorization changes under lock contention. Focused JavaScript tests pass 28/28 independently. Full repository tests pass: 1,356 passed, zero failed, nine skipped; all 96 files completed with no remaining descendants. Independent SQL, integration and harness reviews approve this delta; build/lint/typecheck/security/memory/whitespace checks pass. This reviewed local delta requires its own CI after commit/push. Source resolver, authenticated route integration and live n8n replacement remain incomplete.

Baseline `75df6d7e919b673fe1afbe0553ef1bb00c802368` is pushed and exact-head CI run `34071549420` is green: 1,353 passed, zero failed, nine skipped, PostgreSQL 15 groups and zero audit vulnerabilities. This supersedes the earlier pending-CI statement for the normalization/restore-clock increment. Vercel build checks were canceled by the configured Ignored Build Step; no new frontend deployment is proven. PR125 remains open/unmerged. Disk is approximately 3.2 GiB free; no further deletion occurred. Canonical services, production migrations, n8n integration, functional rollback and six real reads remain held/incomplete.

Normalization/planning continuation: pure county conversion, canonical source-date handling and bounded ordered write plans are implemented, without mounting or changing n8n. Planner checks exact clean-sale completeness, duplicate/provenance rejection, detached chunks and PostgreSQL numeric/UTF-8 byte overhead. Source timestamps retain PostgreSQL calendar-date semantics; malformed/non-finite numeric values no longer silently become null/zero (legacy numeric-prefix parsing is otherwise preserved). Independent SQL and integration reviews APPROVE. Final PostgreSQL suite passes 15 groups, including a 201-row multi-chunk actual HTTP write, all five tables, empty completion and real JSONB/date parity. Full root suite passes 1,352 tests with zero failures, nine skips and all 95 files contained; final additional source-price regression passes the focused seven-test suite. No real source request, paid provider call or production mutation occurred.

HTTP commit `a8e3dda8b72f6f5ebeefae3d14809310399135b2` was pushed; its CI run `34070576713` failed one existing partial-restore classification test. A matching failure was deterministically reproduced: fractional filesystem mtime versus integer Date.now could misclassify a fresh backup as future-dated. The independently reviewed correction aligns millisecond precision while retaining future/stale/checksum rejection. All 39 focused recovery tests pass, including the new regression. New-head CI is required after these reviewed local changes are committed/pushed; do not report current remote CI as green.

Read-only disk sizing shows the root source-only release and authority backups need roughly 32 MiB before logs, rather than a frontend build on the VPS. A proposed 256 MiB bounded operation allowance with at least 2 GiB untouched reserve can fit current approximately 3.5–3.76 GiB free, but needs fresh final-SHA sizing and checks before every phase; additional writer dependencies and concurrent growth remain outside that estimate. No further deletion occurred. Secure source-context issuance, n8n integration, external rollback intake routing and real acceptance remain incomplete.

HTTP continuation: `packages/buyer-writer/http.js` now provides an explicitly created, unbound server with exact routes, authentication before body/availability work, bounded connections/bodies, two availability observations and retained admission slots for disconnected pending queries. It remains unmounted in production and does not claim atomic cross-database emergency-stop fencing. Independent HTTP and harness reviews APPROVE. Eighteen protocol/gateway/HTTP tests PASS; PostgreSQL acceptance now passes 14 groups, including actual loopback HTTP through a dedicated runtime login committing all five tables. Updated full root suite: 1,345 PASS, zero failures, nine skips; all 94 files completed with clean containment. Final listen-error cleanup correction also passes focused HTTP tests. Transactional core commit `bf769c0aa2b1face207b41fd494fa97a1f0a6c3e` is pushed and all five PR checks pass (run `34069980393`); this HTTP delta requires its own exact-head CI. No production changes occurred.

The private PostgreSQL writer ledger and fixed routines are implemented in `packages/buyer-writer/sql/install.sql`, with unmounted parameterized write/receipt adapters in `packages/buyer-writer/gateway.js`. Thirteen adversarial PostgreSQL 17.6 groups PASS, including all five Buyer tables, browser denial, owner/generation isolation, malformed/expired permits, replay rejection, atomic failure receipts, installer reapplication, and cancellation/expiry under lock contention. The harness applies the exact reviewed Buyer restriction migration to synthetic canonical-shaped tables and installs as a non-superuser CREATEROLE manager. It is not a full production restore or real six-read proof. Protocol/gateway tests PASS 12/12. Independent SQL review reproduced the managed-role test suite; final container cleanup and gateway reviews also APPROVE this isolated increment. Full root suite: 1,339 PASS, zero failures, nine skips; all 93 files completed with no remaining descendants. Build, lint, typecheck, secret scan, living-memory and whitespace checks PASS.

No production writer installation, n8n change, Buyer search or provider call occurred. The live seven-node/nine-write anon workflow remains unchanged. Authenticated issuer/source-policy integration, secure n8n transformation, HTTP mounting and fixed-SHA rollback intake continuity remain engineering work. Production Buyer/Nexus migrations remain UNAPPLIED. Canonical API/worker remain inactive, functional rollback remains NO, and all six actual reads remain NOT RUN. Supabase quota/recovery and prior provider/receiver evidence are preserved.

Baseline local/remote/PR head is `044a85c7e2c1836b9ac94a3ecd10f53747d6f4fa`; implementation changes are local and require final review, commit and new-head CI. Main remains `53adf74e05c607c0d296923bae05d7ac023ecb57`. Disk free is approximately 3.77 GiB; no cleanup occurred in this increment, and no safe path to 8 GiB was demonstrated. Release remains held. Autonomous work is incomplete; continue from `/tmp/zola-astra-activation-current.md`.

## 2026-09-06 23:17 UTC — writer protocol increment and disk recovery

Recovered exact local/remote/PR head `9d2512ef91b1a74e64decf6f3c41fa0b193bc109`, main `53adf74e05c607c0d296923bae05d7ac023ecb57`, clean initial checkout. PR125 is the sole canonical release PR, OPEN/MERGEABLE, with all five exact-head checks successful (run `34065784801`, audit zero vulnerabilities). Subsequent local changes require their own CI before merge.

Fresh authenticated n8n read verifies actual workflow ID `VvMHSIbycYCx4CZN`, unchanged active version `cdd141ba-8d20-4981-b598-6af8e35aff86`, zero visible execution records and exact protected pre-change backup parity/digest. The supplied alternate ID was incorrect. Seven nodes still contain legacy anon authority; all nine writes suppress HTTP failures. No workflow mutation or execution occurred. Owner authorization now covers scoped writer implementation and safe cache cleanup; earlier pending routine approvals are superseded, while release gates remain binding.

Implemented an **unmounted protocol foundation** in `packages/buyer-writer/protocol.js`: separate workload/permit inputs, bounded fixed operations, exact field validation, canonical receipt digests and generic rejection errors. Independent review and seven isolated test groups pass under credential-free Node 22.23.1; PostgreSQL year-zero incompatibility was corrected and regression-covered. This is **not** a complete writer, HTTP endpoint, database authorization, five-table write proof or deployed workflow. Transactional permit/owner/generation/receipt validation, scoped SQL roles/routines, provenance/scoring parity, authenticated frontend dispatch, secure workflow transformation and isolated writer acceptance remain outstanding. Both reviewed migration hashes match; production migrations remain unapplied.

Deleted only `/root/.npm/_cacache` after independent read-only audit and immediate parent checks for process references, symlinks and repository markers. Free bytes rose from 2,284,109,824 to 5,538,758,656 (2.13 to 5.16 GiB); recovered 3.03 GiB. Preserve installed tooling, hardlinked caches, worktrees and all protected state. Preferred 8–10 GiB was not safely achieved; deployment/backup headroom still needs a measured budget.

Independent rollback review reverified exact `2c0b600` artifact/profile/authority digests. Its six-read `POST /api/tasks` dispatch is already compatible; authenticated Buyer intake continuity is a separate unresolved contract. Functional rollback remains NO and all six reads NOT RUN. Primary Command login access exists in protected storage; do not request it again. Cross-owner witnesses were not found in scoped discovery. SQL and runtime-log tools exist, so observer completion is still engineering work, not a proven blanket owner-only blocker. Existing supervisor requires cleanup, lock, generation and descendant-containment corrections before execution. Canonical API/worker remain inactive; no activation, production migration, push, merge or cutover occurred. Supabase quota/recovery and provider/receiver evidence are preserved, not redone. Current checkpoint: `/tmp/zola-astra-activation-current.md`. **Autonomous work remains incomplete.**

## 2026-09-06 22:40 UTC — n8n access restored; writer defects and migration gap verified

Owner evidence verifies canonical Supabase physical recovery point **06 Sep 2026 14:03:39 UTC** and authenticated Restore controls. Quota remains CLEARED. Recovery is operationally verified for that point, operator capability and the documented procedure; no production restore, measured RTO or Storage-object restoration is claimed. These facts supersede earlier access/backup blockers below.

Authenticated n8n management returns HTTP 200. Buyer workflow `VvMHSIbycYCx4CZN` is active/published at version `cdd141ba-8d20-4981-b598-6af8e35aff86`. Its exact root-only snapshot passes checksum, graph, active-version parity and offline restoration-payload checks. The same legacy anon JWT appears in seven Code nodes; it is not service-role authority. The webhook is unauthenticated, validation can be overridden by input, and all nine database write sites ignore HTTP failures. Three offline regressions reproduce the unsafe behavior with zero production calls. No workflow mutation or execution occurred.

Fresh catalog inspection and a disposable SQL rehearsal confirm the reviewed Buyer migration omitted RawSale/CleanSale anonymous inserts. The unapplied file now closes that gap; independent review and eight grouped amended-fixture checks pass. New Buyer digest: `61baa67314a77d4fa0f0b587821de9216dfa0220d1bb9e22b2808bc2002ae01e`. Both production migrations remain UNAPPLIED. Synthetic PGlite/PostgreSQL 18.3 proof is not canonical PostgreSQL 17.6 snapshot, dedicated-writer or Nexus end-to-end proof.

Writer replacement remains unimplemented and not approved for live mutation. It requires authenticated owner-authorized dispatch and a bounded job-scoped server gateway; secure storage alone cannot safely elevate the current public webhook. Existing owner-scoped web authorization can issue workload permits without inventing new command write permissions. Fixed rollback `2c0b600` cannot send the proposed authenticated dispatch; recovery must explicitly resolve Buyer intake continuity. All six reads remain NOT RUN and functional rollback remains NO. Boot/provider/authority/generation evidence is preserved.

Root filesystem is 97–98% utilized with approximately 2.2 GiB free. Only `/root/.npm/_cacache` (3.1 GiB regenerable content cache) is recommended for scoped cleanup after checking active npm operations; approval is pending and nothing was deleted. Canonical API/worker remain inactive/disabled/PID0 with the forbidden retained pointer untouched. Original PR head `f934856` has five successful checks; changed SQL needs new-head CI before merge. No push, merge, migration, activation or cutover occurred. Details: `docs/ZOLA_N8N_ACCESS_RESTORED.md`; active checkpoint `/tmp/zola-astra-activation-current.md`.

## 2026-09-06 16:09 UTC — Supabase quota cleared; recovery and writer gates remain held

Fresh exact-path probes at 16:09:35 UTC confirm authenticated canonical `/auth/v1/settings` HTTP 200 and public production `https://blackspirehelix.com/api/auth/status` HTTP 200 with `{"ok":true,"authConfigured":true,"bootstrapRequired":false,"operator":null}`. Direct Auth settings without an API key returns expected HTTP 401: `{"message":"No API key found in request","hint":"No `apikey` request header or url param was found."}`. None contains `exceed_storage_size_quota`. Authentication used the existing active anon API key obtained through authenticated management; this is API-key Auth-settings health, not an end-user login test. Earlier quota/Free-plan blockers below are superseded.

Live management confirms canonical project `kchtrvfcixnimvxxctkj`, `blackspire insight`, ACTIVE_HEALTHY, organization `cfqfpnhjhpqjvtzgezrx` / Blackspire Helix Group, now PRO. Official backup documentation establishes PRO daily-backup capability and seven-day retention, but an actual dated project recovery point and successful isolated restoration remain UNVERIFIED. Available connectors expose no backup inventory/export operation; no canonical database profile, Management API token or recovery artifact was found in the scoped operator/configuration locations. Existing SQLite backups are not Supabase backups. Recovery procedure and safe rollback remain documented in `docs/ZOLA_REMAINING_GATE_PREPARATION.md`.

Fresh READ ONLY migration-history query returns no versions `20260904201014` or `20260904223151`; both reviewed Buyer/Nexus files retain their recorded SHA256 digests and remain unapplied. Historical n8n management returns HTTP 404, “No workspace here”; actual workflow/writer, protected version, executions and authorized server identity remain UNVERIFIED. Secure recovery access/artifact and actual n8n management access are still required. No migration, service activation, provider login, receiver rework, webhook or six-read task was performed. Remaining live activation is blocked on recovery, writer reconciliation and authoritative acceptance witnesses. Sanitized probe evidence: `/tmp/zola-quota-recheck-20260906.json`.

## 2026-09-06 — Final gate continuation; exact acceptance pairing prepared

Recovered clean expected release/PR head `464f05fde73f7a213d6d833b581ef89071a098b2`; remote main remains `53adf74e05c607c0d296923bae05d7ac023ecb57`. Parent and independent review confirm PR #125 OPEN with all five checks successful and exact-head CI `34002197446` successful. No application source changes, new PR, merge or canonical activation occurred.

The isolated production recovery profile now explicitly pairs all four division transports to the existing frontend deployment at exact `2c0b600c268faa0571f08322e16d7f81f37789be`. The prior profile is privately preserved; parent verified only the four URL keys changed and canonical protected fingerprints remain unchanged. Inherited boot/generation proof remains PASS; this newly paired profile has not executed the six reads. Functional rollback remains NO. Canonical API/worker remain inactive/disabled/PID0 with the forbidden retained pointer unchanged.

Fresh authenticated Supabase at 08:10:07 UTC still returns 402 `exceed_storage_size_quota`; public Auth returns 500/upstream 402, independently confirmed by parent. Current n8n management remains unavailable, with historical endpoint 404 and no successor/access found. Current writer, execution history, protected version and replacement identity remain UNVERIFIED. Owner quota/recovery and actual n8n management access remain required. Six reads remain NOT RUN; security migrations and merge stay held. Detailed continuation and local evidence paths are recorded in `/tmp/zola-astra-activation-current.md`.

Continuation preparation now records exact recovery ownership, migration/rollback procedures, service-lifetime containment and additional Storage mutation observations in `docs/ZOLA_REMAINING_GATE_PREPARATION.md`. Fresh 08:31:58 UTC dependency checks remain blocked; no live reads or activation occurred.

## 2026-09-06 — Post-login provider recovered; production acceptance held

The protected provider login is now verified HEALTHY under both API and worker identities. Each passes the exact contained authenticated provider probe and full production environment preflight, including credential, provider and WebSocket checks. The previous expired-login blocker is superseded; no further login or failover is required from this evidence.

Recovered clean release/remote/PR head `8d1582073e0c7ccc33e690db9fbde4e96b3ad88f`; main remains `53adf74e05c607c0d296923bae05d7ac023ecb57`. PR #125 is open with exact-head green validation and remains unmerged. Prepared identities, API-only password separation, eight caller settings, six grants, current authority integrity and fresh protected backup pass. Canonical callers still target public main, so complete six-read acceptance requires explicit pairing to the current candidate frontend.

The nine-file rollback remains REJECTED. The recommended replacement candidate is exact reviewed `2c0b600c268faa0571f08322e16d7f81f37789be`, with ZERO source delta, packaged by the existing immutable release flow. The artifact is created and verified; all 190 packaged files match source and actual authorization modules accept current history and twelve permissions. Independent security tests pass 57/57, root/frontend audits report zero vulnerabilities, and separate disposable composition tests pass 99/99. This is same-source recovery for artifact/configuration failures, not an independent escape from shared application defects. It remains FUNCTIONALLY VERIFIED: NO, and six real reads remain NOT RUN.

Fresh authenticated Supabase still returns HTTP 402 `exceed_storage_size_quota`; public auth returns 500/upstream402. Recovery evidence remains unavailable. Historical n8n management still returns404; current workflow, writer, executions and protected rollback version remain unverified. Owner actions are quota/recovery access and actual n8n management access, not provider login. No webhook, security migration, paid Nexus operation, enrichment or merge occurred.

Detailed exact-source evidence and six-read acceptance requirements are in `docs/ZOLA_POST_LOGIN_ACTIVATION.md`. A separate persistent production-mode rehearsal now PASSES actual API -> health -> worker -> current-generation readiness -> stopped-worker rejection. The protected copy fenced 42 queued subtasks, preserved outcome_unknown without replay, revoked copied sessions and remapped workspace paths; emergency stop stayed active. Both hardened rehearsal services stopped cleanly, cgroups/listener are absent, and canonical files plus copied authority/queues/sessions remained unchanged. This proves isolated boot and generation gating, not six reads or functional rollback. Canonical units remain inactive/disabled/PID0 with forbidden retained608 pointer unchanged.

## 2026-09-06 — Nine-file rollback composition rejected

Recovered clean local/remote/PR #125 head `f11b31ef9f4a3926d45d754892454983972b42e8`, with main unchanged `53adf74e05c607c0d296923bae05d7ac023ecb57`. Established Vercel receiver parity and release application validation were preserved, without redoing deployments or capability implementation.

The exact nine-file recovery proposal (266 additions/47 removals; SHA256 `d5954c65b757db5381551a121e24aaa21ab2512b76762d10abca3df73029ab66`) is REJECTED as a production rollback composition. Every-file necessity and behavioral scope are recorded in `docs/ZOLA_NINE_FILE_ROLLBACK_AUDIT.md`. A fresh isolated main archive plus only that patch passes root lint/typecheck/build and frontend deterministic install/lint/TypeScript/production build, but parent independently reproduces three cancellation failures, three Buyer report ownership failures and three route-authorization source-contract failures. Frontend audit reports eight high, two moderate and one low vulnerability. The earlier118 selected passing tests were insufficient to establish full composition safety. Omitted Telegram fail-closed protections are an additional source finding. No accepted rollback SHA or immutable artifact was created, and no approval to adopt this unsafe proposal is requested.

A fresh read-only canonical authority backup, `command-20260906T000335Z.sqlite` under `/var/backups/blackspire-command/zola-nine-composition-20260906`, passes checksum, integrity and candidate schema verification. Actual candidate authorization modules accept current versions1/2, the valid grant chain and all12 permissions on the copied snapshot. This proves compatibility without a migration, not successful boot. The backup SHA256 is `674fab68506ccfd8627917314e3ee38a52150103f9086e8446ab1b105238d46c`.

Fresh dependency evidence remains unchanged: authenticated Supabase returns402 `exceed_storage_size_quota` on the canonical Free organization; public auth returns500/upstream402; current n8n management/writer/recovery access is unavailable; provider credentials remain expired and unchanged sinceAug24. Exact owner login command remains `sudo /var/lib/blackspire-operator/login-codex`; no refresh loop or login process was started. Both canonical roles remain inactive/dead/PID0 with prepared identities and API credential separation preserved. No migration, webhook, grant mutation, paid Nexus call, enrichment write, outreach, public runtime activation or merge occurred.

Real API/worker boots, generation-fenced readiness and six real reads remain unproven and cannot be replaced by synthetic tests. Before any future copied-state worker rehearsal, fence all executable copied work and remap workspace paths, while preserving authority history. A fully reviewed release-derived recovery composition is a possible next engineering direction after dependency recovery; it is not selected or approved here. Current recovery state is maintained in `/tmp/zola-astra-activation-current.md`.


## 2026-09-05 — Timeout recovery and receiver deployment parity verified

Recovered canonical PR #125 without discarding inherited work or creating another branch/PR. The completed supported prebuilt operation `33999369616` passed on `2c0b600c268faa0571f08322e16d7f81f37789be`; exact-head CI `33999371203` also passed. Candidate `dpl_7z57bqJfHf9QJeRGjg9wmpX4SbBF` is READY at that release SHA. Production `dpl_6o7u5qxfvJNR1AS9SKXuo3N9kgzM` is READY at unchanged main `53adf74e05c607c0d296923bae05d7ac023ecb57`. Parent independently resolved the canonical production alias and verified both candidate and public production: paired token/workspace reaches input validation with HTTP 400 for limit zero; wrong token and wrong workspace return HTTP 404 JSON. These probes make no database queries. All four scoped receiver settings remain paired. Receiver configuration and deployed auth parity are complete; six real data reads are not claimed.

The operational helper uses pinned CLI 59.11.7, ordinary isolated exact-SHA clones with verified genuine GitHub origin, credential-free dependency installation, a local-only reinstall suppression, private drained logs, contained artifact symlinks and digests, fresh Git/production identity fences, candidate-first acceptance, nonsecret upload recovery identifiers, and bounded terminal-state polling. Global project settings, ignore rules, author identity and generic preview configuration are unchanged. Twenty-four focused operational tests pass, with independent reviews and memory/secret/whitespace checks. Prior application validation remains 1,317 total, 1,308 passed, zero failed, nine expected skips, 89 completed files and clean containment; no additional local full trusted run was needed for this operational continuation. Initial preparation failures and the CLI worktree-provenance defect are resolved; the earlier Vercel account block is not a remaining external action.

Fresh authenticated Supabase evidence still reports HTTP 402 `exceed_storage_size_quota` on the canonical Free organization; public production auth still fails with upstream 402 after rebuild. Provider credentials remain unchanged and expired; user action is `sudo /var/lib/blackspire-operator/login-codex`, then both role provider paths require verification. Historical n8n management returns 404, so actual writer, executions, replacement authority and protected rollback version remain unverified. Supabase quota/recovery access and actual n8n management remain external. Reviewed Buyer/Nexus migrations remain unapplied. Prepared API/worker identities, API-only password profile, eight caller settings, six grants and protected snapshots are preserved. Both canonical services remain inactive/disabled with PID zero; the incompatible retained VPS pointer must not be started.

Rollback A (unchanged main) accepts post-v2 authority history and all twelve permissions but fails functional six-read requirements. Rollback B (the exact three-file Buyer-only patch, 152 additions/9 removals) passes nine selected Buyer tests and preserves other main source, but leaves Deal read mutations and Nexus argument loss. Parent reproduced the actual Deal helper's two synthetic mutation calls and the dropped Nexus ID. Neither A nor B is a safe six-read target. A separate wider proposal, `/tmp/zola-proposed-recovery-wider-delta.patch` and `.md`, contains nine files with 266 additions/47 removals covering the necessary Buyer/Deal/Nexus contracts, receiver/helper and dispatch dependencies. It passes 118 isolated tests; parent reran 91 and all six adapter payload probes. Its full security composition, build and real runtime/provider rehearsal remain unproven. It is a reviewable scope proposal, not an approved or functionally verified rollback target. Preserve immutable main and obtain the deliberate broader-scope decision before selecting a replacement. Retained `608b10…` and `b71c9c…` remain forbidden.

No canonical VPS activation, Supabase migration, n8n invocation, paid Nexus call, enrichment mutation, outreach, merge or release cutover occurred. The production frontend rebuild retains main's source SHA; VPS remains inactive. Current heads, exact deployment evidence, external actions and recovery instructions are in `/tmp/zola-astra-activation-current.md`.


## 2026-09-05 — Continuation fixes and scoped receiver-access audit

A fresh read-only review of canonical PR #125 found two original findings still present despite the earlier preparation-complete checkpoint. The internal Deal Analysis path now resolves the exact persisted deal ID independently of the UI's top-100 ranked list and rejects missing configuration or failed persisted detail queries. It retains computed underwriting when the analysis row is genuinely absent and preserves the existing UI/scaffold behavior. Buyer Profiles now uses unique ascending ID as the secondary ordering before its bounded limit. Behavioral regressions failed on the previous implementation and pass with these corrections; no division redesign or production data write occurred.

The Vercel connector and local CLI lack environment management, but the existing runner-only Vercel credential works. Audit run `33995999008` at `42b7012` confirmed both receiver settings absent in production and release preview; its failure is a truthful configuration gate, not a broken credential. The reviewed missing-only operation ran successfully as `33996318069` at `7072d92`: four scoped records were created and exact token/workspace pairing verified. Generic previews were unchanged. A second predecessor-fenced operation is prepared to build the exact release preview, prove token/workspace denial gates without database reads, then rebuild only unchanged production main53ad. It rechecks both Git refs and the recorded live production deployment before each creation, stops on cancellation/uncertainty, and changes no project settings or ignore rules. Subsequent pushes only audit. Seventeen focused receiver tests and independent review cover scope, acknowledgements, lost responses, SHA/production drift and negative auth probes. Fresh builds are not yet executed at this checkpoint.

Fresh probes still show authenticated Supabase Auth HTTP 402 `exceed_storage_size_quota`, public frontend auth HTTP 500/upstream 402, unchanged expired private provider credentials, and no current n8n management access. The historical n8n management host returns 404; actual current workflow/writer identity remains UNVERIFIED. No refresh was retried, n8n webhook invoked, browser grant revoked, or security migration applied. Caller configuration, six minimum grants, authority integrity, API-only password profile and split identities remain prepared. Exact units `blackspire-command.service` and `blackspire-command-worker.service` are loaded, inactive and disabled with PID zero; the public Jarvis API returns 502. The approved immutable main recovery candidate remains authorization-compatible but cannot satisfy all six functional reads unchanged: its frontend lacks the Buyer receiver and its actual VPS adapter drops Buyer Match arguments. An isolated invocation of the immutable adapter reproduced that loss. Even a VPS-only rehearsal against the candidate receiver cannot establish correct Buyer matching; a false empty match is not acceptance. Preserve the artifact and request an explicit recovery-target or acceptance-scope decision after preparing a concrete minimal patch. Retained older runtimes are not rollback targets.

Independent review of the narrow corrections and receiver audit found no blocking issue. Frontend lint, TypeScript and production build, root lint/typecheck/build, secret scan and both high-severity dependency audits pass with zero vulnerabilities. The first trusted run completed all 89 files with 1,317 tests (1,307 passed, one failed, nine skipped) and clean process containment. Independent inspection of its disposable audit rows proved the historical persistence fixture changed client IP from IPv6 to IPv4 across restart, rather than losing a rate-limit bucket. The fixture now pins IPv4 with all assertions unchanged; the final trusted rerun passed 1,308 of 1,317 tests, with zero failures and nine expected skips. All 89 files completed and containment proved natural exit, output drain and zero remaining descendants. The final audit-only scope clarification was separately verified by six focused tests; exact-head CI run `33996000808` passed on `42b7012`, including the additional scope test. The subsequent missing-only provisioning operation has eleven focused tests and independent clean review; CI `33996320065` passed exact `7072d92`. Fresh rebuild-operation head validation remains required. PR #125 remains the sole release PR; main stays `53adf74e05c607c0d296923bae05d7ac023ecb57`. Merge, canonical service activation and deployment remain held until every activation gate passes. Current operational evidence is maintained in `/tmp/zola-astra-activation-current.md`.

## 2026-09-05 — Astra activation preparation completed; external access gates remain

Release implementation remains `df8c87c68868da1e980b8206ef65ee14a5c69047`; no application code changed and the trusted 1,301-test result remains applicable. Recovery began at clean local/remote/PR head `76a72a43cd025c9d02aa678da76fd6c82563f673`, with main unchanged at `53adf74e05c607c0d296923bae05d7ac023ecb57`. PR #125 was freshly OPEN/MERGEABLE/CLEAN with all five GitHub contexts successful. Vercel confirms its documentation-only frontend deployment was skipped by the ignored-build step; the implementation preview is READY. Production frontend remains the main deployment, not the candidate.

Under the explicit activation authorization, Astra stopped/disabled the already-degraded coordination target, cleared failed unit state, and prepared the reviewed non-root API/worker identities, byte-identical units, shared-state access, API-only credential profile, and log rotation. A cryptographically random operator password was generated and verified without disclosure; the worker cannot read the API profile or root-only operator credential store. The private provider home is worker-owned with explicit API diagnostic access. No API or worker was started and the production release pointer remains on retained `608b10fd233a5a2a94fd0ce4cc03d73894c5694d`.

The six minimum capability permissions were provisioned through the canonical version chain after a successful disposable rehearsal and fresh writer-absent backup. Version 1 lifecycle was atomically superseded while its authority payload/history remained intact; version 2 adds exactly the six read permissions. Production verification passed the single-head chain, original plus added permissions, denial of broader permissions, and SQLite integrity. Pre-change `command-20260905T075425Z.sqlite` and post-change `command-20260905T075529Z.sqlite` passed checksum, integrity, freshness, and schema compatibility. Existing snapshots remain root-only. An immutable candidate release was prepared and passed release preflight without activation.

All eight VPS division caller settings are now provisioned. Vercel receiver synchronization remains external: the available connector cannot manage environment values and no usable local Vercel credential was found. Requests using the prepared caller token are denied; receiver parity and authenticated capability reads are not healthy claims. Both role preflights now reach the provider gate and fail there. A supported, non-generating Codex account refresh returned HTTP 401 `invalid_refresh_token`; this proves interactive service login is required. The protected VPS login helper also repairs API access to the replacement credential file.

Supabase quota remains the established account-owner blocker. Current authenticated metadata confirms both reviewed security migrations remain unapplied. Project backup/recovery remains unproven through available access. Current frontend still depends on the external n8n Buyer workflow; its archived anonymous writer touches SearchJob, RawSale, CleanSale, BuyerProfile, and BuyerReport, so current identity must be verified/migrated before browser revocation. No n8n webhook was invoked and no Supabase migration or production enrichment was performed.

Retained VPS artifacts passed digest/deployment identity and snapshot schema checks; historical provider success is corroborated for `b71c9cd7178a76c0e86c15d6f3b633ed13c278b8`. Neither artifact is a currently proven healthy rollback because provider/configuration recovery and a functional rehearsal remain outstanding. Independent follow-up found retained `608b10…` and `b71c9c…` reject the new permission names anywhere in grant history; a later successor removing them does not repair old-runtime compatibility. The pre-grant snapshot remains preserved. An immutable current-main `53adf74…` recovery candidate was prepared; its exact authorization modules passed the post-grant snapshot chain and all twelve permissions without rewriting history. This is compatibility evidence only; functional rollback still requires recovered dependencies and a controlled rehearsal. PR merge and deployment remain held. Exact gate table, protected local helper locations, preparation recovery paths, and next actions are maintained in `/tmp/zola-astra-activation-current.md`.

- Last verified implementation commit: `eb3f5d196eacbc7ee3e03da5f6b9e17634dba236`

## 2026-09-04 — Activation recovery delta

Release implementation remains `df8c87c68868da1e980b8206ef65ee14a5c69047`; existing PR #125 was freshly OPEN/MERGEABLE with all five CI/Vercel contexts successful at that exact head. Main remains `53adf74e05c607c0d296923bae05d7ac023ecb57`; merge and deployment remain held. Auth's authenticated upstream probe confirms HTTP 402 `exceed_storage_size_quota`; Supabase management confirms the production organization is on the Free plan. Account-owner subscription action is required.

A fresh canonical SQLite snapshot, `command-20260904T234621Z.sqlite`, was created using the reviewed backup script with a sanitized environment and Node 22.23.1. Both canonical units had MainPID zero and remained failed, with unchanged restart counts; no process held the database or its WAL/SHM files open. Independent verification passed checksum, integrity, freshness, and schema compatibility. This closes the fresh local snapshot gap only: Supabase recovery, current external n8n writer identity, private provider authentication, operator password, runtime identity/configuration, division grants/transports, and a healthy functional rollback remain unresolved. No source database, service configuration, migration, release pointer, or deployment changed.

## ZOLA production follow-up audit (2026-09-04 UTC)

The clean release worktree and existing PR #125 at `e15c8d4b1f76eb8503f0b447f6afe22d81537c66` were preserved. Three native subagents covered all twelve reconnaissance domains; parent review reproduced additional Buyer defects. Capability profiles now reject missing configuration; capability matching rejects failed database reads and cannot seed or substitute registry data. Buyer reports require an authenticated operator and an owner-filtered `SearchJob` join before pagination and counts. These changes extend the existing engines and authority path.

A full-suite follow-up exposed a pre-existing cancellation fixture race: a rejectable adapter promise was left unobserved across a scheduling yield. The fixture now uses explicit adapter-start coordination and stronger persisted-result assertions. Separate production regressions reproduced on an exact starting-main archive showed synchronous cancellation leaving an unhandled abort rejection and already-aborted signals still dispatching an adapter. The shared executor now checks cancellation before creating the abort promise and prevents dispatch for an aborted signal; focused tests fail on starting main and pass with the correction. No sleeps, suppressed errors, forced exits, or weakened assertions were used to make these gates pass.

The historical launcher second-signal flake was also reproduced: an idle API can correctly exit zero before receiving the second signal. Its test now holds an accepted HTTP request open and observes draining before sending that signal, preserving the nonzero-exit and listener-cleanup assertions. Production launcher behavior is unchanged.

Final Node 22.23.1 trusted validation passed 1,292 of 1,301 tests, with zero failures and nine expected host skips. All 87 files completed; output drained, the runner exited naturally, and zero descendants remained. Root syntax/build gates, actual frontend ESLint/TypeScript/production build, secret and whitespace checks, and both high-severity dependency audits passed; audits reported zero vulnerabilities. These are local release gates, not production acceptance.

Authenticated Supabase metadata access now works. The earlier claim of unavailable migration access is superseded. Live metadata still shows the unapplied Nexus security migration and unconditional Buyer browser policies. The additional `20260904223151_buyer_browser_security.sql` revokes browser access to Buyer profiles/reports and anonymous SearchJob access while preserving the authenticated own-job policy and service-role grants. Both migrations passed disposable PostgreSQL verification for browser denial, owner isolation, service reads, row preservation, and Buyer migration reapplication. Neither has been applied to production. An archived n8n workflow uses anonymous writer credentials: reconcile its actual live identity before revoking grants, and establish Supabase backup/recovery first.

Fresh runtime evidence: `https://jarvis.blackspirehelix.com` returns HTTP 502; canonical API and worker services are failed at retained SHA `608b10fd233a5a2a94fd0ce4cc03d73894c5694d`. Vercel production remains at starting main; `/api/auth/status` reports `Auth user count failed with status 402.` This does not establish a missing browser key or the exact account failure. API password configuration and division transport configuration remain absent, installed service identities are stale, and healthy private provider authentication and rollback remain unverified. Updating the release PR is authorized; merging it is held because main automatically deploys the frontend and production recovery/configuration gates are not satisfied. No production data, configuration, services, providers, or outreach changed.

## ZOLA production-live release candidate (2026-09-04 UTC)

Nexus PR #124 merged from exact head `3c8a6991d91c8705618c6903bd272fc6ed0e8e18` as main commit `53adf74e05c607c0d296923bae05d7ac023ecb57`. Release implementation `eed4f641fdc44b7ae88b0353b3c77a8df227211d` preserves Nexus as a bounded persisted read: no provider lookup, paid call, mutation, raw phone, raw email, or raw provider payload occurs or is disclosed. Exact Deal-ID routing, Buyer profile/match reads, and Deal analysis not-found semantics are now covered by focused endpoint tests. Cancellation consumes late adapter rejection while the existing ownership, stale-worker, no-replay, permission-recheck, emergency-stop, and transactional-finalization authority path remains shared rather than forked.

The frontend now denies anonymous access to operator pages and sensitive API routes, removes raw contact fields from search and Nexus surfaces, and keeps internal capability endpoints service-authenticated and exact-workspace-bound. Telegram webhook mode fails closed without its secret, bot token, and non-empty allowlist. The Nexus browser-role revocation is a timestamped Supabase migration. Next.js is pinned to 16.3.4 with React 19.2.8; root and frontend high-severity audits report zero vulnerabilities.

Node 22.23.1 focused capability/security validation passed 100/100. The preserved candidate's trusted lane passed 1,278 of 1,287 tests with zero failures and nine expected host-conditional skips; all 87 intended files started and completed, the runner exited naturally, output drained, and no descendants remained. Root lint, typecheck, build, secret scan, audit, whitespace, and frontend lint/build/audit passed. Current configuration and deployment findings are recorded in the follow-up audit above; the earlier missing-key and unavailable-control-plane conclusions are superseded. The previously deployed VPS SHA `608b10fd233a5a2a94fd0ce4cc03d73894c5694d` is retained but is not a proven healthy rollback because its canonical API/worker units are currently failed.

## PR #120 final P1/P2 correction (2026-09-02 UTC)

Branch `feature/jarvis-password-auth` continues the authenticated Blackspire Command browser/PWA path on `origin/main`. It replaces token-shaped browser login with asynchronous password verification against `COMMAND_ADMIN_PASSWORD_HASH`, preserves `COMMAND_ADMIN_TOKEN` solely for explicitly enabled bearer clients, and enforces one process-wide four-derivation admission bound that refuses excess scrypt work immediately with no waiting queue. Session issuance still requires verified credentials and a server-resolved canonical principal. Production requires a valid supported password hash; development retains only an explicit non-browser legacy fixture path.

## PR #119 capability/security history

Branch `feature/jarvis-capability-registry` extends the existing Blackspire Command/Hermes path with one server-owned capability contract and deterministic fail-closed registry. The first registered capability, `seller.opportunities.search`, is read-only, requires the persisted `seller.opportunities.read` workspace grant, and reaches the existing Seller Engine Supabase `seller_leads`/`properties` owner only through a token-authenticated, exact-workspace internal adapter. It returns bounded, non-contact opportunity fields and canonical evidence/result text; Jarvis receives no database, filesystem, credential, provider, or registration authority.

The first exact-head review reported three P1 and five P2 defects. The local correction makes successful attempt/task/evidence/event finalization one authority-fenced transaction; blocks Seller-bearing task, evidence, export, conversation, event, and action surfaces without the current Seller permission; distinguishes empty results from Seller configuration/query failure; and enforces byte-safe token comparison, object-only JSON, request-scoped result counts, streaming response bounds, and deterministic query ties. Focused acceptance includes disclosure ablation plus cancellation, grant, emergency-stop, claim-loss, oversized stream, and version-skewed adapter cases. This correction remains local: new exact-head CI/review, merge, deployment, production Seller configuration, and production verification are not yet claimed.

PR #117 is merged in `main` and supplies the canonical server-authoritative Jarvis task result consumed by this slice. PR #118 remains open at documentation head `d269015b4ac7a71e3c6e4eb09e5b1fe700757428`; its architecture remains consistent, but an exact-head external review was unavailable, so no approval or merge is claimed.

Local follow-up to `759970c07c8b6c86f60179a687252edeff75200e` rejects API credential-group numeric aliasing with either worker-allowed GID. The offline session-fence primitive now independently requires a successful systemd proof that the API unit is `inactive` before revocation; the runbook is fail-fast and checks the same state before invoking it. Targeted regressions and mutations passed. This correction is local and unpushed; production changed: NO.

## Canonical Jarvis result implemented and validated locally (2026-08-30 UTC)

- Added a canonical frozen capability contract and deterministic server registry, then extended existing Hermes rather than creating a second orchestrator. The first registered capability is `seller.opportunities.search`, fixed to read-only intent, low risk, no approval, zero external cost, exact task workspace, and the persisted `seller.opportunities.read` grant.
- Added durable capability dispatch/finalization on the existing provider-attempt owner. Grants are resolved from the canonical persisted principal and rechecked before dispatch and disclosure; cancellation, emergency stop, worker ownership loss, and uncertain post-dispatch failure remain absorbing/no-replay outcomes.
- Added a token-authenticated, exact-workspace internal Seller adapter that queries the existing Supabase Seller Engine owner and returns bounded canonical property/lead fields without contact data. No parallel real-estate database, unrestricted division access, or caller-controlled capability authority was introduced.
- Deterministic focused tests pass 11/11, the broader compatibility lane passes 79/79, root lint/typecheck/build and frontend lint/build pass. A deliberate removal of the permission guard failed the targeted authorization regression before restoration. The full trusted lane recorded 1,140 tests: 1,130 passed, 1 failed, and 9 host-conditional skips; the sole failure was `launcher-signal-containment`, which immediately passed 6/6 in isolation and does not touch changed files. CI, review, merge, deployment, production Seller configuration, and live data acceptance are not yet claimed.

Current-head CI correction (2026-09-08): full CI at dfd4197 exposed seven stale Buyer/Nexus helper fixture and source assertions after explicit read-client injection. Updated the tests to supply synthetic clients, preserve failure-source assertions and require observed response sealing. Production implementation is unchanged; full exact-head CI must pass after this correction.
