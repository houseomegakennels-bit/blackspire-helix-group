# Blackspire Next Actions

0. **DONE 2026-07-30 — PR #57 merged as `763e74e6332b51fdacd30b29d9f836d5de6c2001` from reviewed head `80726cd3e034afc3e72921de9e426bd28dbe717e`.** Full local validation, GitHub CI, all Vercel checks, mergeability, resolved threads, and two fresh independent final reviews were green. Phase 3A remains factual and non-operative.

## Immediate safe actions

0. **Hermes Milestone 2 (Runtime & Provider Framework) is MERGED** via [PR #56](https://github.com/houseomegakennels-bit/blackspire-helix-group/pull/56) (reviewed implementation head `148a82cde1105a8917be2e38018c6e8ff4a7e1d4`; merge `a4a0407e5d72019b4a99cf61568598434e7e136d`). It adds a development-only real-provider runtime (first adapter: non-agentic Anthropic Messages API), disabled by default and refused in production. Real dispatch requires the development profile, explicit feature flag, provider allowlist, registered/allowlisted workspace root, bounded positive spend reservation, and a valid scoped approval. **Only with separate explicit operator approval**, the prepared-but-disabled `scripts/hermes-dev-smoke.js` may make one bounded development call; it was not run for M2. **Deferred to Milestone 3:** multi-provider learned routing, scorecards, approval-gated memory promotion, cross-host limits. Do not enable multi-provider autonomous routing or automatic memory promotion. Production stays `disabled/inactive/dead` and Gate 4 stays unauthorized regardless of Hermes milestones.
0a. **IN REVIEW — Hermes Milestone 3B is implemented on [PR #60](https://github.com/houseomegakennels-bit/blackspire-helix-group/pull/60)** (head `ec1410c6de41b0086c7064e1e3c5b710616df795`, still an open draft, not merged). Deterministic aggregation, minimum-evidence thresholds, confidence/unknown handling, workspace isolation, replay/idempotency, append-only lineage, and zero routing or memory side effects are implemented and pinned by 22 tests; three review rounds of two fresh independent read-only reviewers each returned zero Critical findings, and every confirmed finding is fixed. Each of the second and third rounds found a real defect introduced by the previous round's own fix, so the final round was briefed to hunt that class specifically and found none; both final reviewers recommend merge. Remaining before merge: operator merge authority, then the docs-only anchor refresh. Original scope statement: build non-operative verified scorecards and learning-quality evidence from intact Phase 3A records. Acceptance must prove deterministic aggregation, minimum evidence thresholds, confidence/unknown handling, workspace isolation, replay/idempotency, append-only lineage, and zero routing or memory side effects. Approval-gated memory candidate review/promotion follows only after 3B; provider/agent registry, verified identity, operator PWA, Telegram, learned routing with rollback, and production/Gate 4 remain dependency-ordered later tracks.
0b. **Hermes Milestone 1 is MERGED (PR #55, anchor `a419c3e`, merge `4e1ec49`)** — mock-only foundation.
1. PR #35 is MERGED into `origin/main` (superseded as trusted `origin/main`, see item 2): absolute Node interpreter pinning across production startup, systemd unit Environment=BLACKSPIRE_NODE_BIN, host-interpreter verification, and strict production preflight.
2. PR #36 is MERGED into `origin/main` (`6f40a4443f498e77a8a2ee1e17be6da4a4837e67`, parents base `693fb03e4596d26e990f87a40508307810cc5e5d` and reviewed head `bb727976c47eb430af1587c3f361864aaa7d15f4`): the fail-closed living-memory ancestry containment checker with adversarial test coverage. Independent exact-head review returned zero findings; GitHub CI and Vercel checks passed. PR #37 then attempted a documentation refresh to record the new trusted `origin/main`, but recorded it as an exact-equality SHA, which made that refresh commit invalidate its own recorded value the instant it merged (a self-invalidating loop, not a regression). PR #38 (`fix/living-memory-docs-descendant-policy`, reviewed head `bc5a817231c1a325f31a112e256425140bbbf073`, merged as `ce6dcecf5335233656392248a67326a2524521ea`) fixed the contract itself: the recorded value is now a verified implementation anchor checked for ancestry plus a fail-closed canonical-memory-only descendant policy (`scripts/check-living-memory.sh` computes the aggregate tree diff from the anchor to live trusted `origin/main` and requires every changed path to be in the canonical-memory Markdown allowlist), so future documentation-only refreshes no longer invalidate themselves. Independent exact-head review of PR #38 returned 0 Critical, 0 High, 0 Medium, and 0 Low findings; GitHub CI and Vercel checks passed. Because PR #38 changed non-documentation files (`scripts/check-living-memory.sh`, `tests/living-memory-ancestry.test.js`), the immediate post-merge checker run correctly returned `UNREVIEWED_NON_DOCUMENTATION_CHANGE`, requiring this docs-only refresh to record the new reviewed anchor (`bc5a817231c1a325f31a112e256425140bbbf073`) against the PR #38 merge snapshot (`ce6dcecf5335233656392248a67326a2524521ea`). No release was built, no PR was marked ready or merged beyond PR #38 itself, and no production action occurred as part of this action.
3. DONE 2026-07-22 — **PR #29 is MERGED**, not draft. It merged into `origin/main` as `f0a7b66853b4c983d6930c1b70d1d94cb5f26f36` (parents: base `588ea6e1156d7d39e7f8a5a7bde44f8c4e519807`, reviewed head `819d0fc57c6157ecd2bc70dde51216d146d9fc91`) at 2026-07-22T22:09:46Z; both commits are contained in current trusted `origin/main`. This item previously read "Keep it draft; do not mark ready or merge until that review passes," which was already false when written into the record and stayed false until 2026-07-27. No PR #29 action remains.
4. Preserve the verified root-Vercel boundary: the VPS-owned root project must be ignored on every ref while the `frontend` project remains independently deployable. PR #29 and PR #30 exact-head root contexts pass via repository configuration; no external Vercel setting needs mutation.
5. DONE — both preconditions are long satisfied and the rehearsal has since been run twice. PR #29 merged 2026-07-22 (`f0a7b66…`) and staging was rebuilt onto `releases/f0a7b66853b4c983d6930c1b70d1d94cb5f26f36`; a first disposable-only Gate 3 rehearsal ran that same day, with sanitized evidence retained on the host at `shared/evidence/gate3-f0a7b66853b4c983d6930c1b70d1d94cb5f26f36` (its result was recorded only in the unmerged PR #31, so canonical memory never carried it). The authoritative Gate 3 result is the 2026-07-27 rehearsal at trusted `origin/main` `73856891c5f6f35f14c09d1ce5acef39f20c4b25`, recorded in item 21.
6. Gate 3 passed with sanitized evidence on 2026-07-27 (see item 21), which satisfies this precondition and nothing more. Gate 4 production activation is still **not** authorized and must not be requested or attempted until the six host-side blockers and the installed-unit mismatch are closed and a separate bounded production approval is given.
7. Preserve the four original commits and backup branch; do not rewrite them or the integration merge.
8. Keep all validation credential-free and mock-only until explicit authority changes.
9. Preserve the completed operator iPhone acceptance and teardown evidence; restage only for an explicitly approved new acceptance need.
10. When included Codespaces usage renews, inspect the designated existing Codespace before any creation request; do not enable billing.
11. Before production Command promotion, apply and verify the immutable release, WAL-safe backup/restore, no-provider profile, monitoring/log-retention, stable HTTPS, and rollback procedures documented in `docs/VPS_RUNTIME_RUNBOOK.md`; current VPS application remains unpromoted.
12. Preserve the completed restricted subscription Codex acceptance. Any new live Codex task requires a separately scoped approval; do not switch to API-key billing or another provider.
13. PR #26 readiness tooling is merged into `main` (`a9602496`). Do not deploy or activate any release until the six host-side blockers are closed and a separate bounded production approval is given. The prepared `ops/` proxy/TLS and runtime-ownership plans are review artifacts only — do not install or apply them.
14. Obtain a fresh independent read-only review of the draft production loopback-bind PR at its exact remote head before any merge: it must confirm `BIND_HOST` reaches `server.listen`, that public/wildcard production binds and implicit port fallback are rejected, that ports 8787 and 8788 stay protected, and that no running service changed. Keep it draft; do not mark ready or merge until that review passes.
15. Separate pre-existing follow-up (not part of PR #32): `tests/trust-boundary-regression.test.js` is intermittently flaky at the pathname-replacement case, lines 37-40 of the test `immutable test identity detects content mutation and pathname replacement`. The file is unlinked and rewritten with byte-identical content, and the test asserts `verifyTestTreeUnchanged` throws. `descriptorIdentity` in `scripts/test-inventory.js` records `{dev, ino, mode, nlink, size, mtimeMs, ctimeMs, sha256}`, so when the filesystem reuses the freed inode and the rewrite lands within the same coarse timestamp tick, every recorded field is identical and no replacement is detected. The independent reviewer reproduced this against `origin/main`; 25 isolated repetitions on the correction host did not reproduce it, so it is load- and filesystem-dependent, not deterministic. The relevant test and implementation are byte-identical to `origin/main`, so this is pre-existing and unrelated to the bind/port work. Recommended repair: give each capture an explicit generation marker independent of inode and timestamp reuse, or force a timestamp advance past filesystem granularity before the rewrite. Do not fold this into PR #32 and do not claim it repaired. **TRACKED INDEPENDENTLY AS ISSUE #52 on 2026-07-28** (`Flaky trust-boundary regression test caused by unlink/recreate timing and inode reuse`), which supersedes this item as the working record. Measured there, 12 isolated runs per checkout: fresh `main` `96d804d` 3/12, fresh PR #50 `3452b33` 5/12, fresh PR #51 `7a34aec` 5/12, long-established PR #50 worktree 0/12, long-established PR #51 worktree 1/12 — indistinguishable between `main` and both branches. The mechanism was measured directly rather than inferred: on this host's `/tmp` the inode is reused 400/400 times after unlink+recreate, so detection collapses onto sub-millisecond timestamp granularity alone. The test and `scripts/test-inventory.js`, `scripts/trusted-test-runner.js`, and `scripts/test-process-supervisor.js` are byte-identical across `main`, PR #50, and PR #51 (blobs `31ac662`, `a4d7f5c`, `ba7ef2a`, `7a9814f`), so branch identity is not supported as the cause; fresh-versus-established checkout state is a timing correlate, not a proven cause. The defect predates both PRs, neither introduced it, and it blocked neither merge.
16. Do not activate production on the selected port `8789` until that review passes, Gate 3 passes, and a separate bounded production approval is given. `BIND_HOST=127.0.0.1` and an explicit `PORT` must be present in the operator-supplied environment file; there is no default.
17. A staging/nginx outage (root-caused to a 2026-07-24 10:03 UTC mass service-kill) recovered 2026-07-26; both services are healthy again. The nginx restart-policy and staging clean-exit resilience gap this outage exposed is a separate future reliability task requiring its own scoped work; it is not part of PR #36 and is not authorized to start here. Issue #33 remains separate.
18. PR #39 (docs-only implementation-anchor refresh after PR #38) is MERGED into `origin/main` as `1a28343f7d3ea3c2968799bc57fb13555fc73787`. The immutable release was then built and privately validated at that exact SHA (see item 19); do not repeat that rebuild/validation step.
19. The immutable release at `/opt/blackspire-command/releases/1a28343f7d3ea3c2968799bc57fb13555fc73787` passed source validation, the release manifest, ownership/permissions, and the release-tree preflight, and passed a bounded private blue-green boot on loopback `127.0.0.1:8789` with disposable SQLite state, providers disabled, Hermes mock/bounded, and Telegram dry-run/disconnected; teardown removed all temporary state and preserved the release and prior rollbacks. Staging, nginx, port 8787, and production were unchanged. This makes Gate 3 requestable; it does not authorize Gate 3, Gate 4, replacing the installed production systemd unit (which still differs from the reviewed template), or any production `current` symlink/environment/database/activation. The next safe action is to request Gate 3 data-protection, WAL, and backup/restore rehearsal under separate explicit authority.
20. PR #41 (`fix/restore-reject-empty-invalid-backup`) is MERGED into `origin/main` as `d800d283b11b03d88c519e18abbed0870d9fa750`; its reviewed head `40e6d6a873e1579b91004736ba55e2ab87f2e462` is the new verified implementation anchor recorded by this refresh. Gate 3 Finding #1 is closed on both the restore and backup sides. Because PR #41 changed source and test files, the immediate post-merge checker correctly returned `UNREVIEWED_NON_DOCUMENTATION_CHANGE` until this docs-only refresh recorded the new anchor. Do not build an immutable release from a state where this checker fails.
21. **Gate 3 data-protection rehearsal PASSED on 2026-07-27** at trusted `origin/main` `73856891c5f6f35f14c09d1ce5acef39f20c4b25`, disposable-only, against the immutable release built at that exact SHA (36 checks, 0 failures). Do not repeat it for the same SHA. Gate 4 production activation is still **not** authorized: it requires separate explicit approval and remains blocked by the six host-side blockers and the installed-unit mismatch. The next safe action is to close host-side blockers under separate explicit authority, not to activate anything.
22. DONE — `docs/VPS_RUNTIME_RUNBOOK.md` now documents the enforced backup/restore contract, merged as PR #44 (`a78b6395c6176b1dc02d97b7d70807938a3d1beb`, reviewed head `5fa024ec02a7c8677172dd54d9d60579b561da1d`, the new verified implementation anchor recorded by this refresh). As predicted, that file is outside the canonical-memory allowlist, so it required its own reviewed change and this follow-on anchor refresh rather than riding along in a docs-only commit. The runbook no longer understates the contract.
23. DONE — configurable Hermes workspace root shipped as PR #47 (`e55de773fe3e10e7b1352fd8b2b2b10b40063e87`, reviewed head `d11ac82f62e1bf44e716af7d58b6cd3d77115c30`, the new verified implementation anchor). `BLACKSPIRE_WORKSPACE_ROOT` is server-side only, defaults to the historical `.`, and fails closed when set to an empty, relative, missing, non-directory, symlinked, non-git, symlinked-`.git`, or NUL-byte value. This closes the only unique work in PR #22, implemented fresh against main; PR #22's compose/port/container/bind-mount changes were deliberately excluded and PR #22 is closed as superseded. Follow-up, not started and not authorized here: decide whether `scripts/verify-environment.sh vps-production` should *require* `BLACKSPIRE_WORKSPACE_ROOT` rather than allow the `.` default, and whether the resolver should additionally refuse a root inside `releases/`. Both were left out as beyond the minimal safe change — an unset root fails at the kernel rather than corrupting anything, since the release is read-only — and either would tighten the production contract, so both need their own scoped review.
24. DONE 2026-07-27 — the installed production systemd unit is repaired by a host-only correction (no code change). `/etc/systemd/system/blackspire-command.service` is now byte-identical to the reviewed `ops/runtime-ownership/blackspire-command.service` (`root:root` `0644`, SHA-256 `4c3f3387e99086f94f2aca4e1755d12b34b6422c6618add1c9ff5b09aa95866b`), `daemon-reload` was run, and the service remains `disabled`/`inactive`/`dead` — it was not started, restarted, enabled, or activated. `npm run production:preflight` and `npm run production:preflight:host` both report `ok=true source=21/21 deployment=2/2`. This closes the installed-unit blocker; it does **not** authorize Gate 4 or any activation. The prior unit would have failed every start (`/usr/bin/env node` → Node 18 without `node:sqlite`), so re-check this after any future host Node change or unit edit.
25. **PREFLIGHT HALF CLOSED 2026-07-28 by PR #50 (merge `827c50015f8d3af3f6bb6c2c9f581097edf6b839`); the supervisor/`config.js` coupling below remains OPEN.** Original text, retained for the record: open follow-up from the unit repair, needing its own scoped review because it changes the production preflight contract: `scripts/verify-environment.sh` does not validate `BLACKSPIRE_WORKSPACE_ROOT`, and `scripts/production-supervisor.js` does not import `packages/shared/config.js`. An invalid workspace root therefore passes `ExecStartPre` and fails only when the API/worker children load config, producing a `Restart=on-failure` loop up to `StartLimitBurst` instead of a clean preflight refusal. Related to the separately tracked coupling in which `scripts/backup.js` and `scripts/restore.js` import `config.js` and so also refuse to run under an invalid workspace root.
26. Track startup-wrapper signal forwarding (the private validation runner did not forward termination signals to its API/worker children, requiring teardown to terminate them directly) as a separate future tooling-hardening item before the private runner is treated as production-grade.
27. **DONE 2026-07-28 — PR #50 (`fix/verify-workspace-root-preflight`, reviewed head `3452b33278ed250f00fc4a173d32a66e530fac9c`) is MERGED as `827c50015f8d3af3f6bb6c2c9f581097edf6b839` and closes the preflight half of item 25.** This item previously read "IN REVIEW … OPEN, not merged"; that was accurate when written and is superseded by the merge and by this anchor refresh. `scripts/verify-environment.sh vps-production` fails closed on a missing, empty, whitespace-only, relative, nonexistent, non-directory, symlinked, unreadable, untraversable, unwritable, non-git, or application-file-missing `BLACKSPIRE_WORKSPACE_ROOT`. The rules are deliberately stricter than the runtime resolver's and never looser, and the asymmetry is pinned by a test. `scripts/production-profile.env.example` now documents `BLACKSPIRE_WORKSPACE_ROOT=/opt/blackspire-command/shared/workspace` (the only tree `ReadWritePaths` makes writable) and `profile-required-keys` enforces it. Validation at that head: 463 tests, 454 passed, 0 failed, 9 host-conditional skips; preflight `ok=true source=21/21 deployment=2/2`. Do not record item 25 as closed, and do not build an immutable release assuming it is, until this merges and its anchor refresh lands. The supervisor/`config.js` coupling in item 25 is unchanged and stays open regardless.
28. **Gate 4 preparation tooling exists; Gate 4 itself remains unauthorized.** `scripts/gate4-prepare.sh` (read-only, no mutating mode, idempotent, fail-closed) reports which Gate 4 prerequisites are satisfied and which operator inputs are outstanding, and `--plan` prints the exact operator command sequence with a hard authorization boundary before any activation command. `docs/GATE4_ACTIVATION_CHECKLIST.md` is the operator checklist. On this host the current outstanding items are: the approved SHA (operator input), `/etc/blackspire/command.env` (absent), the seeded workspace at `shared/workspace` (absent), the approved release and `current` symlink, a production backup snapshot, log rotation, and three operator attestations (reverse proxy/TLS, monitoring alert testing, backup/migration rehearsal). Preparing is not authorizing: no automation may run the activation block, and Gate 4 still requires a separate explicit bounded operator approval. **PR #51 is MERGED as `437e27e0f2c5ac1e79cd0b93ea44c23790d06486` (reviewed head `7a34aecb9f34cb69af41728f0281045fabf4ff64`, the new verified implementation anchor); post-merge revalidation reproduced 23/23 focused and both preflights `ok=true source=21/21 deployment=2/2`, and the live checker still reports `ready=false`.**
29. **Next required step before any Gate 4 preparation: none in the repository — the next action is an operator decision.** With this refresh merged, `scripts/check-living-memory.sh` returns to `PASS` and the repository is clean at the anchor, so an immutable release may be built from trusted `origin/main` without stale canonical memory. To advance Gate 4, the operator must first name the approved SHA (`BLACKSPIRE_GATE4_APPROVED_SHA`) and then, each as a separately authorized action, create `/etc/blackspire/command.env` from `scripts/production-profile.env.example`, seed the workspace at `/opt/blackspire-command/shared/workspace` as a real checkout, verify the approved release, create `current`, take a production backup with `scripts/backup.js`, install log rotation, and record the three MANUAL attestations. Run `PATH=/opt/nodejs/node-v22.23.1-linux-x64/bin:$PATH bash scripts/gate4-prepare.sh --check` first; it is read-only and changes nothing. None of these may be performed by automation, and none of them authorizes Gate 4, which remains a separate explicit bounded operator approval.

## Operator-only actions

Push/merge authorization, budgets/spend, credentials, GitHub authorization, device acceptance, production and DNS changes, real Telegram, paid/live providers, host security, emergency controls, constitutional changes, trading, and funds movement.

## Blocked or deferred

- New Codespace creation: blocked by usage credit.
- Real Telegram and live providers: not authorized.
- Production Command launch: readiness incomplete and live state `UNVERIFIED`.
- PR #26: repository-side readiness tooling is merged into `main`, but host-side production approval remains blocked by six items, each separately authorized: (1) reverse proxy/TLS install and verification, (2) least-privileged non-root runtime ownership provisioning, (3) installed monitoring/log rotation with alert-delivery testing, (4) approved production backup/migration rehearsal or execution, (5) recorded exact known-good live release/database rollback target, and (6) readiness tooling in the deployment artifact.
- Multi-instance/serverless Command persistence: requires architecture work beyond SQLite.
