# Blackspire Next Actions

## ZOLA continuation critical path (2026-09-05 UTC)

1. Local trusted validation and independent review pass for the narrow Deal exact-ID/failure and Buyer deterministic-limit corrections; push only the existing release branch/PR #125, then verify exact-head CI and the new audit-only receiver workflow. The existing runner Vercel credential is verified and both receiver settings are missing. Parent can now execute the reviewed missing-only synchronization and rebuild pinned deployments without requiring new user access. Audit equality alone is not deployed receiver acceptance.
2. Owner clears the Supabase organization quota and securely makes recovery evidence or DB access available. Owner completes `sudo /var/lib/blackspire-operator/login-codex` on the canonical VPS. Owner supplies management access to the actual current n8n Buyer workflow or authoritative successor. Recheck Auth and both provider-role paths, establish recovery and protected writer identity before the two reviewed security migrations.
3. Preserve split API/worker preparation and six grants. Unchanged current-main cannot pass all six rollback reads (missing Buyer receiver and dropped match arguments); prepare a minimal recovery patch and obtain an explicit target/scope decision before claiming functional rollback. After dependency recovery and that decision, rehearse recovery and refresh backups, activate API then worker with generation-fenced health/readiness, and prove all six bounded reads. Merge #125 only with every gate closed and expected-head protection; deploy the resulting same SHA to frontend/VPS and complete production smoke without outreach or paid Nexus activity.

## ZOLA activation external gates (2026-09-05 UTC)

1. Supabase organization owner clears storage quota and makes a verifiable recovery point or secure database backup access available. Then re-test authenticated upstream Auth and public auth status; account plan state alone is not health or recovery proof.
2. Complete interactive Codex login with the protected VPS helper `/var/lib/blackspire-operator/login-codex`. The normal refresh was rejected with `invalid_refresh_token`. Recheck both service roles and required provider transports; do not change the provider contract or introduce failover.
3. Provide Vercel frontend environment-management access or synchronize its internal capability token with the protected VPS token and bind the exact canonical workspace in production and candidate preview. Rebuild the candidate preview before six-capability acceptance. Caller configuration and minimum canonical grants are already provisioned.
4. Provide management access to the canonical external n8n Buyer workflow so its active writer identity, current executions, protected rollback version, and authorized server-credential replacement can be verified. Then establish Supabase recovery and apply only the two reviewed security migrations with row-preservation and role probes.
5. After external dependencies recover, Astra functionally verifies the prepared, authorization-compatible main recovery candidate (retained older runtimes reject the new grant history), refreshes backups, and completes controlled API/worker preparation and generation-fenced capability acceptance. Only after every gate passes may PR #125 merge with expected-head protection, followed by same-main-SHA frontend/VPS deployment and live smoke. Services remain inactive/disabled; no merge or activation is claimed.

Completed preparation: role split, API-only generated password/session profile, reviewed units/log policy, protected provider access, eight caller transport settings, canonical six-permission successor grant, verified fresh pre/post authority backups, and candidate immutable artifact. No application changes require another full trusted suite. Current report: `/tmp/zola-astra-activation-current.md`.

## ZOLA production-live critical path (2026-09-04 UTC)

1. DONE — release implementation `df8c87c68868da1e980b8206ef65ee14a5c69047` is pushed to existing PR #125 with exact-head CI/Vercel success. Preserve the recorded trusted suite; no code changes require rerunning it. Hold merge because main automatically deploys production and recovery/configuration gates remain unmet.
2. NEXT — the Supabase organization owner resolves the confirmed Free-plan storage-quota restriction through subscription/billing controls. Recheck authenticated upstream Auth health and frontend `/api/auth/status` after that action; no application patch is justified for the HTTP 402 account restriction.
3. Supabase metadata access is available. Verify backup/recovery and the live n8n writer identity, then apply only the reviewed Nexus and Buyer browser-security migrations. An archived writer uses anonymous credentials and must use an authorized server identity before revocation. Verify denied browser access, retained own-job reads, and service-role capability reads after applying; do not blindly replay unrelated migration history.
4. Before VPS activation, obtain the operator-created `COMMAND_ADMIN_PASSWORD_HASH` in the protected API-only profile and healthy private Codex service login; install reviewed split API/worker identities and configuration, configure all four division adapter transports, and provision six minimum read grants through the append-only authority chain. A fresh writer-absent SQLite backup passed checksum, integrity, and schema verification at 23:46 UTC (`command-20260904T234621Z.sqlite`); refresh it before cutover if state changes. The retained `608b10fd233a5a2a94fd0ce4cc03d73894c5694d` currently fails and cannot be used as an unverified healthy rollback.
5. Once production target, configuration, migration/recovery, code/CI, and rollback gates pass, merge only with expected-head-SHA protection and record new main. Verify the exact Vercel production deployment, deploy that SHA to the VPS through immutable releases/Gate 4, and require generation-fenced health/readiness plus authenticated ZOLA/Hermes and six-capability smoke tests. Roll back immediately on a newly introduced P0. Telegram remains dry-run; no paid Nexus call or outreach is permitted.

## ZOLA critical path (2026-08-31 UTC)

1. NEXT — branch `fix/pr120-p2s-660dfb` contains the locally validated PR #120 candidate through implementation commit `09d9dfa39fb24cd3c683608080ede07e097ddcbf`, followed only by the canonical-memory checkpoint. All 16 review topics are fixed or superseded in the local tree; remote PR head `660dfb63444069c734dcff50415cb84c4fd919b6` lacks the three latest P2 corrections. Nothing is pushed. The single next action is operator authorization to push the final local candidate SHA, followed by fresh exact-head CI and independent remote review.
2. AFTER #120 — reconcile `feature/jarvis-capability-registry` onto post-auth `main`, preserving its bounded Seller disclosure/finalization fences.
3. AFTER #119 — reconcile draft `ui/jarvis-mobile-polish` onto the stable authentication UI and perform rendered mobile verification when approved tooling is available.

## Immediate ordered work (2026-08-30 UTC)

1. DONE — PR #115 merged with expected-head protection as `a78bb4011a8b263845288351d22c68a500b3772b` from exact reviewed head `545f425d801151966ad2ddc0d01bb6d24d249d85`; CI run `33301410453` (#497) passed and the exact-head Codex review was clean.
2. DONE — PR #117 merged as `979272fb46c5a6fc370a24144643166b0cf3a635` from exact reviewed head `13c864b7ee72b7a427a8579e5a725dc4f31fe872`. The canonical human-readable result's provider-attempt selection, authorization, sanitization, 8,000-character bound, generic-result fallback, and terminal-state exclusion are server-authoritative; Task, Evidence, and Conversation consume the same field.
3. DONE — focused validation passed 112/112 after the authorization-scoped batch-lookup correction; exact-head CI and final review passed before the expected-head merge.
4. NEXT — deploy through the existing Gate 4 release path when authorized and verify the historical task without replaying it, creating a provider attempt, or rewriting its evidence.

## Immediate PR #115 closure (2026-08-24 UTC)

1. Push the validated exact-head provider-isolation correction as one checkpoint.
2. Wait for exact-head CI, then request one focused Codex review of the changed head; do not duplicate review on an unchanged SHA.
3. Merge only after required checks and review are clean, using expected-head protection, then verify the reviewed tree landed on main.
4. Immediately inspect the persisted successful provider attempt for `task_61a88e75d88dbcd1` read-only and implement the canonical result-display contract without replaying the historical task.

## Post-live acceptance work (2026-08-24 UTC)

1. Run the rendered mobile PWA journey when an approved browser executable is available; the public HTML, manifest, service worker, viewport, and state/error copy have been inspected, but a visual touch-layout pass is not yet evidence-backed.
2. Exercise the remaining live destructive acceptance matrix with isolated tasks: cancel before/during dispatch and completion/failure races, stale undispatched recovery and old-claim rejection, dispatch-crash `outcome_unknown` with no replay, restart while halted, and rollback rehearsal. Preserve the completed real task and historical databases.
3. Run one controlled write test only in a disposable repository/worktree: Codex stays provider-side read-only and Hermes alone validates/applies the harmless documentation artifact.
4. Add operational monitoring/alerts for public readiness, worker heartbeat generation, task backlog, `outcome_unknown`, and unit restart rate. Resolve Vercel preview quota separately; the current public PWA is served by the canonical VPS runtime.
5. Keep Telegram dry-run/disconnected until canonical persisted identity mapping, authorization, durable offset/dedupe/context, and negative cross-workspace tests exist. Telegram is not part of the proven PWA live path.

The former deployment blockers below are retained as historical context; database selection, writer-stopped backups, operator grants, service-compatible authenticated `CODEX_HOME`, reviewed units, Gate 4 activation, nginx cutover, and a real read-only Jarvis-to-Codex completion are now done.

0. **BLOCKED — Telegram canonical authority.** Do not activate or directly merge historical PR #86. First bind each accepted Telegram identity to a persisted canonical principal, require active workspace grants before reads or writes, and persist polling offset, dedupe, selected workspace, and conversation context. Then recover only the still-useful bounded timeout/retry/ordering/chunking behavior.

0. **BLOCKED BEFORE DEPLOYMENT — reconcile observed VPS runtime.** Initial production will use a deliberately clean database created by the reviewed migration path at the configured canonical target; do not promote or merge either historical database. Take writer-stopped cutover backups of both preserved sources, provision the real persisted operator/grant in the new database, authenticate and configure the prepared private service `CODEX_HOME` without exposing credentials, record rollback evidence, install the now-merged reviewed three-unit topology, and switch nginx only after activation-generation readiness succeeds. See `docs/VPS_CURRENT_STATE_AUDIT_2026-08-24.md`.

## Current ZOLA delivery sequence (2026-08-24 UTC)

0. **IN PROGRESS — ZOLA canonical operator observability.** Reconciled this focused slice onto post-#105/#107/#109 main. Health/readiness, stale/stopped/current-generation worker, task-state, offline, and `outcome_unknown` no-replay fixtures are implemented. Preserve server authority for principal, workspace, provider, model, readiness, and task state; never offer automatic replay for `outcome_unknown`.

1. **DONE:** workspace authorization merged in PR #104 at `31a716961b174496e5d343a8e4586017204b4990` from reviewed head `5e877a8e6f7ff2af11c61e76bb1234b8623e50b7`.
2. Provision and verify the configured production operator principal and active workspace grants as a separately controlled database operation before activation; configuration never substitutes for a persisted grant.
3. Complete full validation and exact-head review of `fix/api-worker-service-topology` candidate `7c475b63c6ca2c2b456bfc3fbd3a8727c34a6c39`, whose bounded activation waiter now fences readiness to current API/worker systemd invocations; merge only after CI/Vercel and stale-heartbeat adversarial review pass. Then complete PWA observability, Telegram canonical intake, and release integrity before rollback-safe deployment.
4. Only after those prerequisites, prepare private `CODEX_HOME`, deploy exact reviewed main, and run one bounded read-only task through the real ZOLA pipeline.

0. **IN REVIEW 2026-08-17 — PR #103 production Hermes execution final correction.** Complete full validation, push the corrected exact head, obtain fresh Codex review, and merge only after the original and fresh P1 classes are proven closed: metered OpenAI/Anthropic API dispatch cannot bypass the monetary ceiling because those production providers are refused until conservative accounting exists; every production preflight-accepted provider mode maps to an implemented executor, with Codex production limited to authenticated CLI and direct-api refused; canonical memory records the milestone without claiming live deployment; Codex CLI output is parsed as real JSONL plus terminal final output; the server-selected model reaches the actual CLI; Codex executes in the selected workspace under read-only sandboxing; runtime dispatch enforces `BLACKSPIRE_PRODUCTION_EXECUTION=enabled`; every Codex probe/execution child environment is sanitized; production `CODEX_HOME` is required outside protected home; Hermes packet/final-output control files stay outside the selected checkout; Codex child execution is bounded by the Hermes deadline, cancellation, emergency stop, stdout/stderr limits, and final-output file size checks; subscription Codex CLI execution is not retried; Claude Code production execution is disabled; and subscription accounting is durable with NULL monetary cost. After merge, continue to workspace authorization remediation, worker service topology, ZOLA PWA, Telegram, release-integrity recovery, deployment, and one real Codex CLI smoke through ZOLA's persisted task pipeline.

0. **IN REVIEW 2026-08-11 — PWA deployment identity freshness.** Complete independent review of the exact PR head after the focused fix that re-renders freshness at its deadline even while a health fetch is unresolved. Preserve server-authoritative identity validation and network-only health/API caching.

0. **IN REVIEW — reduce Vercel preview churn without weakening final validation.** Merge `ci/vercel-preview-churn-control` only after independent exact-head review and green required checks. Once merged and the rolling Hobby deployment window has capacity, retry only stable affected heads. Backend/docs-only descendants should then receive an Ignored Build Step success from the frontend project; frontend-changing heads must still build. Because ignored builds still consume deployment records, batch intermediate fixes locally and push once only after focused validation. Do not fabricate checks, disable production deployment behavior, or upgrade/purchase capacity.
0. **IN REVIEW 2026-08-04 — M3A evaluation reads authorize before provenance validation.** Node 22.23.1 validation passed focused 21/21 and the clean full rerun at 699 total, 690 passed, 0 failed, 9 host-conditional skips. The read-ordering invariant is carried by an access-recording behavioural guard rather than by source-text pins. Round 6 replaced a stale ten-name sentinel list with runtime schema discovery. Round 8 closes the remaining blanket authorization-table exclusion: the guard records per-relation row-read counts, derives the legitimate authorization trace from `canReadEvaluation`, and requires the full denied read to match that trace exactly. A reproduced pre-denial `recordedActorValid` call now fails on an additional auth-graph read. The evaluation identity lookup and authorization audit write remain the only documented instrumentation exclusions. Setup and restoration track every altered table, attempt every cleanup after partial failure, and report restoration failures without hiding a primary test failure. Fresh exact-head independent review is required before merge. This closes carried follow-up (v) (the M3A read-ordering inconsistency) and follow-up (i) (the stale 3C second-slice status heading). **New follow-up (viii):** the authorization step itself still costs more for an evaluation that exists than for one that does not, leaving a residual existence signal - measured across independent reviewers at ~45x to ~77x on this host under differing load, against pre-fix baselines of ~119x to ~302x measured the same way, the ratio being host-, load-, and fixture-dependent rather than a constant - the pre-fix cost grows with the evaluation's correction count - that is architecturally identical to the one already accepted for M3C and leaks no workspace identity. It is documented, not closed. The workspace-scoped M3C review queue and cross-cutting schema-constraint validation remain separate next milestones.
0. **IN REVIEW 2026-08-04 — bounded M3C workspace review queue.** GET-only, evaluation-admin-bound, authorization-first, keyset-paginated, and fail-closed over existing candidates and effective review heads. Focused validation passes 64 total, 63 passed, 0 failed, 1 root-only skip; the full suite passes 713 total, 704 passed, 0 failed, 9 host-conditional skips with trusted inventory 49/49. Complete independent review before merge. Promotion remains a separate operator/architecture decision and must not inherit `evaluation.correct` automatically.

0. **IN REVIEW 2026-08-04 — exact CHECK-integrity validation for authorization and Hermes 3A-3C.** The shared gate now pins 52 expressions and refuses removed/widened constraints during startup, restore, and migration post-validation. Node 22.23.1 validation passes focused 30 with 1 root-only permission skip and full 704 total, 695 passed, 0 failed, 9 host-conditional skips. Complete independent review before merge; NOT NULL/primary-key coverage remains separate hardening.
0. **IN REVIEW 2026-08-11 — latest-backup verification hardening.** Complete fresh exact-head independent review after the verifier was bound to one stable standalone snapshot inode and filesystem failures were sanitized. Do not install or schedule the verifier, invent an RPO, restore production data, or update the verified-implementation anchor from this branch.

0. **DONE 2026-08-04 — PR #64 merged as `0d6e977113e826095c4ed57db150bf30d9de954f` from reviewed head `055cd5bd919b2ddc2f94113e07134bb8b72829cc`, the new verified implementation anchor.** Merged under an expected-head guard (`--match-head-commit`) after two fresh independent exact-head reviewers (I and J) both returned **unconditional `approve`** on that exact SHA — J with zero findings of any severity — and both independently reproduced focused 59/59, full suite 698 · 689 · 0 · 9, mutation **22 killed / 3 declared equivalent / 0 surviving / 0 misdeclared**, lint, typecheck, build, secret scan, `npm audit --audit-level=high` at 0 vulnerabilities, `git diff --check`, `bash -n` over 39 tracked scripts, and preflight `ok=true source=21/21 deployment=2/2` with no deployment. Eight correction passes preceded them; in the seventh and eighth alone, four defects in harness-behaviour claims and declared-equivalent-rationale text were caught by independent review rather than self-check — two unverified rationales retracted in place, one non-existent identifier corrected, and one omitted mutant added to the committed inventory — with earlier passes recording further documentation and evidence defects of the same kind, so that four is a scoped subcount rather than a total. Milestone 3C's second slice (re-review and successor chains) is review-only and remains non-operative. **The paid end-to-end smoke test (`scripts/hermes-dev-smoke.js`) was NOT RUN** — it makes a live call and no authorization was given, so no claim is made about it; the end-to-end local smoke test in `tests/smoke.test.js` runs inside the full suite above and passed. **Open follow-ups carried out of 3C, none closed by PR #64:** (i) `docs/HERMES_IMPLEMENTATION_STATUS.md` now heads its 3C **second-slice** section "draft PR, in progress" — it is outside the canonical-memory allowlist, so correcting it in this anchor refresh would invalidate the anchor being recorded; the next milestone's PR must correct it, as PR #64 did for the first-slice heading; (ii) CHECK constraints are still not covered by startup/restore schema validation, which compares column names only — PR #64 registered the new `hermes_memory_candidate_rereviews` table's columns, its three unique indexes, and its two immutability triggers, but not its `decision` CHECK, so the gap now spans the 3A, 3B, and both 3C tables and should be closed for all of them together rather than piecemeal; (iii) the 2000-character rationale bound is still checked pre-redaction while `redactString` can lengthen the stored value, so the stated bound is not the bound that holds on stored data; (iv) `getPendingMemoryCandidatesForWorkspace` still has no production caller, and `recordMemoryCandidateRereview` likewise ships exercised only by tests — both are intended slice boundaries, not defects; (v) Milestone 3A's `readOutcomeEvaluation` still validates before authorizing, leaving that one read path inconsistent with the ordering corrected for 3B and both 3C slices; (vi) `tests/trust-boundary-regression.test.js` remains an intermittent timing flake unrelated to 3C. **New follow-up from this slice:** (vii) `reviewer_principal_id` sits outside digest coverage, so an attacker with **direct database write access** can re-attribute a stored re-review while it still reads as intact — an accepted, documented limitation pinned by a committed regression proven non-vacuous; a service-only caller cannot influence attribution. If that regression ever fails, the gap was closed and the threat model should be updated rather than the test.
0. **DONE 2026-07-31 — PR #62 merged as `bf9072c5ebc1f195020e7c1709610c741cf8be43` from reviewed head `1b589807af5d8179ad5b63e13648645dc2741d57`, the new verified implementation anchor.** Full local validation, GitHub CI, all five Vercel/CI checks, clean mergeability, zero review threads, and two exact-head independent reviews were green. The first round found one Medium — a cross-workspace candidate-existence oracle on the record path — which was independently reproduced, fixed at `1b58980`, and pinned by a mutation-tested regression; the second round at the fixed head returned zero Critical/High/Medium and recommended merge. Milestone 3C memory-candidate reviews are review-only and remain non-operative. **Open follow-ups carried out of 3C:** (i) `docs/HERMES_IMPLEMENTATION_STATUS.md` still heads its 3C section "draft PR, in progress" — it is outside the canonical-memory allowlist, so the next milestone's PR must correct it, as PR #62 did for the 3B heading; (ii) the `decision` CHECK constraint is not covered by startup/restore schema validation, which compares column names only, so a database restored from a snapshot lacking it would not be upgraded — this is a pre-existing pattern shared with the 3A/3B tables and should be closed for all three together rather than piecemeal; (iii) the 2000-character rationale bound is checked pre-redaction while `redactString` can lengthen the stored value, so the stated bound is not the bound that holds on stored data; (iv) `getPendingMemoryCandidatesForWorkspace` has no production caller yet; (v) Milestone 3A's `readOutcomeEvaluation` still validates before authorizing, the ordering now corrected for both 3B and 3C, leaving that one read path inconsistent; (vi) `tests/trust-boundary-regression.test.js` remains an intermittent timing flake — it failed once during this session's full suite and passed standalone and on every rerun, and it is unrelated to 3C.
0. **DONE 2026-07-31 — PR #60 merged as `9dccdd95f5119195b192ee1f553b55d0eae3e48f` from reviewed head `fd3ab87e4a8363699a11775301e50fb67d56fd44`, the new verified implementation anchor.** Full local validation, GitHub CI, all Vercel checks, clean mergeability, zero review threads, and two fresh exact-head independent final reviews (both MERGE, zero Critical/High/Medium) were green. Phase 3B verified scorecards are a read model only and remain non-operative. The source branch was deleted under post-merge policy and remains recoverable as the merge's second parent.
0. **DONE 2026-07-30 — PR #57 merged as `763e74e6332b51fdacd30b29d9f836d5de6c2001` from reviewed head `80726cd3e034afc3e72921de9e426bd28dbe717e`.** Full local validation, GitHub CI, all Vercel checks, mergeability, resolved threads, and two fresh independent final reviews were green. Phase 3A remains factual and non-operative.

## Immediate safe actions

0. **Hermes Milestone 2 (Runtime & Provider Framework) is MERGED** via [PR #56](https://github.com/houseomegakennels-bit/blackspire-helix-group/pull/56) (reviewed implementation head `148a82cde1105a8917be2e38018c6e8ff4a7e1d4`; merge `a4a0407e5d72019b4a99cf61568598434e7e136d`). It adds a development-only real-provider runtime (first adapter: non-agentic Anthropic Messages API), disabled by default and refused in production. Real dispatch requires the development profile, explicit feature flag, provider allowlist, registered/allowlisted workspace root, bounded positive spend reservation, and a valid scoped approval. **Only with separate explicit operator approval**, the prepared-but-disabled `scripts/hermes-dev-smoke.js` may make one bounded development call; it was not run for M2. **Deferred to Milestone 3:** multi-provider learned routing, scorecards, approval-gated memory promotion, cross-host limits. Do not enable multi-provider autonomous routing or automatic memory promotion. Production stays `disabled/inactive/dead` and Gate 4 stays unauthorized regardless of Hermes milestones.
0a. **MERGED — Hermes Milestone 3B is complete on [PR #60](https://github.com/houseomegakennels-bit/blackspire-helix-group/pull/60)** (merge `9dccdd95f5119195b192ee1f553b55d0eae3e48f`, reviewed head `fd3ab87e4a8363699a11775301e50fb67d56fd44`). Deterministic aggregation, minimum-evidence thresholds, confidence/unknown handling, workspace isolation, replay/idempotency, append-only lineage, and zero routing or memory side effects are implemented and pinned by 23 focused tests. Six review rounds of two fresh independent read-only reviewers each returned zero Critical findings overall; the final two rounds returned zero Critical, High, and Medium and both reviewers recommended merge in each. Every confirmed finding is fixed and regression-pinned. **Open follow-ups carried out of 3B:** (i) `docs/HERMES_IMPLEMENTATION_STATUS.md` still heads its 3B section "PR #60, open draft" - it is outside the canonical-memory allowlist, so the next milestone's PR must correct it, as PR #60 did for the 3A heading; (ii) Phase 3A's `readOutcomeEvaluation` still validates before authorizing, the ordering corrected for 3B, leaving the two read paths inconsistent; (iii) derivation is unbounded and must be bounded, and the corrected-source workspace-wide permanent refusal must be resolved with a lower cutoff bound or an excluded-source manifest, **before any derivation API is exposed**. Original scope statement: build non-operative verified scorecards and learning-quality evidence from intact Phase 3A records. Acceptance must prove deterministic aggregation, minimum evidence thresholds, confidence/unknown handling, workspace isolation, replay/idempotency, append-only lineage, and zero routing or memory side effects. Approval-gated memory candidate review/promotion follows only after 3B; provider/agent registry, verified identity, operator PWA, Telegram, learned routing with rollback, and production/Gate 4 remain dependency-ordered later tracks.
0b. **Hermes Milestone 1 is MERGED (PR #55, anchor `a419c3e`, merge `4e1ec49`)** — mock-only foundation.
1. PR #35 is MERGED into `origin/main` (superseded as trusted `origin/main`, see item 2): absolute Node interpreter pinning across production startup, systemd unit Environment=BLACKSPIRE_NODE_BIN, host-interpreter verification, and strict production preflight.
2. PR #36 is MERGED into `origin/main` (`6f40a4443f498e77a8a2ee1e17be6da4a4837e67`, parents base `693fb03e4596d26e990f87a40508307810cc5e5d` and reviewed head `bb727976c47eb430af1587c3f361864aaa7d15f4`): the fail-closed living-memory ancestry containment checker with adversarial test coverage. Independent exact-head review returned zero findings; GitHub CI and Vercel checks passed. PR #37 then attempted a documentation refresh to record the new trusted `origin/main`, but recorded it as an exact-equality SHA, which made that refresh commit invalidate its own recorded value the instant it merged (a self-invalidating loop, not a regression). PR #38 (`fix/living-memory-docs-descendant-policy`, reviewed head `bc5a817231c1a325f31a112e256425140bbbf073`, merged as `ce6dcecf5335233656392248a67326a2524521ea`) fixed the contract itself: the recorded value is now a verified implementation anchor checked for ancestry plus a fail-closed canonical-memory-only descendant policy (`scripts/check-living-memory.sh` computes the aggregate tree diff from the anchor to live trusted `origin/main` and requires every changed path to be in the canonical-memory Markdown allowlist), so future documentation-only refreshes no longer invalidate themselves. Independent exact-head review of PR #38 returned 0 Critical, 0 High, 0 Medium, and 0 Low findings; GitHub CI and Vercel checks passed. Because PR #38 changed non-documentation files (`scripts/check-living-memory.sh`, `tests/living-memory-ancestry.test.js`), the immediate post-merge checker run correctly returned `UNREVIEWED_NON_DOCUMENTATION_CHANGE`, requiring this docs-only refresh to record the new reviewed anchor (`bc5a817231c1a325f31a112e256425140bbbf073`) against the PR #38 merge snapshot (`ce6dcecf5335233656392248a67326a2524521ea`). No release was built, no PR was marked ready or merged beyond PR #38 itself, and no production action occurred as part of this action.
3. DONE 2026-07-22 — **PR #29 is MERGED**, not draft. It merged into `origin/main` as `f0a7b66853b4c983d6930c1b70d1d94cb5f26f36` (parents: base `588ea6e1156d7d39e7f8a5a7bde44f8c4e519807`, reviewed head `819d0fc57c6157ecd2bc70dde51216d146d9fc91`) at 2026-07-22T22:09:46Z; both commits are contained in current trusted `origin/main`. This item previously read "Keep it draft; do not mark ready or merge until that review passes," which was already false when written into the record and stayed false until 2026-07-27. No PR #29 action remains.
4. Preserve the verified root-Vercel boundary: the VPS-owned root project must be ignored on every ref while the `frontend` project remains independently deployable. PR #29 and PR #30 exact-head root contexts pass via repository configuration; no external Vercel setting needs mutation.
5. DONE — both preconditions are long satisfied and the rehearsal has since been run twice. PR #29 merged 2026-07-22 (`f0a7b66…`) and staging was rebuilt onto `releases/f0a7b66853b4c983d6930c1b70d1d94cb5f26f36`; a first disposable-only Gate 3 rehearsal ran that same day, with sanitized evidence retained on the host at `shared/evidence/gate3-f0a7b66853b4c983d6930c1b70d1d94cb5f26f36` (its result was recorded only in the unmerged PR #31, so canonical memory never carried it). The authoritative Gate 3 result is the 2026-07-27 rehearsal at trusted `origin/main` `73856891c5f6f35f14c09d1ce5acef39f20c4b25`, recorded in item 21.
6. Gate 3 passed with sanitized evidence on 2026-07-27 (see item 21), which satisfies this precondition and nothing more. Gate 4 production activation is still **not** authorized and must not be requested or attempted until the six host-side blockers and the installed-unit mismatch are closed and a separate bounded production approval is given.
7. Preserve the four original commits and backup branch; do not rewrite them or the integration merge.
8. Keep all validation credential-free and mock-only until explicit authority changes.
9. Preserve the completed operator iPhone acceptance and teardown evidence; restage only for an explicitly approved new acceptance need.
10. When included Codespaces usage renews, inspect the designated existing Codespace before any creation request; do not enable billing.
11. Before production Command promotion, apply and verify the immutable release, WAL-safe backup/restore, no-provider profile, monitoring/log-retention, stable HTTPS, and rollback procedures documented in `docs/VPS_RUNTIME_RUNBOOK.md`; current VPS application remains unpromoted.
12. Preserve the completed restricted subscription Codex acceptance. Any new live Codex task requires a separately scoped approval; do not switch to API-key billing or another provider.
13. PR #26 readiness tooling is merged into `main` (`a9602496`). Do not deploy or activate any release until the six host-side blockers are closed and a separate bounded production approval is given. The prepared `ops/` proxy/TLS and runtime-ownership plans are review artifacts only — do not install or apply them.
14. Obtain a fresh independent read-only review of the draft production loopback-bind PR at its exact remote head before any merge: it must confirm `BIND_HOST` reaches `server.listen`, that public/wildcard production binds and implicit port fallback are rejected, that ports 8787 and 8788 stay protected, and that no running service changed. Keep it draft; do not mark ready or merge until that review passes.
15. Separate pre-existing follow-up (not part of PR #32): `tests/trust-boundary-regression.test.js` is intermittently flaky at the pathname-replacement case, lines 37-40 of the test `immutable test identity detects content mutation and pathname replacement`. The file is unlinked and rewritten with byte-identical content, and the test asserts `verifyTestTreeUnchanged` throws. `descriptorIdentity` in `scripts/test-inventory.js` records `{dev, ino, mode, nlink, size, mtimeMs, ctimeMs, sha256}`, so when the filesystem reuses the freed inode and the rewrite lands within the same coarse timestamp tick, every recorded field is identical and no replacement is detected. The independent reviewer reproduced this against `origin/main`; 25 isolated repetitions on the correction host did not reproduce it, so it is load- and filesystem-dependent, not deterministic. The relevant test and implementation are byte-identical to `origin/main`, so this is pre-existing and unrelated to the bind/port work. Recommended repair: give each capture an explicit generation marker independent of inode and timestamp reuse, or force a timestamp advance past filesystem granularity before the rewrite. Do not fold this into PR #32 and do not claim it repaired. **TRACKED INDEPENDENTLY AS ISSUE #52 on 2026-07-28** (`Flaky trust-boundary regression test caused by unlink/recreate timing and inode reuse`), which supersedes this item as the working record. Measured there, 12 isolated runs per checkout: fresh `main` `96d804d` 3/12, fresh PR #50 `3452b33` 5/12, fresh PR #51 `7a34aec` 5/12, long-established PR #50 worktree 0/12, long-established PR #51 worktree 1/12 — indistinguishable between `main` and both branches. The mechanism was measured directly rather than inferred: on this host's `/tmp` the inode is reused 400/400 times after unlink+recreate, so detection collapses onto sub-millisecond timestamp granularity alone. The test and `scripts/test-inventory.js`, `scripts/trusted-test-runner.js`, and `scripts/test-process-supervisor.js` are byte-identical across `main`, PR #50, and PR #51 (blobs `31ac662`, `a4d7f5c`, `ba7ef2a`, `7a9814f`), so branch identity is not supported as the cause; fresh-versus-established checkout state is a timing correlate, not a proven cause. The defect predates both PRs, neither introduced it, and it blocked neither merge.
16. Do not activate production on the selected port `8789` until that review passes, Gate 3 passes, and a separate bounded production approval is given. `BIND_HOST=127.0.0.1` and an explicit `PORT` must be present in the operator-supplied environment file; there is no default.
17. A staging/nginx outage (root-caused to a 2026-07-24 10:03 UTC mass service-kill) recovered 2026-07-26; both services are healthy again. The nginx restart-policy and staging clean-exit resilience gap this outage exposed is a separate future reliability task requiring its own scoped work; it is not part of PR #36 and is not authorized to start here. Issue #33 remains separate.
18. PR #39 (docs-only implementation-anchor refresh after PR #38) is MERGED into `origin/main` as `1a28343f7d3ea3c2968799bc57fb13555fc73787`. The immutable release was then built and privately validated at that exact SHA (see item 19); do not repeat that rebuild/validation step.
19. The immutable release at `/opt/blackspire-command/releases/1a28343f7d3ea3c2968799bc57fb13555fc73787` passed source validation, the release manifest, ownership/permissions, and the release-tree preflight, and passed a bounded private blue-green boot on loopback `127.0.0.1:8789` with disposable SQLite state, providers disabled, Hermes mock/bounded, and Telegram dry-run/disconnected; teardown removed all temporary state and preserved the release and prior rollbacks. Staging, nginx, port 8787, and production were unchanged. This makes Gate 3 requestable; it does not authorize Gate 3, Gate 4, replacing the installed production systemd unit (which still differs from the reviewed template), or any production `current` symlink/environment/database/activation. The next safe action is to request Gate 3 data-protection, WAL, and backup/restore rehearsal under separate explicit authority.
20. PR #41 (`fix/restore-reject-empty-invalid-backup`) is MERGED into `origin/main` as `d800d283b11b03d88c519e18abbed0870d9fa750`; its reviewed head `40e6d6a873e1579b91004736ba55e2ab87f2e462` is the new verified implementation anchor recorded by this refresh. Gate 3 Finding #1 is closed on both the restore and backup sides. Because PR #41 changed source and test files, the immediate post-merge checker correctly returned `UNREVIEWED_NON_DOCUMENTATION_CHANGE` until this docs-only refresh recorded the new anchor. Do not build an immutable release from a state where this checker fails.
21. **Gate 3 data-protection rehearsal PASSED on 2026-07-27** at trusted `origin/main` `73856891c5f6f35f14c09d1ce5acef39f20c4b25`, disposable-only, against the immutable release built at that exact SHA (36 checks, 0 failures). Do not repeat it for the same SHA. Gate 4 production activation is still **not** authorized: it requires separate explicit approval and remains blocked by the six host-side blockers and the installed-unit mismatch. The next safe action is to close host-side blockers under separate explicit authority, not to activate anything.
22. DONE — `docs/VPS_RUNTIME_RUNBOOK.md` now documents the enforced backup/restore contract, merged as PR #44 (`a78b6395c6176b1dc02d97b7d70807938a3d1beb`, reviewed head `5fa024ec02a7c8677172dd54d9d60579b561da1d`, the new verified implementation anchor recorded by this refresh). As predicted, that file is outside the canonical-memory allowlist, so it required its own reviewed change and this follow-on anchor refresh rather than riding along in a docs-only commit. The runbook no longer understates the contract.
23. DONE — configurable Hermes workspace root shipped as PR #47 (`e55de773fe3e10e7b1352fd8b2b2b10b40063e87`, reviewed head `d11ac82f62e1bf44e716af7d58b6cd3d77115c30`, the new verified implementation anchor). `BLACKSPIRE_WORKSPACE_ROOT` is server-side only, defaults to the historical `.`, and fails closed when set to an empty, relative, missing, non-directory, symlinked, non-git, symlinked-`.git`, or NUL-byte value. This closes the only unique work in PR #22, implemented fresh against main; PR #22's compose/port/container/bind-mount changes were deliberately excluded and PR #22 is closed as superseded. Follow-up, not started and not authorized here: decide whether `scripts/verify-environment.sh vps-production` should *require* `BLACKSPIRE_WORKSPACE_ROOT` rather than allow the `.` default, and whether the resolver should additionally refuse a root inside `releases/`. Both were left out as beyond the minimal safe change — an unset root fails at the kernel rather than corrupting anything, since the release is read-only — and either would tighten the production contract, so both need their own scoped review.
24. DONE 2026-07-27 — the installed production systemd unit is repaired by a host-only correction (no code change). `/etc/systemd/system/blackspire-command.service` is now byte-identical to the reviewed `ops/runtime-ownership/blackspire-command.service` (`root:root` `0644`, SHA-256 `4c3f3387e99086f94f2aca4e1755d12b34b6422c6618add1c9ff5b09aa95866b`), `daemon-reload` was run, and the service remains `disabled`/`inactive`/`dead` — it was not started, restarted, enabled, or activated. `npm run production:preflight` and `npm run production:preflight:host` both report `ok=true source=21/21 deployment=2/2`. This closes the installed-unit blocker; it does **not** authorize Gate 4 or any activation. The prior unit would have failed every start (`/usr/bin/env node` → Node 18 without `node:sqlite`), so re-check this after any future host Node change or unit edit.
25. **PREFLIGHT HALF CLOSED 2026-07-28 by PR #50 (merge `827c50015f8d3af3f6bb6c2c9f581097edf6b839`); the supervisor/`config.js` coupling below remains OPEN.** Original text, retained for the record: open follow-up from the unit repair, needing its own scoped review because it changes the production preflight contract: `scripts/verify-environment.sh` does not validate `BLACKSPIRE_WORKSPACE_ROOT`, and `scripts/production-supervisor.js` does not import `packages/shared/config.js`. An invalid workspace root therefore passes `ExecStartPre` and fails only when the API/worker children load config, producing a `Restart=on-failure` loop up to `StartLimitBurst` instead of a clean preflight refusal. Related to the separately tracked coupling in which `scripts/backup.js` and `scripts/restore.js` import `config.js` and so also refuse to run under an invalid workspace root.
26. Track startup-wrapper signal forwarding (the private validation runner did not forward termination signals to its API/worker children, requiring teardown to terminate them directly) as a separate future tooling-hardening item before the private runner is treated as production-grade.
27. **DONE 2026-07-28 — PR #50 (`fix/verify-workspace-root-preflight`, reviewed head `3452b33278ed250f00fc4a173d32a66e530fac9c`) is MERGED as `827c50015f8d3af3f6bb6c2c9f581097edf6b839` and closes the preflight half of item 25.** This item previously read "IN REVIEW … OPEN, not merged"; that was accurate when written and is superseded by the merge and by this anchor refresh. `scripts/verify-environment.sh vps-production` fails closed on a missing, empty, whitespace-only, relative, nonexistent, non-directory, symlinked, unreadable, untraversable, unwritable, non-git, or application-file-missing `BLACKSPIRE_WORKSPACE_ROOT`. The rules are deliberately stricter than the runtime resolver's and never looser, and the asymmetry is pinned by a test. `scripts/production-profile.env.example` now documents `BLACKSPIRE_WORKSPACE_ROOT=/opt/blackspire-command/shared/workspace` (the only tree `ReadWritePaths` makes writable) and `profile-required-keys` enforces it. Validation at that head: 463 tests, 454 passed, 0 failed, 9 host-conditional skips; preflight `ok=true source=21/21 deployment=2/2`. Do not record item 25 as closed, and do not build an immutable release assuming it is, until this merges and its anchor refresh lands. The supervisor/`config.js` coupling in item 25 is unchanged and stays open regardless.
28. **Gate 4 preparation tooling exists; Gate 4 itself remains unauthorized.** `scripts/gate4-prepare.sh` (read-only, no mutating mode, idempotent, fail-closed) reports which Gate 4 prerequisites are satisfied and which operator inputs are outstanding, and `--plan` prints the exact operator command sequence with a hard authorization boundary before any activation command. `docs/GATE4_ACTIVATION_CHECKLIST.md` is the operator checklist. On this host the current outstanding items are: the approved SHA (operator input), `/etc/blackspire/command.env` (absent), the seeded workspace at `shared/workspace` (absent), the approved release and `current` symlink, a production backup snapshot, log rotation, and three operator attestations (reverse proxy/TLS, monitoring alert testing, backup/migration rehearsal). Preparing is not authorizing: no automation may run the activation block, and Gate 4 still requires a separate explicit bounded operator approval. **PR #51 is MERGED as `437e27e0f2c5ac1e79cd0b93ea44c23790d06486` (reviewed head `7a34aecb9f34cb69af41728f0281045fabf4ff64`, the new verified implementation anchor); post-merge revalidation reproduced 23/23 focused and both preflights `ok=true source=21/21 deployment=2/2`, and the live checker still reports `ready=false`.**
29. **Next required step before any Gate 4 preparation: none in the repository — the next action is an operator decision.** With this refresh merged, `scripts/check-living-memory.sh` returns to `PASS` and the repository is clean at the anchor, so an immutable release may be built from trusted `origin/main` without stale canonical memory. To advance Gate 4, the operator must first name the approved SHA (`BLACKSPIRE_GATE4_APPROVED_SHA`) and then, each as a separately authorized action, create `/etc/blackspire/command.env` from `scripts/production-profile.env.example`, seed the workspace at `/opt/blackspire-command/shared/workspace` as a real checkout, verify the approved release, create `current`, take a production backup with `scripts/backup.js`, install log rotation, and record the three MANUAL attestations. Run `PATH=/opt/nodejs/node-v22.23.1-linux-x64/bin:$PATH bash scripts/gate4-prepare.sh --check` first; it is read-only and changes nothing. None of these may be performed by automation, and none of them authorizes Gate 4, which remains a separate explicit bounded operator approval.

## Operator-only actions

Push/merge authorization, budgets/spend, credentials, GitHub authorization, device acceptance, production and DNS changes, real Telegram, paid/live providers, host security, emergency controls, constitutional changes, trading, and funds movement.

## Blocked or deferred

- New Codespace creation: blocked by usage credit.
- Real Telegram and live providers: not authorized.
- Production Command launch: readiness incomplete and live state `UNVERIFIED`.
- PR #26: repository-side readiness tooling is merged into `main`, but host-side production approval remains blocked by six items, each separately authorized: (1) reverse proxy/TLS install and verification, (2) least-privileged non-root runtime ownership provisioning, (3) installed monitoring/log rotation with alert-delivery testing, (4) approved production backup/migration rehearsal or execution, (5) recorded exact known-good live release/database rollback target, and (6) readiness tooling in the deployment artifact.
- Multi-instance/serverless Command persistence: requires architecture work beyond SQLite.
