# Blackspire Active Context

Last reconciled: 2026-07-28 UTC. **PR #55 (Hermes Intelligence Layer — Milestone 1) is MERGED into `origin/main` as merge `4e1ec4936e1c9bbc9eeb7d113837bb84194271ed`, and the verified implementation anchor is now its reviewed head `a419c3ee20ac27393c6bc19ac4390a5f1d47e4a2`.** Milestone 1 is an additive, **mock-only** orchestration + learning foundation (`packages/hermes-orchestrator/`) plus six additive `hermes_*` tables; the existing `packages/hermes/hermes.js` pipeline is unchanged. **Execution is mock-only** (real providers registry-disabled and refused by the executor; the production profile disables provider execution), **no real provider is active, no provider credentials exist**, and there is **no automatic memory promotion** (memory candidates are always `status='pending'`; promotion is a later approval-gated milestone). Three review threads (bearer/AWS/Authorization/PEM redaction; canonical-policy enforcement so prior denials and protected actions are never re-allowed; real budget enforcement) were resolved before merge. Final validation from merged main: Hermes 19/19; migration-safety + unified-input + unified-policy 23/23; lint/typecheck/build/secret-scan clean; `production:preflight` `ok=true source=21/21 deployment=2/2`. **Milestone 2 has NOT begun and no new feature branch exists.** Separately, from this session's earlier approved Gate 4 *preparation* (host-only, uncommitted), `/etc/blackspire/command.env` (`root:blackspire 0640`), the seeded workspace `/opt/blackspire-command/shared/workspace`, and the approved immutable release `/opt/blackspire-command/releases/85753ffa8c75daf220d62bfa88a5095c22173d8b` now exist on the host; the `current` symlink is still absent, no production backup or log rotation is installed, and **Gate 4 remains unauthorized with production `disabled`/`inactive`/`dead`, `MainPID=0`; nginx still serves the intentional HTTPS 503 maintenance page. No production, nginx, TLS, DNS, firewall, or systemd change was made in the Hermes work.**

Prior baseline (superseded by the anchor above): **PR #50 and PR #51 are both MERGED, and the previous verified implementation anchor was PR #51's reviewed head `7a34aecb9f34cb69af41728f0281045fabf4ff64` (merge `437e27e0f2c5ac1e79cd0b93ea44c23790d06486`).** PR #50 (`827c50015f8d3af3f6bb6c2c9f581097edf6b839`) closes the preflight half of next-action item 25 by making `scripts/verify-environment.sh vps-production` fail closed on every invalid `BLACKSPIRE_WORKSPACE_ROOT`, and adds the key to the reviewed profile and `profile-required-keys`. PR #51 (`437e27e0f2c5ac1e79cd0b93ea44c23790d06486`) adds the read-only `scripts/gate4-prepare.sh` preparation checker, `docs/GATE4_ACTIVATION_CHECKLIST.md`, and 23 contract tests. Because PR #51 was stacked on PR #50 and merged after it, the merged `origin/main` tree is byte-identical to the tree at the anchor, so the single anchor covers both. Both source branches were preserved. Post-merge revalidation at the merge commit: focused Gate 4 suite 23/23, both preflights `ok=true source=21/21 deployment=2/2`, and lint, typecheck, build, secret scan, `git diff --check`, and `bash -n` over all 39 tracked shell scripts clean; this refresh clears the expected post-merge `UNREVIEWED_NON_DOCUMENTATION_CHANGE`. The live checker reports `ready=false` with the approved SHA, environment file, workspace, approved release, `current` symlink, production backup, log rotation, three MANUAL operator attestations, and Gate 4 authorization all outstanding. **Gate 4 remains unauthorized and production remains `disabled`/`inactive`/`dead`; `/etc/blackspire/command.env`, `shared/workspace`, and `current` are all still absent, and all seven releases are preserved.** The pre-existing `tests/trust-boundary-regression.test.js` flake is now tracked independently as **issue #52** and blocks neither merged PR.

**The installed production systemd unit is repaired** (host-only, no code change). The previously installed `/etc/systemd/system/blackspire-command.service` was a 2026-07-21 template copy that declared `ExecStart=/usr/bin/env node`; because systemd's manager PATH excludes `/opt/nodejs`, that resolved `/usr/bin/node` — v18.19.1 on this host, without `node:sqlite` — so every production start would have failed and exhausted `StartLimitBurst`. The reviewed template was installed verbatim (exact-equality is what the preflight compares), `root:root` `0644`, byte-identical to `ops/runtime-ownership/blackspire-command.service`, with the prior file backed up outside `/etc`. `daemon-reload` was run; the service was **not** started, restarted, enabled, or activated and remains `disabled`/`inactive`/`dead`. systemd's parsed view confirms `ExecStart` uses the absolute reviewed Node 22.23.1 interpreter, the pinned `PATH` and `BLACKSPIRE_NODE_BIN` follow `EnvironmentFile` so reviewed values outrank the operator file, and the loopback-only bind contract fails closed on wildcard, non-loopback, absent `BIND_HOST`, reserved 8787/8788, absent, privileged, and malformed ports, and a wrong state owner. `npm run production:preflight` and its `--strict` host variant now both report `ok=true source=21/21 deployment=2/2`, closing the long-standing installed-unit blocker. nginx, TLS, DNS, firewall, production database, production `current` symlink, Telegram, providers, credentials, staging, and releases were untouched; staging PID `2909886` and nginx PID `2909804` kept identical PIDs and start timestamps, staging health stayed 200, and all seven releases were preserved. Residual, tracked, not repaired in that unit-reinstall scope: `verify-environment.sh` did not validate `BLACKSPIRE_WORKSPACE_ROOT` and the supervisor does not import `config.js`, so an invalid workspace root passed `ExecStartPre` and failed later at child startup as a restart loop. The preflight half is now **CLOSED** by **PR #50** (`fix/verify-workspace-root-preflight`, reviewed head `3452b33278ed250f00fc4a173d32a66e530fac9c`), **MERGED** 2026-07-28 as `827c50015f8d3af3f6bb6c2c9f581097edf6b839`; production preflight stays `ok=true source=21/21 deployment=2/2`, and this refresh is the anchor refresh that permits recording it as closed. The supervisor/`config.js` coupling is unchanged and remains open. Gate 4 remains unauthorized; production remains inactive.

PR #47 (`feat/configurable-workspace-root`) is MERGED into `origin/main` as `e55de773fe3e10e7b1352fd8b2b2b10b40063e87` (reviewed head `d11ac82f62e1bf44e716af7d58b6cd3d77115c30`, the new verified implementation anchor). It adds the server-side `BLACKSPIRE_WORKSPACE_ROOT` contract so Hermes git/build work can target a real writable checkout instead of the immutable release the runtime's cwd points at — the reviewed unit uses `WorkingDirectory=/opt/blackspire-command/current` with `ProtectSystem=strict` and only `shared/` writable, and the workspace root was hardcoded to `.`. The value comes only from the server's own environment, never a request, frontend value, database row, or task payload; `/api/workspaces` stays read-only. Absent leaves the historical `.` default untouched; set, it is validated at startup and fails closed on empty, whitespace-only, relative, missing, non-directory, symlinked, non-git, symlinked-`.git`, and NUL-byte values, never degrading back to `.`. Binding, ports, release layout, workspace allowlists, and policy boundaries are unchanged. This is the narrowly scoped replacement for the only unique work in PR #22, implemented fresh against main without merging, rebasing, or reusing that branch, and deliberately excluding its docker-compose service layout, port publishing, container changes, and host bind-mount. Full suite at the reviewed head: 450 tests, 441 passed, 0 failed, 9 host-conditional skips, trusted inventory `37/37/37/37/37`; build, lint, typecheck, scan, audit, living-memory, and preflight (`source=21/21`) clean; CI and all Vercel checks green. This refresh records the new anchor so an immutable release is never built from stale canonical memory. Gate 3's PASS at `73856891c5f6f35f14c09d1ce5acef39f20c4b25` is unaffected; Gate 4 and production activation remain unauthorized.

PR #44 (`docs/runbook-backup-restore-contract`) is MERGED into `origin/main` as `a78b6395c6176b1dc02d97b7d70807938a3d1beb` (reviewed head `5fa024ec02a7c8677172dd54d9d60579b561da1d`, the new verified implementation anchor), closing tracked next-action item 22. `docs/VPS_RUNTIME_RUNBOOK.md` now documents the contract both scripts actually enforce instead of presenting `PRAGMA integrity_check` as the content check: backup refuses a table-less source, compares the snapshot's table set against the source's, leaves no artifact or sidecar on refusal, and stays unpinned from the current application schema so a pre-migration backup is still possible; restore rejects a zero-byte backup before opening it, proves full Blackspire schema completeness before and after copying, publishes atomically via a temp file plus a non-overwriting link, and leaves no target or temp artifact on any refusal. Because that file sits outside the six-file canonical-memory allowlist, the post-merge checker correctly returned `UNREVIEWED_NON_DOCUMENTATION_CHANGE`, and this docs-only refresh records the new anchor to clear it. The Gate 3 PASS below was rehearsed at `73856891c5f6f35f14c09d1ce5acef39f20c4b25` and is unaffected, since PR #44 changed documentation only. Gate 4 and production activation remain unauthorized.

**Gate 3 data-protection rehearsal PASSED** (disposable-only) at trusted `origin/main` `73856891c5f6f35f14c09d1ce5acef39f20c4b25`, run against the immutable release built at that exact SHA rather than a working tree: 36 checks, 0 failures, covering explicit-migration-only enforcement, WAL journal mode, a WAL-safe backup taken with an open writer and 8272 uncheckpointed WAL bytes, backup mode/checksum/integrity/schema validation, restore preserving the WAL-committed row with no temp artifact, eight fail-closed restore failure paths (including the exact Gate 3 Finding #1 0-byte-plus-matching-sidecar scenario), two fail-closed backup failure paths, a disposable-release-root rollback rehearsal with a negative proof that the `root:blackspire` ownership contract is enforced, and confirmation that no backup timer or cron is installed. The disposable workspace was removed and verified absent; port 8787, staging 8788 (PID/uptime), nginx (PID/start time), and staging health (200) were unchanged with neither service restarted; production stayed disabled/inactive with its `current` symlink, environment, and database absent; all six prior releases were preserved. `PRODUCTION_CHANGED: no`. `GATE_3_DATA_PROTECTION: PASSED`. Gate 4, production activation, the installed-unit mismatch, and the six host-side blockers all remain open and unauthorized; `READY_FOR_PRODUCTION_ACTIVATION: no`.

PR #41 (`fix/restore-reject-empty-invalid-backup`) is MERGED into `origin/main` as `d800d283b11b03d88c519e18abbed0870d9fa750` (reviewed head `40e6d6a873e1579b91004736ba55e2ab87f2e462`, the new verified implementation anchor). It closes Gate 3 Finding #1 on both the restore and backup sides. `scripts/restore.js` now rejects zero-byte backups before opening them, proves the backup carries the full required Blackspire schema before copying, re-validates the copy, and publishes atomically via a temp file plus a link that refuses to overwrite an existing destination; the shared 19-table contract moved to `packages/shared/schema-validation.js`. The fresh independent exact-head review of the restore change found no defects under adversarial probing and found one real finding on the sibling script — `scripts/backup.js` reported `{"ok":true}` with a matching checksum sidecar for a 0-byte or schema-less source, so a worthless snapshot would only be discovered during an actual recovery. That was reproduced, fixed, and regression-tested (three regression tests proven to fail without the fix): backup now refuses a source with no tables and re-proves the snapshot reproduced the source's table set, while deliberately staying unpinned from the current application schema so a pre-migration backup is still possible. At the merged head, 432 full tests ran with 423 passed, 0 failed, and 9 host-conditional skips under Node 22.23.1; build, lint, typecheck, secret scan, high audit, and preflight (`source=21/21`) were clean; GitHub CI and all Vercel checks passed at the exact reviewed head. This refresh records that anchor so an immutable release is never built from stale canonical memory. Updating `docs/VPS_RUNTIME_RUNBOOK.md` to describe the new backup-side checks is a tracked follow-up: it is not in the canonical-memory allowlist, so it requires its own reviewed implementation-bearing change rather than a docs-only refresh. Gate 3 has not yet been rerun at this anchor; Gate 4 and production activation remain unauthorized and untouched.

PR #39 (`docs/refresh-implementation-anchor-after-pr38`) is MERGED into `origin/main` as `1a28343f7d3ea3c2968799bc57fb13555fc73787` (reviewed head `a4feeeb2d693f66f85b6fb5814032124bfe47fe5`); `scripts/check-living-memory.sh` passed `CANONICAL_MEMORY_ONLY_DESCENDANT` against the unchanged anchor `bc5a817231c1a325f31a112e256425140bbbf073`. Trusted `origin/main` at that exact SHA passed repository validation and was built into the immutable release at `/opt/blackspire-command/releases/1a28343f7d3ea3c2968799bc57fb13555fc73787` (matching `COMMIT_SHA`), with the release manifest, ownership/permissions, and release-tree preflight all passing. A bounded private blue-green boot on loopback `127.0.0.1:8789`, using disposable SQLite state under a private temporary directory with providers disabled and Hermes mock/bounded and Telegram dry-run/disconnected, passed: repeated `/health` and root HTTP 200, readiness reporting providers unconfigured, disposable task creation/retrieval, a harmless task failing closed before unauthorized provider dispatch, and prohibited funds/credential actions denied before Hermes/worker dispatch, with no real provider, Hermes, or Telegram contact and no secrets in logs. Teardown terminated the private descendants, released port 8789, and removed all temporary/disposable state while preserving the immutable release and every prior rollback release. Before/after host checks confirmed port 8787, staging (8788 PID/uptime), staging/public-preview health (200), and nginx (PID/start time) were unchanged, neither nginx nor staging was restarted, and production stayed disabled/inactive with its `current` symlink/environment/database absent, providers disabled, and real Hermes/Telegram disconnected; `PRODUCTION_CHANGED: no`. The private startup wrapper's lack of signal forwarding to its child processes is recorded as a separate future tooling-hardening item, not a failed validation — teardown still reaped every descendant directly. `READY_TO_REQUEST_GATE_3_DATA_PROTECTION: yes`. Gate 3, Gate 4, the installed-unit mismatch, the production `current` symlink/environment/database, and production activation itself all remain unresolved blockers; `READY_FOR_PRODUCTION_ACTIVATION: no`. Trusted `origin/main` is resolved live by `scripts/check-living-memory.sh`, not pinned to a fixed SHA in this document. The verified implementation anchor is `bc5a817231c1a325f31a112e256425140bbbf073`, PR #38's independently reviewed head, contained by PR #38's merge commit `ce6dcecf5335233656392248a67326a2524521ea`. PR #38 (`fix/living-memory-docs-descendant-policy`) replaced the prior self-invalidating living-memory main-equality contract with the fail-closed canonical-memory-only descendant policy and independently re-validated the docs-only descendant fix; independent exact-head review returned 0 Critical, 0 High, 0 Medium, and 0 Low findings, and GitHub CI and Vercel checks passed at the exact reviewed head. Because PR #38 changed `scripts/check-living-memory.sh` and `tests/living-memory-ancestry.test.js` (non-documentation files), the immediate post-merge checker run correctly returned `UNREVIEWED_NON_DOCUMENTATION_CHANGE`; this docs-only refresh records the new reviewed anchor and satisfies that requirement. The prior verified implementation anchor was PR #36's reviewed head `bb727976c47eb430af1587c3f361864aaa7d15f4` (merge `6f40a4443f498e77a8a2ee1e17be6da4a4837e67`, merged 2026-07-26T00:36:23Z with parents base `693fb03e4596d26e990f87a40508307810cc5e5d`, the PR #35 merge); trusted `origin/main` advanced past that anchor through PR #37's canonical-memory-only merge `5424cde6c4045424f3df8de952fb23e22cd5d500` before PR #38. PR #37 had recorded `origin/main` as an exact-equality SHA, which made its own refresh commit invalidate that recorded value the instant it merged (a self-invalidating loop); PR #38's checker instead proves every change since the anchor is contained in the canonical-memory Markdown allowlist. The prior PR #34 merge remains `14509f22f07a761bbd7314416287faf6710cfc39`, the PR #32 merge remains `2428f06769c34990f333c95da9937e6b9c936339`, and the PR #30 merge remains `588ea6e1156d7d39e7f8a5a7bde44f8c4e519807`. `READY_FOR_HOST_SIDE_READINESS_WORK: yes`; `READY_FOR_VPS_PRODUCTION_DEPLOYMENT: no`.

PR #36 merged with an independent exact-head review returning zero findings; GitHub CI and Vercel checks passed at the exact reviewed head `bb727976c47eb430af1587c3f361864aaa7d15f4`. The corrected checker binds to its physical repository, exact trusted origin URL/refspec, exact `refs/remotes/origin/main`, canonical repository-format object IDs, `/usr/bin/git`, sanitized Git configuration/environment, complete non-shallow/non-partial history, and replacement/graft/alternate-free object storage; exact-direction ancestry is `reviewed ancestor -> trusted origin/main` and reverse ancestry is rejected. The living-memory implementation is now on trusted `origin/main`. Immutable-release creation stopped because canonical memory had not yet been refreshed after the merge; **no release was created**, and PR #37/PR #38 correct that in stages. No readiness transition, Gate 3, Gate 4, or production activation has occurred through PR #36, PR #37, PR #38, or this refresh. Issue #33 remains separate and unaddressed.

Separately, a staging/nginx outage (root-caused to a 2026-07-24 10:03 UTC mass service-kill) recovered on 2026-07-26: `blackspire-command-staging.service` and `nginx` are both active again, local staging health and public preview both return HTTP 200. Ports 8787 and production remained untouched throughout the outage and its recovery. The nginx restart-policy and staging clean-exit resilience gap this outage exposed is a separate future reliability task, not part of PR #36.

Blackspire currently has two distinct surfaces: the Vercel-hosted Next.js public frontend and the root Node.js Blackspire Command control plane. Command includes Jarvis, Hermes orchestration, Telegram bridge code, deterministic policy/approvals, workspace and budget controls, evidence/audit, emergency controls, workers/providers, and SQLite persistence.

The Unified Input foundation and environment-readiness work is on local branch `feature/unified-input-foundation`. Its four original unpublished commits are preserved, backed up by `backup/unified-input-foundation-9bdfa5f`, and integrated non-destructively with `origin/main` by merge `b270ad3`. The locally fixed repository-creation regression now fails closed before Hermes/provider/worker dispatch; 139 full tests pass. Real Telegram and live providers remain disconnected/unverified.

The credential-free Restricted Hermes Readiness Foundation is locally verified at `ac3d887`. It adds a strict version 1 low-risk contract, exact and size-bounded response validation, a loopback-only fake restricted adapter, one Hermes/provider dispatch guard, explicit provider allowlisting with mock default, paid-provider isolation outside production, propagated deadlines/cancellation references, replay prevention, cancelled-state finality, and evidence redaction. The expanded suite has 148 tests; no real Hermes, credential, paid provider, or production surface was used.

A subscription-authenticated Codex worker adapter is implemented and live-validated. Codex CLI 0.144.5 reported ChatGPT authentication. The final authorized invocation used the official noninteractive interface exactly once, produced four sanitized JSONL records with no error event, passed the exact version 1 response contract, and completed canonical task state. There was no timeout, retry, fallback, observed tool call, API key, or standalone Blackspire Responses API call; disposable state was removed. The restricted subscription Codex worker path is ready for separately approved restricted work.

The VPS is the planned sole durable Command state owner. Codespaces are disposable development/recovery/test environments, currently limited by exhausted usage credit. Vercel remains the public frontend owner and must not be assumed to host stateful Command SQLite.

Operator iPhone Safari acceptance is complete at `eceb921`: harmless task, shared-conversation follow-up, idempotent replay, Telegram policy denial, eligible cancellation, and bounded mock delivery failure passed. The disposable application, authentication material, SQLite workspace, listener, and Quick Tunnel were removed and verified absent afterward. Active blockers are the unpublished implementation/integration commits and Codespaces budget. Live Command production state and the canonical Constitution location remain unverified. Monitoring Wave 1 is operator-recorded complete, but current dashboard/alert delivery is unverified.
A disposable VPS-style rehearsal foundation is now merged into `main` via PR #26 (`a9602496`): immutable full-SHA release directories, atomic current symlink switching, SQLite `VACUUM INTO` backups with SHA-256/integrity checks, disposable-only restore, fail-closed manual/no-provider production verification, a signal-forwarding production supervisor, operator-only monitoring/logrotate templates, shallow-checkout-safe release tests, a CI-hardened smoke startup wait, and collision-safe worker claim readback. Independent review returned SAFE_TO_MARK_PR_READY with 228/228 tests, and its non-blocking hardening corrections are now applied: canonical real-path/symlink validation and partial-artifact cleanup for backup/restore, a fail-closed injectable production-runtime verifier (Node major 22, bounded port/timeouts, non-root ownership, no external providers/test mode/implicit migrations), matching shell checks, and a destination-safe atomic release move; the full suite is 253/253 under Node 22.23.1. Independent second review confirmed 253/253 with zero confirmed defects and three informational findings, and the tooling was merged (repository readiness only). Six host-side blockers remain open for separate approval: reverse proxy/TLS, least-privileged runtime ownership, monitoring/log-rotation install plus alert testing, approved production backup/migration, recorded exact rollback target, and readiness tooling in the deployment artifact. Reverse-proxy/TLS and runtime-ownership plans with credential-free verification are prepared as reviewed `ops/` templates only. No live service, database, host configuration, DNS, proxy, credential, or deployment was changed.

The following 2026-07-21 Gate 3 entry is superseded by the 2026-07-27 Gate 3 PASS recorded above and is retained as history. Gate 3 recovery on 2026-07-21 verified the later host staging state: `blackspire-command-staging.service` is active on loopback port 8788, public HTTPS health is 200, the disposable staging database is WAL-mode with `integrity_check=ok`, and production paths remain absent/inactive. The gate stopped before mutations because the actual deployed source violates the explicit-migrations requirement: `scripts/migrate.js` invokes `migrate()` without checking `BLACKSPIRE_RUN_MIGRATIONS`, while normal API/worker imports invoke `migrate()` at module load. No backup, restore, WAL rehearsal, migration rehearsal, timer install, database write, or service restart occurred. Gate 3 and Gate 4 are blocked pending reviewed remediation and a new disposable rehearsal.

PR #28 merged the explicit-migration repair: API, worker, supervisor, and local startup preflight only compatible existing schemas; schema writing is private to `scripts/migrate.js`; and no ordinary wrapper authorizes migration. Disposable fixtures invoke the dedicated command in isolated child processes, while CI scopes exact `BLACKSPIRE_RUN_MIGRATIONS=true` only to its disposable migration command and Codespace readiness does not run migrations. (As written on 2026-07-22 this paragraph ended "Gate 3 remains blocked pending the separate draft PR #29 immutable-release review, a new immutable staging release, and a complete disposable rehearsal." All three conditions have since been satisfied: PR #29 merged as `f0a7b66853b4c983d6930c1b70d1d94cb5f26f36`, staging was rebuilt onto `releases/f0a7b66853b4c983d6930c1b70d1d94cb5f26f36`, and Gate 3 PASSED on 2026-07-27 at trusted `origin/main` `73856891c5f6f35f14c09d1ce5acef39f20c4b25`.)

PR #30 is now merged into `origin/main` (`588ea6e`): deterministic test discovery, the trusted-inventory contract, a PID-namespace trusted test runner that keeps authority in parent memory and runs a tracked repository snapshot under an unprivileged UID with `no_new_privs`/cleared capabilities/ptrace boundary, arrival-order lifecycle validation, and a repository-root `vercel.json` that exits the Ignored Build Step so the VPS-owned Command project is skipped while the separate `frontend` project stays deployable.

Read-only host reconciliation on 2026-07-22 found `/opt/blackspire-command` with immutable staged release `releases/0a9affacaf13dd1b040c5d96eb112d979ab59444` loaded by staging, plus inactive candidate `releases/691973870e0048f273fa7e9251d7f78776e3612b` at `0700 root:root`. No `current` symlink, `/etc/blackspire/command.env`, or production `shared/command.sqlite` exists; `blackspire-command.service` is disabled/inactive. `blackspire-command-staging.service` remains active on `127.0.0.1:8788`, and the public frontend preview `/health` returned the sanitized 200 response. (Both of those host observations are superseded: PR #29 merged later the same day as `f0a7b66853b4c983d6930c1b70d1d94cb5f26f36`, and staging was rebuilt onto `releases/f0a7b66853b4c983d6930c1b70d1d94cb5f26f36`, which read-only inspection on 2026-07-27 confirms it still runs; `releases/0a9affacaf13dd1b040c5d96eb112d979ab59444` is retained as the rollback target.) Connected Vercel logs proved its prior root failure was not an application or environment failure: the VPS-owned repository-root project successfully ran the build check, then failed because external project configuration expected an absent `public` output. Root `vercel.json` now exits the Ignored Build Step successfully on every ref; the repaired root context passes while the separate frontend remains deployable. Providers remain disabled, Telegram remains disconnected, Gate 3 has not run or passed, and no production cutover occurred.

PR #29's source-only immutable-release follow-up adds one shared completed-tree validator used by release creation, explicit preflight, switch, and rollback: it establishes `root:blackspire` / `0755` on the release root and `releases/` parent, normalizes directories/executables to `0755` and ordinary files to `0644`, rejects symlinked ancestors and all dangling, looping, or canonical out-of-tree links, permits only canonical in-tree links, validates type/ownership/modes/containment before creating `.release-complete`, and preserves the active release and shared state on failure. Disposable source archives exercise relative, absolute, nested, chained, dangling, and looping unsafe-link cases and prove their incomplete candidates and markers are removed. It also omits safe review/development metadata from the artifact. That description of its source-only content remains accurate, but the statement that followed it here — that PR #29 had not rebuilt or switched staging and remained unmerged — is superseded: PR #29 merged as `f0a7b66853b4c983d6930c1b70d1d94cb5f26f36` on 2026-07-22T22:09:46Z, and staging was then rebuilt onto `releases/f0a7b66853b4c983d6930c1b70d1d94cb5f26f36` and still runs it. PR #29 was reconciled with merged `origin/main` (PR #30) via a merge commit; only the three canonical-memory docs conflicted, and PR #30's newer test-infra content was preserved. Staging/public health claims in that 2026-07-22 record were historical operator evidence and `UNVERIFIED` at the time; read-only inspection on 2026-07-27 has since confirmed staging active on `127.0.0.1:8788` running `releases/f0a7b66853b4c983d6930c1b70d1d94cb5f26f36` with `/health` returning 200. The clause "Gate 3 remains blocked" that closed this paragraph is superseded — Gate 3 PASSED on 2026-07-27 at trusted `origin/main` `73856891c5f6f35f14c09d1ce5acef39f20c4b25`. No production activation or cutover has occurred at any point.

The Gate 4 production activation attempt stopped safely before any production mutation. Its root cause is now fixed at the source: `scripts/production-supervisor.js` spawned `apps/api/server.js` with no host, so `start()` reached `listen(port, undefined)` and bound `0.0.0.0`, while `PORT` silently defaulted to `8787` — the port already held by the healthy existing Docker API. Production could therefore have bound publicly and collided with a running listener.

`packages/shared/bind.js` is now the single canonical bind/port contract. In the `vps-production` profile it requires `BIND_HOST=127.0.0.1` and rejects wildcard (`0.0.0.0`, `::`, `*`), unspecified, and non-loopback addresses; it requires an explicit `PORT` with no default and no fallback to 8787, and rejects malformed, privileged (< 1024), out-of-range, and the reserved `8787` (existing API/worker) and `8788` (restricted staging). Development behavior is preserved unchanged. Restricted staging was **not** preserved at reviewed head `c43ed58d`, contrary to the earlier claim recorded here: the deployed staging profile sets `NODE_ENV=production`, `BLACKSPIRE_RUNTIME_MODE=production`, `BLACKSPIRE_STATE_OWNER=vps-staging`, `PORT=8788`, and no `BIND_HOST`, and because the runtime mode outranked the explicit state owner, `resolveBindTarget()` classified staging as production and rejected both its absent `BIND_HOST` and its own port 8788 — the launcher's explicit `127.0.0.1` was never reached, since the environment is resolved first. The independent exact-head re-review found this (High) together with a Medium defect in which an imported startup could log an `EADDRINUSE` listen error and stay alive with no listener, because exit was conditional on `IS_ENTRY_POINT || bind.production` and the staging unit imports `start()`. Both are now corrected: an explicit `BLACKSPIRE_STATE_OWNER` is authoritative and outranks the runtime mode (`vps-production` means production; any other explicit owner does not; only an absent or blank owner lets the runtime mode decide), restricted staging resolves loopback `127.0.0.1:8788` on its own, an unrecognized owner is never silently authorized because both `verifyVpsRuntime` and `scripts/verify-environment.sh` require the owner to be exactly `vps-production`, and `start()` now exits on a listen error by default for every real startup path with a test-only argument opt-out that a production profile always overrides. The supervisor resolves the contract once, preflights with a read-only probe, fails closed on an occupied port without terminating or modifying the existing listener, and propagates the exact host and port to both children; the API server resolves the same contract, refuses arguments that disagree with it in production, and exits on `EADDRINUSE` rather than falling back. `verifyVpsRuntime`, `scripts/verify-environment.sh`, the systemd unit, the nginx upstream, the health check, and the production profile now share that contract, and the health check no longer defaults to 8787. The production application port stays loopback-only and is never opened publicly. Read-only inspection on 2026-07-23 confirmed `127.0.0.1:8789` free, so `8789` is the selected future production port (`8790`-`8799` remain reviewed fallbacks via `scripts/select-production-port.js`).

Node 22.23.1 validation of `cd018b00f0bf23bc64e2f645dd833fc7d699cbf1` passed 22/22 focused bind/port tests, 56/56 adjacent supervisor/environment/systemd/release/migration/inventory tests, and the full suite at 315 tests with 308 passed, 0 failed, and 7 pre-existing environment-conditional immutable-release ownership skips; the trusted inventory was `33/33/33/33/33` with zero mutated files and zero remaining descendants. Clean `npm ci --ignore-scripts`, build, lint, typecheck, secret scan, zero-vulnerability high audit, and `git diff --check` passed.

Ports 8787 and 8788 remain protected and untouched. Before/after read-only host checks retained docker-proxy PID 949448 and the API/worker containers, staging PID 1658707 with its original start timestamp, listeners `0.0.0.0:8787` and `127.0.0.1:8788`, public preview and local staging health 200, unchanged nginx/TLS, DNS, and firewall state (`ufw` inactive, 23 normalized iptables rules). Production remains inactive: `blackspire-command.service` is disabled/inactive, no `/etc/blackspire/command.env`, no production `shared/database/command.sqlite`, and no `current` symlink was created. Providers remain disabled and Telegram remains disconnected. Staging was not rebuilt or restarted, Gate 3 was not executed, Vercel was not modified, and production was not changed. The repair is unmerged and draft, pending a fresh independent exact-head review.

Three rounds of independent exact-head review refined the branch. The first found the bind/port implementation sound but required test isolation (real production paths removed from the fixtures) and health-check determinism. The second found and corrected two restricted-staging startup blockers: staging was misclassified as production because the runtime mode overrode an explicit `vps-staging` state owner, and an imported startup could log an occupied-port listen error and stay alive with no listener. The third found the staging and imported-startup blockers genuinely fixed and left one Medium — the trusted test runner (`scripts/trusted-test-runner.js`) executed `run()` with no bounded timeout, so a hung test file could stall it indefinitely. The runner now applies a bounded per-test timeout (120s constant), `scripts/run-tests.js` applies an outer 600s watchdog around the whole contained run, and a timeout fails closed nonzero while the existing process-group and descendant reaping terminates the tree, emits one diagnostic, and leaves no leaked process, listener, or port. Trusted-inventory omission, duplicate, interruption, and hostile-child protections remain intact. The production loopback and explicit-port contract is unchanged, production remains inactive and unchanged, staging was not restarted or rebuilt, ports 8787 and 8788 and their services were untouched, providers remain disabled, and Telegram remains disconnected.
