import {spawnSync} from 'node:child_process';
export function activeModes(){return {openai:process.env.OPENAI_API_KEY?'api':'unconfigured',anthropic:process.env.ANTHROPIC_API_KEY?'api':'unconfigured',codex:process.env.CODEX_API_ENDPOINT&&process.env.CODEX_API_KEY?'direct-api':(spawnSync('codex',['--version'],{encoding:'utf8'}).status===0?'cli':'manual-handoff'),claudeCode:spawnSync('claude',['--version'],{encoding:'utf8'}).status===0?'cli':'unavailable'};}
export function selectProvider(policy={}){const modes=activeModes(); for(const p of policy.preferred||['codex','openai','anthropic','manual']){ if(p==='codex'&&modes.codex) return {provider:'codex',mode:modes.codex}; if(p==='openai'&&modes.openai==='api') return {provider:'openai',mode:'api'}; if(p==='anthropic'&&modes.anthropic==='api') return {provider:'anthropic',mode:'api'}; if(p==='manual') return {provider:'manual',mode:'handoff'};} return {provider:'manual',mode:'handoff'};}
